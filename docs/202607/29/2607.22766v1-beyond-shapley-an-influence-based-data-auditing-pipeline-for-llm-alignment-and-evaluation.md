---
title: "Beyond Shapley: An Influence-Based Data Auditing Pipeline for LLM Alignment and Evaluation"
title_zh: 超越Shapley：一种基于影响力的LLM对齐与评估数据审计流水线
authors: "Yunting Song, Matthew Watson, Peter Grabowski, Jun Qin"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22766v1"
tags: ["query:llm-eval"]
score: 8.0
evidence: 面向LLM对齐和评估的数据审计流水线
tldr: "大型语言模型对齐受数据质量瓶颈制约，现有审计方法如语义去重或LLM-as-a-judge难以捕捉个体记录对模型预测的实际影响。本文提出一种仅推理的数据价值管道，通过语义k-NN邻域映射为有向图，利用条件对数似然变化近似Shapley值评估数据效用，并转化为局部优势指标隔离冲突记录。在HelpSteer2上减少99.1%人工审计搜索空间并发现错误标签，在HH-RLHF中识别数千隐藏偏好反转，并暴露基准标签缺陷。该工作为对齐数据审计提供了高效、数学基础的工具。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22766v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1358, \"height\": 376, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.22766v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1449, \"height\": 405, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22766v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1457, \"height\": 709, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22766v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1461, \"height\": 421, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22766v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1456, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22766v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1456, \"height\": 338, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22766v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1456, \"height\": 343, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22766v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1457, \"height\": 302, \"label\": \"Table\"}]"
motivation: 现有数据审计方法无法捕捉个体记录的实际预测影响，且难以发现深度功能规则冲突和系统性标注错误。
method: 提出可扩展的仅推理数据价值管道，通过语义k-NN邻域映射有向图，利用零样本和单样本条件对数似然变化评估数据效用，再转化为局部优势指标隔离梯度冲突记录。
result: "在HelpSteer2上减少99.1%人工审计空间并发现错误标签，在HH-RLHF中识别数千隐藏偏好反转，并暴露基准标签缺陷。"
conclusion: 本文提供了数学基础的高效诊断工具，能发现人类标签错误、清理评估基准，确保LLM对齐数据完整性。
---

## 摘要
大语言模型（LLM）的对齐正日益受到数据质量的瓶颈。随着数据集规模的扩大，大规模偏好和指令调优语料库不可避免地积累了隐藏的结构性矛盾、安全风险以及系统性的人工标注错误。诸如语义去重或LLM-as-a-judge等标准数据集审计方法难以捕捉单个记录对预测的实际影响，且常常遗漏深层的功能规则冲突。为解决此问题，我们引入了一种可扩展的、仅需推理的数据价值评估流水线，该流水线无需迭代模型重训练即可近似Shapley值。通过将语义k近邻邻域映射为有向图，我们的框架利用参考LLM的概率分布，通过零样本和单样本条件对数似然变化直接评估数据效用。随后，该流水线将这些预测影响力分数转化为局部优势指标，以隔离梯度冲突的记录。我们展示了该流水线在两个经过严格审查的对齐数据集上的净化效果。首先，将我们的流水线应用于HelpSteer2数据集，将人工审计搜索空间减少了99.1%，成功发现了不同失败模式下的错误标注记录。其次，将我们的自动审计策略应用于Anthropic的HH-RLHF训练和评估划分时，识别出数千个隐藏的安全性和事实性偏好反转。关键在于，通过将审计扩展到评估划分，我们揭示了当前基准测试完整性的严重漏洞：能力强大的模型常常预测出更安全或更有帮助的响应，却受到客观上存在缺陷的人类真实标签的惩罚。总体而言，我们的工作提供了一种数学基础坚实、高效便捷的诊断工具，用于揭示人类标签错误、净化评估基准，并确保LLM对齐数据的完整性。

## Abstract
The alignment of Large Language Models (LLMs) is increasingly bottlenecked by data quality. As datasets scale, massive preference and instruction-tuning corpora inevitably accumulate hidden structural contradictions, safety risks, and systemic human annotation errors. Standard dataset auditing methods, such as semantic deduplication or LLM-as-a-judge, struggle to capture the actual predictive impact of individual records and often miss deep functional rule clashes. To address this, we introduce a scalable, inference-only data valuation pipeline that approximates the Shapley value without iterative model retraining. By mapping semantic k-NN neighborhoods into a directed graph, our framework evaluates data utility directly through a reference LLM's probability distribution using zero-shot and one-shot conditional log-likelihood shifts. Our pipeline then translates these predictive influence scores into localized advantage metrics to isolate gradient-conflicting records. We demonstrate the pipeline's efficacy in sanitizing two heavily vetted alignment datasets. First, applying our pipeline to the HelpSteer2 dataset reduced the manual audit search space by 99.1%, successfully uncovering falsely-labeled records across diverse failure modes. Second, applying our automated audit strategy to Anthropic's HH-RLHF training and evaluation splits identified thousands of hidden safety and factual preference inversions. Crucially, by extending this audit to the evaluation split, we expose severe vulnerabilities in current benchmark integrity: highly capable models frequently predict the safer or more helpful response, only to be penalized by objectively flawed human ground-truth labels. Overall, our work provides a mathematically grounded, highly efficient diagnostic tool to uncover human label failures, sanitize evaluation benchmarks, and ensure the integrity of LLM alignment data.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）的对齐严重依赖数据质量，但大规模偏好数据和指令调优语料库不可避免地积累噪声、冗余和结构性矛盾。现有数据审计方法（如语义去重、LLM-as-a-judge）无法捕捉个体记录对模型预测的实际影响，且难以发现深层的功能规则冲突。
- **研究动机**：需要一种可扩展、数学基础坚实的数据价值评估工具，能够在不重新训练模型的情况下，精确评估每条数据对模型预测分布的贡献，从而揭示隐藏的错误标签、安全风险和基准测试缺陷。
- **整体含义**：本文提出一种仅基于推理的数据审计流水线，近似Shapley值，用于高效发现大规模数据集中的结构性矛盾，提升LLM对齐数据的完整性。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：用零样本和单样本上下文学习作为代理，近似数据点的Shapley边际贡献；通过语义k-NN邻域构建有向图，并将条件对数似然变化转化为局部优势指标。
- **关键步骤**（如图1所示，分为两阶段8步）：
  - **阶段1：影响力测量**
    1. 生成嵌入并找到k-NN。
    2. 生成零样本和单样本提示集。
    3. 通过目标LLM计算条件对数似然。
    4. 构建有向图（节点为记录，边为影响力分数）。
  - **阶段2：指标推导与审计**
    5. 计算优势指标（Exemplified Advantage、Exemplifying Advantage）和拓扑唯一性指标（One-of-a-kind score）。
    6. 提取结构性异常（基于阈值和互近邻约束）。
    7. 专家仲裁（人类或LLM）。
    8. 标记基准漏洞并净化数据集。
- **关键技术细节**：
  - **影响力分数∆LL**：单样本对数似然减去零样本对数似然，正值表示正向贡献，负值表示矛盾。
  - **优势指标**：将原始分数在局部邻域进行Z-score归一化，得到相对冗余度和相对预测贡献。
  - **异常检测**：针对有显式标签的数据，采用两阶段监督审计；针对隐式标签数据，采用无监督一致性审计。
  - **理论近似**：通过1-shot上下文学习近似参数更新、将联盟空间截断至语义邻域、一阶子采样，将Shapley计算复杂度从O(2^N)降至O(K)（K为邻居数）。

### 3. 实验设计：数据集、场景、benchmark、对比方法

- **数据集与场景**：
  - **HelpSteer2**（21,362条记录）：用于奖励模型训练，包含人类有帮助性评分（0-4）。应用监督标签审计，聚焦满分记录（8,434条），寻找标注矛盾。
  - **Anthropic HH-RLHF**（训练集和评估集）：用于偏好对齐，包含多轮对话和成对偏好标签。进行无监督审计（训练集）和评估基准完整性验证（评估集）。
- **Benchmark**：使用全量HH-RLHF评估集作为基线，比较模型在原始集与管道筛选出的异常子集上的分歧率。
- **对比方法**：
  - 语义去重（baseline）无法捕捉功能性效用。
  - LLM-as-a-judge直接评估每条记录（成本高且过度标记）。
  - 纯LLM验证（无数学预过滤）的计算开销和精度对比。
  - 消融实验：替换嵌入模型（BGE-M3 vs Qwen-Embed）和评分模型（Gemma3-12B vs Qwen3.5-9B）。
  - 超参数敏感性分析（邻居大小K、阈值τadv、最小退化邻居数）。

### 4. 资源与算力

- 文中明确说明：所有实验在配备 **4× NVIDIA A100 (80GB) GPU** 的计算节点上进行。
- 具体训练时长未提及，但强调其推理管道复杂度为O(K)前向传播，相比传统重训练高效得多。

### 5. 实验数量与充分性

- **实验组数**：
  - HelpSteer2案例：1个主要实验（监督标签审计），包含77个候选对，最终确认18个矛盾对，10个错误标签记录。
  - HH-RLHF案例：
    - 训练集审计：标注11,663个候选，确认4,238个矛盾（2,841个独特问题记录）。
    - 评估集审计：510个候选，确认108个独特错误记录；然后评估三个模型（Qwen3.5-9B微调版、Qwen3.5-27B、Gemma3-12B）在这些记录上的分歧率。
  - 消融实验：嵌入模型替换（Qwen-Embed vs BGE-M3）、评分模型替换（Qwen3.5-9B vs Gemma3-12B）。
  - 超参数敏感性：邻居大小K（5/10/15/20/25）、阈值τadv（-1.0/-1.5/-2.0/-2.5）、最小退化邻居数（0/5/7/10）。
- **充分性与公平性**：
  - 实验覆盖了两个完全不同类型的数据集（评分式和偏好式），且包含训练和评估划分，场景全面。
  - 对比了多种baseline（语义去重、纯LLM验证），并展示了数学过滤与LLM验证的协同优势。
  - 消融研究验证了方法的鲁棒性，超参数敏感性分析提供了默认参数选择依据。
  - 但缺乏与其他Shapley近似方法（如Cluster Shapley、TAM）的直接定量比较，主要强调自身效率与发现能力。

### 6. 论文的主要结论与发现

- **HelpSteer2**：管道将人工审计空间减少99.1%，发现了四大类标注错误：事实幻觉、格式忽略、学术虚构、功能遗漏。
- **HH-RLHF训练集**：识别出5类偏好反转：安全/无害性违规、幻觉/事实错误、无帮助逃避、结构失败、毒性响应。
- **HH-RLHF评估集**：暴露了基准测试的脆弱性——在108个确认异常记录上，模型分歧率高达62-75%，远高于全集上的29-38%，表明许多标注本身就是错误的。
- **协同必要性**：数学预过滤（降低搜索空间）与LLM验证（精确定位）结合优于任何单一方法。

### 7. 优点

- **高效性**：仅需推理，无重训练，单条评估复杂度O(K)毫秒级前向传播，极大降低审计成本。
- **理论基础扎实**：通过三种近似策略将Shapley值从组合优化转化为可计算的线性推理，保留记录级粒度。
- **通用性**：适用于开放文本生成和成对偏好数据两种范式，支持有监督和无监督审计。
- **实用性**：能发现传统方法忽略的深层结构性矛盾（如评价者偏见），并提供明确的异常记录供专家核查。
- **可解释性**：提供拓扑唯一性指标，帮助数据修剪时保护边界样本。

### 8. 不足与局限

- **依赖LLM验证**：最终确认仍依赖LLM作为专家评估者（如Gemini），可能引入主观偏差和非确定性，重复审计结果可能有波动。
- **精度优先于召回**：严格阈值（τadv=-2）确保高置信度，但可能遗漏部分低信度的真实错误（如108个确认异常仅占全集～30%分歧的很小一部分）。
- **计算开销**：虽然单次推理快，但对每个记录需对其所有k个邻居计算一次前向传播，在超大规模数据集上仍有线性增长。
- **未与其他Shapley近似方法直接比较**：实验聚焦于自身效果，缺乏与Cluster Shapley、TAM等方法在相同数据集上的量化对比。
- **应用限制**：邻域构建依赖嵌入质量，对极端多样或领域狭窄的数据可能敏感；阈值选择需手动调整，可能影响结果稳定性。
- **未来方向**：论文提出可将连续优势指标作为动态损失权重、生成对抗性负例等，但尚未实验验证。

（完）
