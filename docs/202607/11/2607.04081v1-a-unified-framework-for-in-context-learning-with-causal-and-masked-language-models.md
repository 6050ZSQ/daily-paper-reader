---
title: A Unified Framework for In-Context Learning with Causal and Masked Language Models
title_zh: 因果与掩码语言模型上下文学习的统一框架
authors: "Chenrui Liu, Chuanlong Xie, Falong Tan, Yicheng Zeng, Lixing Zhu"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04081v1"
tags: ["query:llm-lang-rep"]
score: 9.0
evidence: 提出了统一框架分析因果和掩码语言模型中的上下文学习，揭示了上下文表示机制
tldr: 上下文学习（ICL）是预训练语言模型的核心能力，但现有理论主要围绕因果语言模型。该工作提出了统一统计学习框架，将自回归和掩码预训练目标纳入共同的风险分析，通过经验测度表示上下文示例。在Wasserstein正则条件下，推导出两者在k-shot推理时具有相同阶的误差上界，并考虑了任务分布偏移。实验表明掩码对编码器（MPE）性能与GPT-2相当，说明ICL并非因果模型特有。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04081v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1582, \"height\": 918, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04081v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1726, \"height\": 371, \"label\": \"Table\"}]"
motivation: 现有ICL理论分析主要针对因果语言模型，掩码语言模型的ICL机制尚不明确，需统一框架理解其共性。
method: 将上下文示例表示为经验测度，建模预测为上下文和查询的函数，在Wasserstein正则条件下统一分析自回归和掩码预训练的风险界。
result: 自回归和掩码预训练在k-shot推理时达到相同阶的误差上界，且分布偏移上界由提升Wasserstein距离控制；MPE性能接近GPT-2。
conclusion: ICL行为并非因果语言模型特有，掩码模型也能达到类似效果，为理解不同架构的ICL机制提供统一理论基础。
---

## 摘要
上下文学习已成为预训练语言模型的核心能力，但其理论分析主要集中在自左向右自回归预测训练的因果语言模型（如GPT风格模型）上。掩码语言模型则从双向上下文中恢复掩码标记，其在上下文学习中的作用仍不甚明了。我们开发了一个统计学习框架，通过经验测度表示上下文示例，并将预测建模为上下文和查询的函数。该框架在共同的超额风险分析下统一了自回归和掩码预训练目标。在Wasserstein型正则性条件下，我们将具有T个任务、每个任务N个样本的预训练与推理时的k-shot超额风险联系起来，为掩码和自回归目标获得了相同阶的上界。我们还研究了任务分布偏移，其中预训练任务从P中采样，推理任务从Q中采样；所得边界包含一个附加项，由P和Q之间的提升Wasserstein距离控制。这些边界进一步暗示，在固定预训练数据预算下可实现阶最优分配，并在内在低维结构下获得改进的速率。在受控函数学习任务上的实验表明，掩码对编码器可以取得与GPT-2风格因果Transformer相当的性能，这表明上下文学习行为并非因果语言模型所特有。

## Abstract
In-context learning (ICL) has emerged as a central capability of pretrained language models, yet its theoretical analysis has focused primarily on causal language models trained by left-to-right autoregressive prediction, such as GPT-style models. Masked language models instead recover masked tokens from bidirectional context, and their role in ICL remains less understood. We develop a statistical learning framework that represents the context examples by their empirical measure and models prediction as a function of the context and the query. This formulation places autoregressive and masked pretraining objectives within a common excess-risk analysis. Under Wasserstein-type regularity conditions, we relate pretraining with T tasks and N samples per task to k-shot excess risk at inference, obtaining same-order upper bounds for masked and autoregressive objectives. We also study task-distribution shift, where pretraining tasks are sampled from P and inference tasks from Q; the resulting bound contains an additional term controlled by the lifted Wasserstein distance between P and Q. The bounds further imply an order-optimal allocation under a fixed pretraining data budget and refined rates under intrinsic low-dimensional structure. Experiments on controlled function-learning tasks show that the Masked Pair Encoder (MPE) can achieve performance comparable to GPT-2-style causal Transformers, suggesting that ICL behavior is not specific to causal language models.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **研究动机**：上下文学习（In-Context Learning, ICL）是预训练语言模型的关键能力，但现有理论分析主要集中于因果语言模型（如GPT系列），其预训练基于自回归（左到右）的下一词预测。掩码语言模型（如BERT）通过掩码预测训练，其ICL机制尚未得到充分的理论理解。两类模型的预训练目标（自回归 vs. 掩码）在上下文构造方式上不同（前缀 vs. 留一法），导致现有分析不能直接迁移。本文旨在回答：**自回归与掩码预训练目标在ICL的超额风险和扩展行为上有何差异？**
- **整体含义**：通过建立统一的统计学习框架，证明两类预训练目标在k-shot推理时具有相同阶的误差上界，从而说明ICL行为并非因果语言模型所特有，掩码模型也能达到类似效果。这为理解不同架构的ICL机制提供了统一理论基础，并具有指导预训练数据分配和分布外泛化的实用意义。

### 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将上下文示例表示为**经验测度**（empirical measure），将预测建模为上下文测度和查询输入的函数 `f(ρ, x)`。这样，自回归与掩码预训练的区别仅在于每个预测所使用经验测度的构造方式：自回归使用前缀测度 `ρ̂^{(<j)}`，掩码使用留一法测度 `ρ̂^{(-j)}`。该抽象使得两类目标可在同一超额风险分析框架下比较。
- **关键技术细节**：
  - **统计模型**：每个任务对应一个分布ρ，从元分布P中采样。预训练时得到T个任务各N个样本。定义掩码风险 `R̂_{T,N}` 和自回归风险 `R̅_{T,N}`。理想ICL预测器最小化期望风险 `R_P(f)`。
  - **Transformer架构**：将每个（输入，标签）对视为一个令牌，查询标签用占位符 `y_mask` 替代。自注意力层被看作依赖于经验测度的映射，并证明这类预测器满足联合Lipschitz性（定理1）：`||f(ρ,x)-f(ρ',x')|| ≤ L_f (W_1(ρ,ρ') + ||x-x'||)`。
  - **超额风险分解**：定理6给出统一分解，包含三项：任务级泛化误差（通过经验元分布与真值的Wasserstein距离）、任务内采样误差（经验任务分布与真值）、训练目标上下文缩减误差。自回归额外多一项 `O(N^{-1})` 的边界项。
  - **速率结果**：在Wasserstein维度假设（d^*_{1,q} ≤ s）下，得到共同速率 `T^{-1/s} + N^{-1/(d_x+d_y)} + N^{-1}`（推论7）。再推广到k-shot推理（定理9）：额外增加 `k^{-1/(d_x+d_y)}` 的推理误差。
  - **任务分布偏移**：定理12、13给出转移界，包含 `W_1(P,Q)` 项。
  - **最优数据分配**：在总预算 `T N = B` 下，最优分配 `T ≍ B^{s/(s+d_x+d_y)}`，`N ≍ B^{(d_x+d_y)/(s+d_x+d_y)}`，得到率 `B^{-1/(s+d_x+d_y)}`（推论11）。
  - **低维结构**：若数据支持在流形或可分簇结构上，可改进为内在维度rate（推论14）。

### 实验设计

- **实验场景**：受控的合成函数学习任务，包括：
  1. **线性回归**（d=20，无噪）
  2. **带噪线性回归**（σ=1，仅评估无噪训练检查点）
  3. **决策树回归**（深度4，随机分割和叶值）
  4. **两层ReLU网络回归**（h=100，随机权重）
- **Benchmark与对比方法**：
  - 本模型：**掩码对编码器（Masked Pair Encoder, MPE）**，使用双向Transformer，对（x_i,y_i）标记，掩码标签预测训练（K=8个掩码目标）。
  - 基线：**GPT-2风格因果Transformer**（从零训练，遵循Garg et al. 2022协议）；**普通最小二乘（OLS）**、**3近邻**、**贪婪决策树**、**每提示拟合的两层ReLU网络**等。
- **评估指标**：均方误差（MSE），线性回归和ReLU网络任务除以输入维度d。

### 资源与算力

- **文中未明确说明**具体的GPU型号、数量或训练时长。仅给出了超参数：批量大小64，学习率1e-4，隐藏层宽度256，8注意力头，层数6或12，步数200k-500k（见表1）。未提供硬件资源细节。

### 实验数量与充分性

- **实验数量**：进行了四个主要合成任务（线性、带噪线性、决策树、两层ReLU），每个任务绘制了不同上下文数下的MSE曲线，并与多种基线对比。此外，还进行了“可变密集留一法”实验（文中提及但未详述）。
- **充分性**：
  - **优点**：覆盖了线性与非线性函数类，包括无噪和带噪设置，对比了多个经典基线，结果清晰支持理论结论（MPE与GPT-2性能相当）。
  - **不足**：仅限合成任务，**未在自然语言基准上评估**；未进行消融实验（如不同K值、层数、注意力头数的影响）；实验设置相对理想化（无位置编码、在线生成数据），可能低估实际部署中的挑战。

### 论文的主要结论与发现

1. **统一框架**：将自回归和掩码预训练纳入同一超额风险分析，两者具有相同阶的k-shot误差上界。
2. **理论速率**：主导项为 `T^{-1/s} + N^{-1/(d_x+d_y)} + k^{-1/(d_x+d_y)} + N^{-1}`，掩码和自回归一致。
3. **转移性**：任务分布偏移引入 `W_1(P,Q)` 项，可量化泛化损失。
4. **最优分配**：固定预算下存在平衡任务多样性和样本量的最优分配。
5. **低维加速**：若数据有内在低维结构，可进一步改善N和k项。
6. **实验证实**：掩码对编码器在四种合成函数类上取得与GPT-2因果Transformer相当或接近的性能，表明ICL并非因果模型特有。

### 优点

1. **理论创新性**：首次将掩码语言模型的ICL纳入严格统计分析，填补了理论空白。
2. **框架统一性**：通过经验测度抽象，简洁地统一了两类预训练目标，使比较成为可能。
3. **结果全面**：覆盖标准设置、分布偏移、最优分配、低维结构，展示丰富的理论内涵。
4. **实验支持**：合成任务验证了理论核心推测，方法透明可复现（代码已开源）。

### 不足与局限

1. **理想化假设**：理论分析基于理想化的自回归和掩码目标，未完全模拟实际语言模型的掩码方案（如随机部分掩码、位置编码等）。
2. **实验覆盖有限**：仅在合成函数学习任务上验证，未涉及真实NLP基准（如分类、QA），普适性存疑。
3. **算力信息缺失**：未报告计算资源，影响实际部署评估。
4. **消融研究不足**：未分析不同模型大小、K值、训练步数对性能的影响，也未比较不同的掩码策略。
5. **潜在偏差风险**：合成任务可能偏好掩码对编码器设计（如成对令牌表示），需在实际语言任务中验证公平性。
6. **应用限制**：理论速率依赖Wasserstein维度等抽象假设，实际中难以验证或估计，指导具体训练设计的直接性有限。

（完）
