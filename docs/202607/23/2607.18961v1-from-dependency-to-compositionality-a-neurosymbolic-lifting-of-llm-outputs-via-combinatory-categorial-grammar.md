---
title: "From Dependency to Compositionality: A Neurosymbolic Lifting of LLM Outputs via Combinatory Categorial Grammar"
title_zh: 从依存到组合性：通过组合范畴语法对大型语言模型输出的神经符号提升
authors: Remo Pareschi
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18961v1"
tags: ["query:llm-ling"]
score: 9.0
evidence: 将LLM输出提升为组合范畴语法推导，以理解其如何处理句法和语义
tldr: LLM生成文本缺乏可解释语法，但前缀驱动的类型完成动态与CCG增量处理模型吻合。本文提出神经符号框架，将LLM输出提升为CCG组合推导，并通过Curry-Howard对应扩展到编程和查询语言。该框架支持组合层和内容层两层检查，可及早检测结构失败和幻觉内容，提升可审计性，且不要求LLM内部实现CCG。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18961v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1504, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 705, \"height\": 543, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1330, \"height\": 539, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1094, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1612, \"height\": 842, \"label\": \"Table\"}]"
motivation: 解决LLM生成文本缺乏可解释语法结构的问题，利用CCG增量处理模型适配LLM前缀生成动态。
method: 提出神经符号框架，将LLM输出重构为CCG类型化组合推导，通过Curry-Howard对应扩展到形式语言。
result: 实现组合层和内容层两层检查，可及早检测结构失败和幻觉内容，提升输出可靠性。
conclusion: 框架仅需前缀驱动生成特性，不要求认知能力，为LLM输出提供可审计组合推导。
---

## 摘要
大型语言模型通过从前缀逐步预测下一个标记来生成流畅文本。生成传统中的批评者认为，这类系统缺乏真正的语法；来自依存语法视角的有影响力的回应则认为，LLM的行为可以通过逐词构建的局部中心-依存结构得到良好描述。我们认为，一个更敏锐的观察被忽视了：自回归生成的前缀驱动、类型补全动态与组合范畴语法最初设计所支持的增量处理模型高度吻合。在此基础上，我们提出了一个神经符号框架，将LLM输出提升为类型化的组合推导——这并非声称LLM内部实现了CCG，而是说它们的输出允许一种有原则的、增量的且可审计的CCG重构。由此产生两个后果。第一，通过Curry-Howard对应，这种提升从自然语言扩展到LLM也生成的形式语言——如Solidity编程语言、描述逻辑和查询语言如OWL和SQL——其中类型系统变化而架构保持不变。第二，这种提升支持两层检查：组合层直接捕获结构失败，内容层根据外部知识源检查提升后的结构，从而尽可能早地标记出幻觉内容。因此，这种解释对生产者不要求认知能力，而只要求一个前缀驱动的生成模式。我们最后勾勒了同步LLM-CCG耦合作为该框架开放的一个方向。

## Abstract
Large language models (LLMs) generate fluent text by incrementally predicting the next token from a prefix. Critics in the generative tradition argue that such systems lack genuine grammar; influential replies from the dependency-grammar perspective hold that LLM behavior is well described by local head-dependent structure built word by word. We argue that a sharper observation has been overlooked: the prefix-driven, type-completing dynamics of autoregressive generation align closely with the incremental processing model that Combinatory Categorial Grammar (CCG) was originally designed to support. On this basis we propose a neurosymbolic framework in which LLM outputs are lifted into typed compositional derivations -- not claiming that LLMs implement CCG internally, but that their outputs admit a principled, incremental, and auditable CCG reconstruction. Two consequences follow. First, through the Curry-Howard correspondence the lifting extends beyond natural language to the formal languages LLMs also produce -- programming languages such as Solidity, description-logic and query languages such as OWL and SQL -- with the type system varying and the architecture held fixed. Second, the lifting supports two layers of checking: a compositional layer that catches structural failures directly, and a content layer that checks the lifted structure against external knowledge sources, enabling the earliest possible flagging of hallucinated content. The account thereby requires of a producer not cognition but a prefix-driven generative profile. We close with a sketch of synchronous LLM-CCG coupling as one direction the framework opens.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）生成的文本虽然流畅，但批评者认为它们缺乏真正的语法结构。依存语法视角的回应认为LLM行为可由局部中心-依存关系描述，但本文指出更深刻的观察被忽略：自回归生成的前缀驱动、类型补全动态与组合范畴语法（CCG）的增量处理模型高度吻合。因此，论文旨在利用这种结构对齐，将LLM输出提升为类型化组合推导，实现可审计的语义解释，并扩展到形式语言（如Solidity、SQL、OWL），同时支持早期检测结构失败和幻觉内容。

- **研究动机**：当前LLM生成文本缺乏可解释的语法结构，而CCG的设计原则与LLM的逐前缀生成模式天然匹配，这种对齐可用于构建神经符号框架，无需修改LLM内部，仅基于输出进行后处理提升。

- **整体含义**：提出一个**分层、保守**的神经符号框架，从依存语法描述层到CCG组合重构层，通过Curry-Howard对应将自然语言提升扩展到形式语言，实现组合层和内容层两层检查，用于早期错误检测和幻觉识别。强调该框架对生产者只要求前缀驱动生成特性，不要求认知能力。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将LLM输出视为前缀驱动的序列，通过CCG语义解析器（如ccg2lambda）将文本解析为CCG推导，产生lambda风格逻辑形式，得到类型化组合的符号对象。这个过程是**事后（post hoc）**的，不修改LLM内部，仅操作其输出。

- **关键技术细节**：
  - **两层提升架构**：第一层为神经生成（LLM产生token序列），第二层为组合CCG层（CCG解析器分配词汇范畴和组合子，产生推导和lambda项）。
  - **增量对齐**：CCG的逐前缀处理特性与LLM的生成步骤对齐。例如，对于句子“John loves Mary”，每个前缀（John、John loves）在CCG中都有明确定义的类型和部分lambda项（如S/NP），与LLM的下一token预测分布一致。
  - **Coherence性质**：由于CCG的推导等价性（组合是结合的），不同推导顺序产生相同lambda项，使提升良定义，不受表面分析歧义影响。
  - **Curry-Howard对应**：将类型提升扩展至形式语言（Solidity、SQL、OWL等），类型系统变化而架构固定。该对应保证了自然语言和形式语言的类型化组合提升具有同一形式基础。
  - **两层检查**：
    - 组合层：检查结构失败（如缺失参数、类型不匹配）。
    - 内容层：将提升后的结构（如谓词）查询外部知识源（如本体、数据库），检测事实错误（如“阿兹特克神宙斯”）。
  - **早期标记**：由于CCG的增量性，可在生成过程中最早可能点触发检查（如结合“The Aztec god Zeus”时立即查询本体发现矛盾）。

## 3. 实验设计：使用了哪些数据集/场景，benchmark，对比方法

- **实验设计**：论文**没有进行任何实验**。它完全是概念性、理论性的架构提案。**没有数据集、基准测试或对比实验**。
- 涉及的应用场景讨论包括：自然语言（如“宙斯”例子）、Solidity智能合约（引用Salzano et al. 2026的研究，显示LLM生成合约功能正确率仅20-26%）、SQL、OWL等，但这些都是作为理论论证的例子，未实施。
- 对比方法：未进行实验对比，仅理论对比了依存语法和原子事实验证方法（FActScore）作为相关工作的位置说明。

## 4. 资源与算力

- **未提及任何计算资源、GPU型号、数量、训练时长等**。论文为纯理论工作，无实验。

## 5. 实验数量与充分性

- **无实验**。因此无法评估充分性或公平性。论文明确属于概念架构贡献，实验被认为“留待后续工作”（如Solidity提升的具体实现）。

## 6. 论文的主要结论与发现

- LLM的自回归生成动态（前缀驱动、类型补全）与CCG的增量处理模型高度吻合，这种对齐为神经符号框架提供了结构动机。
- 通过CCG提升，LLM输出可被重构为类型化组合推导，实现透明、可审计的语义表示。
- 通过Curry-Howard对应，该提升自然泛化到LLM产生的形式语言（编程语言、描述逻辑、查询语言），类型系统变化而架构固定。
- 提升支持**两层检查**：组合层捕获结构失败，内容层针对外部知识源检测事实错误，且由于CCG增量性，可在生成过程中最早可能点触发，优于事后分解方法。
- 该框架对生产者不要求认知能力（mind），只要求前缀驱动生成特性，因此适用于人类和人工系统，避开心智争论。
- 同步LLM-CCG耦合（硬/软约束解码）是开放但有前景的方向，已有类似工作（如PICARD、Synchromesh）在部分形式语言领域成功。

## 7. 优点

- **理论动机强**：精准识别LLM逐前缀预测与CCG增量组合之间的结构对齐，为神经符号集成提供了新颖的基础。
- **分层谨慎**：不声称LLM内部实现CCG，只对输出进行事后重构，降低了实证主张的风险。
- **跨域统一**：利用Curry-Howard对应，自然语言与形式语言（Solidity、SQL、OWL）共享同一架构，类型系统变化而框架固定。
- **早期检测能力**：CCG增量性使组合检查和内容查询可以在生成过程中最早可能点触发，优于事后验证。
- **两层检查清晰分离**：组合层处理结构缺陷，内容层利用外部知识源处理事实错误，分工明确，避免过claim。
- **避免心智争论**：框架只要求生产者具有前缀驱动特性，不涉及心智或理解，使框架对AI和人类语言生产同样适用。

## 8. 不足与局限

- **无实验验证**：论文仅为概念架构，无任何实施或实证结果。理论主张的有效性需要后续具体实现（如Solidity提升的工程化）和评估。
- **多句篇章处理未解决**：当前提升限于句子级别，多句篇章、指代消解、语用上下文等未充分处理。虽然引用了Grenander等人的句子增量指代消解决工作，但整合仍是开放挑战。
- **鲁棒性挑战**：现有CCG解析器主要基于CCGbank（新维华尔街日报语料），训练分布与LLM生成文本的多样域、非正式风格存在差异。虽提及超标签泛化可缓解词汇问题，但结构和语域差异仍是实际障碍。
- **同步耦合的困难**：硬约束解码可能导致解析器引导的“花园路径”错误，即解析器当前分析错误地掩码了合法后续，因此倾向软约束，但未给出具体组合规则。
- **外部知识源依赖性**：内容检查需要可信外部来源，其选择、维护和可信度评估是元问题，框架本身不处理。
- **计算开销**：同步或事后CCG解析可能带来计算成本，论文未讨论效率或可行性考虑。
- **局限性明确陈述**：论文自身在第7节（Discussion）中坦诚指出了这些局限，包括需要端到端实现、篇章处理、解析器鲁棒性评估、下游使用待定等。

（完）
