---
title: "From Dependency to Compositionality: A Neurosymbolic Lifting of LLM Outputs via Combinatory Categorial Grammar"
title_zh: 从依存到组合：通过组合范畴语法对LLM输出进行神经符号提升
authors: Remo Pareschi
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18961v1"
tags: ["query:llm-ling"]
score: 10.0
evidence: 直接将LLM输出与组合范畴语法关联，研究组成性语义
tldr: 大型语言模型自回归生成依赖前缀预测，缺乏组合语法，已有依赖文法描述不充分。本文提出神经符号框架，利用组合范畴语法的增量处理模型将LLM输出重构为类型化组合推导，不假设LLM内部实现CCG。通过Curry-Howard对应，该框架可扩展至Solidity、OWL、SQL等形式语言，并支持组合层与内容层双层检查：组合层检测结构失败，内容层结合外部知识尽早标记幻觉。该工作提供了可审计、可验证的LLM输出解释，推动了神经符号系统发展。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18961v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1504, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 705, \"height\": 543, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1330, \"height\": 539, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1094, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1612, \"height\": 842, \"label\": \"Table\"}]"
motivation: LLM生成缺乏可解释的组合语法，现有依赖文法描述不精确，需可审计的神经符号提升方法。
method: 基于CCG增量类型补全，将LLM输出重构为类型化组合推导，并利用Curry-Howard对应扩展到形式语言。
result: 实现组合层与内容层双层检查：组合层捕获结构失败，内容层结合外部知识检测幻觉，可尽早标记错误。
conclusion: 揭示了LLM生成与CCG对齐，为神经符号系统提供新方向，降低对内部认知的依赖。
---

## 摘要
大型语言模型通过从前缀逐步预测下一个标记来生成流畅文本。生成传统下的批评者认为这类系统缺乏真正的语法；依存语法视角的有力回应则认为，逐词构建的局部中心词-依存结构可以很好地描述LLM的行为。我们认为一个更敏锐的观察被忽视了：自回归生成的前缀驱动、类型完成动力学与组合范畴语法最初设计支持的增量处理模型高度一致。在此基础上，我们提出一个神经符号框架，将LLM输出提升为带类型的组合推导——并不声称LLM内部实现了CCG，而是说它们的输出允许一种有原则的、增量的、可审计的CCG重构。由此产生两个后果。第一，通过柯里-霍华德对应，这种提升超越了自然语言，扩展到LLM也产生的形式语言——例如Solidity等编程语言、描述逻辑与查询语言如OWL和SQL——其中类型系统变化而架构固定。第二，这种提升支持两层检查：直接捕获结构错误的组合层，以及将提升后的结构对照外部知识源检查的内容层，从而能够尽早标记幻觉内容。因此，该理论要求生产者具备的不是认知能力，而是一个前缀驱动的生成特征。最后，我们勾勒了同步LLM-CCG耦合作为该框架开辟的一个方向。

## Abstract
Large language models (LLMs) generate fluent text by incrementally predicting the next token from a prefix. Critics in the generative tradition argue that such systems lack genuine grammar; influential replies from the dependency-grammar perspective hold that LLM behavior is well described by local head-dependent structure built word by word. We argue that a sharper observation has been overlooked: the prefix-driven, type-completing dynamics of autoregressive generation align closely with the incremental processing model that Combinatory Categorial Grammar (CCG) was originally designed to support. On this basis we propose a neurosymbolic framework in which LLM outputs are lifted into typed compositional derivations -- not claiming that LLMs implement CCG internally, but that their outputs admit a principled, incremental, and auditable CCG reconstruction. Two consequences follow. First, through the Curry-Howard correspondence the lifting extends beyond natural language to the formal languages LLMs also produce -- programming languages such as Solidity, description-logic and query languages such as OWL and SQL -- with the type system varying and the architecture held fixed. Second, the lifting supports two layers of checking: a compositional layer that catches structural failures directly, and a content layer that checks the lifted structure against external knowledge sources, enabling the earliest possible flagging of hallucinated content. The account thereby requires of a producer not cognition but a prefix-driven generative profile. We close with a sketch of synchronous LLM-CCG coupling as one direction the framework opens.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）通过前缀预测逐词生成流畅文本，但其输出是否具有真正的语法结构？生成语法学派批评LLM“没有语法”，而依存语法学派（如Gibson）认为LLM行为可由局部中心词-依存关系描述。作者指出这两种视角都忽略了更重要的观察：LLM的自回归生成（前缀驱动的、类型补全的动态过程）与组合范畴语法（CCG）最初设计的增量处理模型高度一致。
- **整体含义**：论文提出一个神经符号框架，将LLM输出“提升”（lifting）为带类型的组合推导（typed compositional derivations），并非宣称LLM内部实现CCG，而是说其输出可被原则性地、增量地、可审计地重构为CCG推导。这一框架有两个延伸：(1) 通过Curry-Howard对应，将自然语言的提升扩展到Solidity、OWL、SQL等形式语言；(2) 支持两层检查：组合层捕获结构错误，内容层结合外部知识尽早标记幻觉。论文最终主张，LLM作为生产者只需“前缀驱动的生成特征”，不需要“认知能力”，从而将问题从心灵哲学争论中脱离出来。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将LLM的输出视为自回归生成的标记序列，利用CCG的增量解析器（如ccg2lambda）将每个前缀（prefix）解析为带类型的范畴和部分lambda项。CCG的独特优势在于：每个单词携带语法范畴和对应的lambda项，组合规则（前向/后向应用、前向/后向组合、类型提升）同时驱动语法和语义组合。这使得LLM生成的每个前缀都有一个明确定义的、带类型的语义状态，与LLM的预测动态在操作上对齐。
- **关键技术细节**：
  - **两层提升框架**：
    1. **神经生成层**：LLM生成标记序列，不修改模型内部。
    2. **组合CCG层**：使用CCG语义解析器（如ccg2lambda）为生成文本分配词汇范畴和组合子，得到推导过程和关联的lambda项。该层显式回答“意义如何组合”以及“已组合部分的类型是什么”。
  - **增量性对齐**：通过示例“John loves Mary”展示，在每个生成步骤，LLM的分布性期望与CCG的范畴化期望一致（例如在“loves”后，LLM预期NP，CCG给出S/NP范畴和部分lambda项）。
  - **派生等价性**：CCG允许多种推导顺序，但通过组合性等价性，同一句子的不同推导产生相同的lambda项，保证了提升的良定义性。
  - **类型即接口**：类型不仅作为组合指令，还作为内容检查的接口：指定何种外部知识可裁决该表达式的正确性以及如何查询。
  - **两层检查**：
    - **组合层**：检查结构是否完整、类型是否匹配。例如Solidity函数缺少require语句导致类型签名弱化。
    - **内容层**：检查提升后的结构（如谓词应用）是否与外部知识源一致。例如“The Aztec god Zeus loved Europa”中“aztec(zeus)”与知识库中“pantheon(zeus)=greek”矛盾，可在前缀“The Aztec god Zeus”处立即标记。
  - **跨域拓展**：通过Curry-Howard对应，将相同架构应用于Solidity、SQL、OWL等形式语言，只需替换类型系统和词汇表。以Solidity为例，展示一个函数setMinDepositAmount：真实版本具有前提φauth ∧ φval和效果State ⊗ Event，而LLM生成版本仅φauth和State，通过类型比较即可检测缺失验证和事件发射的缺陷。
  - **同步耦合**（未来方向）：将增量CCG解析器与LLM解码过程耦合，使符号层在每个解码步骤维护带类型语义状态，约束或重加权生成。软变体组合信号与log概率，硬变体屏蔽不合法标记。该方向部分已有先例（如PICARD用于SQL、Synchromesh、Grammar-guided generation），但缺少带语义的类型状态。

## 3. 实验设计

- **论文性质**：本文属于概念性/理论性论文，并未进行传统的大规模实验。实验部分主要是概念验证示例和案例分析，而非系统性的基准测试。
- **数据集/场景**：无标准数据集。使用了以下示例场景：
  - 自然语言：列举“John loves Mary”的增量提升示例；“The Aztec god Zeus loved Europa” vs “The Greek god Zeus loved Europa”展示内容层检查；真实世界幻觉例子（LLM将Esau的兄弟错认为Elijah）。
  - 编程语言：引用Salzano等人（2026）对Solidity合约生成的实证研究，该研究测试了ChatGPT-4o、Gemini、CodeLlama、DeepSeek-Coder在零样本生成和检索增强生成下的功能正确性（20-26%零样本，最高45%有检索增强）。论文以此作为动机，并未自己运行实验。
- **Benchmark**：无。论文的目标是提出框架，而非评测效果。
- **对比方法**：未与具体方法对比。文中将自身框架与FActScore等原子事实验证方法做了概念对比，但无定量比较。

## 4. 资源与算力

- **未明确说明**：论文完全未提及所使用的GPU型号、数量、训练时长或任何计算资源。作者可能未进行需要大量算力的实验，或实验依赖现有CCG解析器（如ccg2lambda）和LLM（直接调用），但未报告资源消耗。

## 5. 实验数量与充分性

- **实验数量**：严格来说，论文没有设计对照实验、消融实验或定量评测。所有论证基于概念示例和已有文献的引用。
- **充分性**：从学术论文的角度看，本文属于理论贡献和框架设计，因此缺乏实验并不构成缺陷。但是，对于一篇声称可以“检测幻觉”和“验证代码”的论文，缺乏实际实现和定量评估会降低说服力。作者在局限性中承认了这一点，并指出端到端实现和跨段落处理是需要后续工作的。
- **客观性**：论文对自身局限有清晰认识（见第7节），未过度宣称。示例选择合理，但缺乏对抗性案例或大规模统计验证。

## 6. 论文的主要结论与发现

- **主要发现**：
  1. LLM的自回归生成与CCG的增量、前缀驱动、类型补全特性在操作上高度对齐，这比依存语法的描述更深入。
  2. 使用CCG解析器可以将LLM输出提升为带类型的组合推导（lambda项），该推导是可审计的、良定义的（派生等价性保证）。
  3. 通过Curry-Howard对应，提升可扩展到Solidity/OWL/SQL等形式语言，类型系统变化而架构固定。
  4. 提升后的结构支持两层检查：组合层捕获结构错误（如函数缺失验证），内容层结合外部知识（如知识库、本体）检测内容错误（如实体属性矛盾）。CCG的增量性使得检查可在最早可能的前缀进行（如“Aztec god Zeus”处）。
  5. 该框架不需要LLM具备认知能力或内部实现CCG，只需要前缀驱动的生成特征，从而将LLM的语法问题与心灵哲学分离。
  6. 与RAG和智能合约中的oracle模式类似，但CCG提升提供了组合层，使检索内容可被组合式地检查而非统计地使用。

## 7. 优点

- **理论深度**：精准识别了LLM自回归生成与CCG增量处理的深层对齐，超越了现有依存语法视角。
- **形式优雅**：利用CCG的加法组合性和Curry-Howard对应，将自然语言和形式语言统一在同一框架下。
- **实用性潜力**：两层检查（组合+内容）为幻觉检测和形式验证提供了可审计、可增量操作的路径，与RAG、智能合约审计等实际需求契合。
- **保守定位**：不修改LLM，不访问内部，仅作用于输出，降低了实现门槛和承诺风险。
- **清晰的承诺声明**：在4.6节明确列出了框架承诺和不承诺的内容，避免过度解释。
- **跨领域统一**：将自然语言理解、程序验证和形式推理连接起来，体现了神经符号融合的优雅视角。

## 8. 不足与局限

- **缺乏实验验证**：论文没有提供端到端的实现和定量评估，无法证明框架的实际效果（例如幻觉检测准确率、代码验证成功率、性能开销）。
- **多句子处理未解决**：当前框架局限于句子级别；多句子语篇、指代消解、语用依赖等问题未被充分处理。作者虽提及Grenander等人的增量指代消解工作，但未集成。
- **解析器鲁棒性**：现有宽覆盖CCG解析器主要基于新语体（WSJ）训练，对LLM生成的各种领域、语域可能覆盖不足，尤其是同步耦合时解析失败会直接约束生成。
- **内容层依赖外部知识源**：内容检查需要可信的外部知识源，但知识源本身的选择和可信度问题框架未解决。在4.6节作者承认这是元问题。
- **同步耦合未实现**：同步LLM-CCG耦合仅作为未来方向勾勒，没有提供具体算法或实验。硬屏蔽可能引入解析器诱导的“花园路径”错误。
- **Solidity案例分析半手动**：Solidity函数提升示例是手工构造的，未展示如何自动从源代码生成CCG范畴和lambda项。需要设计CCG风格的Solidity词库和解析器，工作量巨大。
- **实验数量：无消融/对比**：与现有方法（如FActScore、直接类型检查）无定量比较，缺乏公平性评估。
- **可解释性未评测**：虽然声称可审计，但没有用户研究或案例分析证明提升后的lambda项对开发者或审查者有帮助。

（完）
