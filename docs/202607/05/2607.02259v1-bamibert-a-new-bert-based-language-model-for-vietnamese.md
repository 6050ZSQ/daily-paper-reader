---
title: "BamiBERT: A New BERT-based Language Model for Vietnamese"
title_zh: BamiBERT：一种新的基于BERT的越南语语言模型
authors: "Dat Quoc Nguyen, Thinh Pham, Chi Tran, Linh The Nguyen"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02259v1"
tags: ["query:llm-lang-rep"]
score: 8.0
evidence: 越南语预训练语言模型，学习语言表示
tldr: 针对现有越南语BERT模型PhoBERT在上下文长度和分词依赖上的局限，本文提出BamiBERT，一个从头训练的预训练语言模型。该模型在129GB语料上训练20轮，支持2048 tokens上下文，无需外部分词。在8个越南语基准测试中，BamiBERT在15项指标上取得11项最优、3项次优，创下base级别越南语编码器的新纪录。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02259v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1623, \"height\": 792, \"label\": \"Table\"}]"
motivation: 现有越南语BERT模型PhoBERT存在上下文长度有限、依赖外部分词等不足，需改进以提升性能。
method: 使用129GB越南语通用语料从头训练BamiBERT，支持2048 tokens上下文，直接处理原始输入无需分词。
result: 在8个越南语基准的15项指标中，BamiBERT获得11项最佳、3项次优，达到base级越南语编码器最优。
conclusion: BamiBERT通过扩展上下文和消除分词依赖，显著提升了越南语表示学习的性能与泛化能力。
---

## 摘要
在本文中，我们介绍了BamiBERT，一种新的基于BERT的越南语预训练语言模型，它解决了当前事实上的越南语文本编码器PhoBERT的关键局限性。BamiBERT在129GB的通用越南语文本语料库上从头训练了20个epoch，支持长达2048个令牌的上下文长度，并直接对原始输入进行操作，无需外部分词。在8个越南语基准测试中，它在15个指标中的11个上取得了最佳成绩，并在另外3个指标上取得了第二好成绩，在“基础”规模的越南语编码器中树立了新的最先进水平，并展示了强大的跨领域泛化能力。我们在以下地址发布BamiBERT：https://huggingface.co/Qualcomm-AI-Research/BamiBERT

## Abstract
In this paper, we introduce BamiBERT, a new BERT-based pre-trained language model for Vietnamese that addresses key limitations of PhoBERT -- the current de facto Vietnamese text encoder. Trained from scratch on a 129GB corpus of general-domain Vietnamese text for 20 epochs, BamiBERT supports an extended context length of up to 2048 tokens and operates directly on raw input, eliminating the need for external word segmentation. Across 8 Vietnamese benchmarks, it achieves the best score on 11 of 15 metrics and the second-best on 3 others, setting a new state of the art among "base"-sized Vietnamese encoders and demonstrating strong cross-domain generalization. We release BamiBERT at: https://huggingface.co/Qualcomm-AI-Research/BamiBERT

---

## 论文详细总结（自动生成）

# 论文总结：BamiBERT：一种新的基于BERT的越南语语言模型

## 1. 核心问题与整体含义（研究动机和背景）
- **现有模型的局限**：当前越南语最广泛使用的BERT模型PhoBERT存在两个关键缺陷：
  - 最大上下文长度仅256个子词，无法处理长文本。
  - 需要外部分词工具（如RDRSegmenter）对输入进行词级分割，增加了部署复杂性和误差传播。
- **研究目标**：开发一个全新的越南语BERT模型，支持长上下文、直接处理原始文本，并实现更优的跨领域性能。
- **整体含义**：BamiBERT旨在替代PhoBERT作为越南语NLP任务的事实标准基线，推动越南语预训练语言模型的发展。

## 2. 方法论：核心思想与关键技术细节
- **核心思想**：从零开始预训练一个BERT-base架构的编码器，采用更大的语料、更长的上下文、原始的字节级BPE分词，无需外部预处理。
- **架构**：
  - 12层Transformer，BERT base尺寸。
  - 预训练目标：掩码语言建模（MLM）+ RoBERTa的动态掩码策略，无下一句预测（NSP）。
- **分词器**：
  - 基于PhoGPT的越南语字节级BPE分词器，额外添加`<mask>`标记。
  - 词汇表大小：20481个token类型。
- **预训练数据**：
  - 129GB清洁的通用越南语文本（未压缩）。
- **训练配置**：
  - 优化器：Adam（Kingma & Ba, 2015）。
  - 学习率：峰值0.00015，前2个epoch预热。
  - 批量大小：1024个序列块。
  - 训练轮数：20个epoch。
- **关键创新**：
  - 最大序列长度2048 tokens（PhoBERT仅256）。
  - 直接处理原始文本，无需外部分词。

## 3. 实验设计
- **使用的数据集（8个）**：
  - **通用领域**：ViNLI（自然语言推理）、PhoNER_COVID19（命名实体识别）。
  - **社交媒体/论坛**：UIT-VSFC（情感分类）、UIT-VSFC（主题分类）、ViSpamReviews（垃圾评论检测）、UIT-ViSFD（方面级情感分析）、UIT-ABSA（酒店）、UIT-ABSA（餐厅）。
  - 数据集统计见论文Table 1。
- **Benchmark与对比方法**：
  - 越南语单语模型：ViDeBERTa-base、ViSoBERT、PhoBERT-base。
  - 多语言模型：XLM-RoBERTa-base。
  - 注：ViDeBERTa和PhoBERT需要先进行越南语分词（使用VnCoreNLP的RDRSegmenter），而BamiBERT、ViSoBERT、XLM-RoBERTa直接处理原始文本。
- **微调设置**：
  - 使用Transformers库 + AdamW优化器。
  - 批量大小32，训练30个epoch。
  - 学习率从{1e-5, 2e-5, 5e-5}中网格搜索，基于验证集最佳F1选择模型检查点，报告测试集结果。

## 4. 资源与算力
- **硬件**：8张NVIDIA A100 GPU（每张40GB显存）。
- **训练时长**：论文未明确给出总时间，但提到“分布训练”和“20个epochs”。考虑到129GB数据量和2048序列长度，预估训练成本较高。
- **备注**：该工作完成于Movian AI公司，使用其资源。

## 5. 实验数量与充分性
- **实验数量**：在8个数据集上共报告了15个评估指标（部分数据集含多个子任务如检测F1和分类F1）。
- **充分性评价**：
  - **正面**：覆盖了多种NLU任务（推理、实体识别、情感、主题、方面检测与分类），且数据来源于通用与社交媒体领域，体现了一定多样性。
  - **不足**：
    - 未进行消融实验（例如：不同上下文长度的影响、是否需要分词、不同数据规模的影响等），难以归因性能提升的具体来源。
    - 未与更大规模的模型（如XLM-RoBERTa-large、CafeBERT等）对比，仅局限于base尺寸。
    - 未报告在领域特定任务（如法律、医疗）上的表现，泛化性验证尚不完全。
- **公平性**：对基线模型使用了完全相同的微调流程和搜索空间，且对分词模型使用了标准分词工具，整体公平。

## 6. 论文的主要结论与发现
- **性能领先**：在15个指标中，BamiBERT在**11个上取得最佳、3个上取得第二、1个上取得第三**，创立了base级越南语编码器的新SOTA。
- **大幅提升领域**：在ViNLI（自然语言推理）上，BamiBERT相比PhoBERT提升超过3个点（Accuracy和F1），相比其他模型提升13-20点，显示出强大的句子对语义理解能力。
- **跨领域泛化强**：即使在社交媒体数据集（如ViSpamReviews、UIT-ABSA）上，BamiBERT依然与专门针对社交媒体的ViSoBERT相当或更优，证明了其鲁棒性。
- **检测优于分类**：在方面级情感分析中，检测任务（定位目标边界）表现突出，但酒店领域的分类任务（情感极性判断）略逊于ViSoBERT，提示可能存在领域特定情感微妙性的差异。

## 7. 优点
- **技术先进**：支持2048长上下文、原始文本处理，切合现实应用需求。
- **性能强劲**：在多个任务上实现明显提升，特别是ViNLI的大幅领先。
- **数据规模大**：使用129GB干净语料，是此前越南语单语模型（如PhoBERT的20GB、viBERT的60GB）的2-6倍。
- **发布资源**：模型开源在HuggingFace，便于社区使用。
- **实验设计严谨**：统一微调流程、搜索学习率、基于验证集选择最佳模型，确保结果可比。

## 8. 不足与局限
- **缺乏消融实验**：无法证实长上下文、免分词、大数据量、更多训练epochs各自贡献了多少。
- **对比范围有限**：未与“large”尺寸模型（如CafeBERT等）比较，也未与最近发布的ModernBERT、NeoBERT等新架构对比。
- **领域覆盖不全**：未在法律、医疗、生物医学等专业领域测试泛化性。
- **酒店分类任务落后**：在UIT-ABSA Hotel的分类F1上低于ViSoBERT和PhoBERT，说明情感极性判断仍有改进空间，可能需要对情感细微差异进行专门优化。
- **未讨论部署开销**：长序列（2048）可能带来更高的推理时间和内存消耗，论文未分析实际应用的效率。
- **数据集质量风险**：社交媒体数据中存在噪声、拼写错误等，但BamiBERT的鲁棒性未特别验证；另外ViSpamReviews上ViSoBERT略优，说明在特定噪声环境下仍存在挑战。

（完）
