---
title: "From Dependency to Compositionality: A Neurosymbolic Lifting of LLM Outputs via Combinatory Categorial Grammar"
title_zh: 从依存到组合性：通过组合范畴语法对LLM输出进行神经符号提升
authors: Remo Pareschi
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18961v2"
tags: ["query:llm-ling"]
score: 9.0
evidence: 基于组合范畴语法的神经符号框架，将LLM输出提升为组合式推导
tldr: 针对LLM依赖语法视角的局限性，本文提出将LLM输出提升为基于组合范畴语法（CCG）的类型化组合推导，利用自回归生成与CCG增量处理的对齐性。该方法通过Curry-Howard对应扩展至编程语言和查询语言，并实现组合层与内容层的双重检查，能够早期检测结构错误和幻觉内容。结果表明，该框架不要求LLM内在具备语法，仅需其前缀驱动生成特性，为LLM输出提供可审计的组合结构重建。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18961v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1504, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 709, \"height\": 717, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1330, \"height\": 538, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1612, \"height\": 837, \"label\": \"Table\"}]"
motivation: 现有依赖语法解释无法完全捕捉LLM生成的组合性，而CCG的增量类型匹配与自回归解码天然对齐，亟需一种可审计的神经符号化提升方法。
method: 提出神经符号框架，将LLM输出通过CCG推导重建为类型化组合结构，并利用Curry-Howard对应扩展到形式语言，实现语法与语义的双层检查。
result: 该方法能够早期捕获结构性失败和幻觉内容，验证了LLM输出可被提升为一致组合推导，无需假设LLM内部实现语法。
conclusion: 该框架表明，只要LLM具备前缀驱动生成特性，即可通过外部CCG重建获得可解释的组合结构，为LLM可信性提供了新路径。
---

## 摘要
大型语言模型（LLM）通过从前缀逐步预测下一个标记来生成流畅的文本。生成传统中的批评者认为，此类系统缺乏真正的语法；依存语法视角的有影响力的回应认为，LLM行为可以通过逐词构建的局部中心词-依存结构来很好地描述。我们认为，一个更尖锐的观察被忽视了：自回归生成的前缀驱动、类型补全的动态过程与组合范畴语法（CCG）最初设计支持的那种增量处理模型高度一致。在此基础上，我们提出一个神经符号框架，将LLM输出提升为类型化的组合推导——不是声称LLM内部实现了CCG，而是其输出允许一种有原则的、增量的、可审计的CCG重构。这带来两个后果。首先，通过Curry-Howard对应，这种提升从自然语言扩展到LLM也产生的形式语言——例如Solidity等编程语言、描述逻辑和查询语言如OWL和SQL——其中类型系统变化而架构保持不变。其次，这种提升支持两层检查：一个组合层直接捕获结构失败，一个内容层针对外部知识源检查提升后的结构，从而能够尽早标记幻觉内容。因此，这种说明要求生产者具备的不是认知能力，而是前缀驱动的生成轮廓。最后，我们简要描述了同步LLM-CCG耦合作为该框架开启的一个方向。

## Abstract
Large language models (LLMs) generate fluent text by incrementally predicting the next token from a prefix. Critics in the generative tradition argue that such systems lack genuine grammar; influential replies from the dependency-grammar perspective hold that LLM behavior is well described by local head-dependent structure built word by word. We argue that a sharper observation has been overlooked: the prefix-driven, type-completing dynamics of autoregressive generation align closely with the incremental processing model that Combinatory Categorial Grammar (CCG) was originally designed to support. On this basis we propose a neurosymbolic framework in which LLM outputs are lifted into typed compositional derivations -- not claiming that LLMs implement CCG internally, but that their outputs admit a principled, incremental, and auditable CCG reconstruction. Two consequences follow. First, through the Curry-Howard correspondence the lifting extends beyond natural language to the formal languages LLMs also produce -- programming languages such as Solidity, description-logic and query languages such as OWL and SQL -- with the type system varying and the architecture held fixed. Second, the lifting supports two layers of checking: a compositional layer that catches structural failures directly, and a content layer that checks the lifted structure against external knowledge sources, enabling the earliest possible flagging of hallucinated content. The account thereby requires of a producer not cognition but a prefix-driven generative profile. We close with a sketch of synchronous LLM-CCG coupling as one direction the framework opens.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **背景**：大型语言模型（LLM）通过前缀逐步预测下一个 token 生成文本，其流畅性引发了关于是否具备“真正语法”的辩论。生成传统批评者认为 LLM 缺乏语法；依存语法视角（如 Gibson 的工作）的回应认为 LLM 行为可由局部头-依存结构描述，但无法充分覆盖组合性。
- **核心问题**：现有依存语法解释无法完全捕捉 LLM 生成的组合性（compositionality），且 LLM 自回归生成的前缀驱动、类型补全动态与组合范畴语法（CCG）的设计高度一致。
- **整体含义**：作者提出一个神经符号框架，将 LLM 输出“提升”（lifting）为基于 CCG 的类型化组合推导，不假设 LLM 内部实现语法，仅利用其前缀驱动的生成特性。该框架通过 Curry-Howard 对应扩展到形式语言（如编程语言、查询语言），并支持组合层（结构检查）和内容层（对外部知识源检查）的双层验证，旨在早期检测结构错误和幻觉内容。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将 LLM 生成视为后验（post hoc）重组过程。对 LLM 输出的每个前缀，用 CCG 语义解析器（如 ccg2lambda）生成组合推导和对应的 lambda 项，从而获得显式、可审计的组合结构。该过程与 LLM 的逐 token 生成步调一致（增量性），且依赖 CCG 的 derivational equivalence 属性保证一致性。
- **关键技术细节**：
  - **CCG 增量处理**：CCG 的类别系统（如 `S/NP`）允许每个前缀拥有类型和部分意义，与 LLM 对下一 token 的预测期望对齐。举例：`John loves` 被解析为 `S/NP` 类别和 `λy.love(john, y)`。
  - **双层检查**：
    - **组合层**：检查结构合法性（如参数类型匹配、缺失参数）。
    - **内容层**：将提升后的结构（如谓词 `aztec(zeus)`）查询外部知识源（如本体、知识库），检测事实性错误。内容层仅在组合层通过后触发。
  - **跨域扩展**：通过 Curry-Howard 对应，将操作抽象为增量类型检查。对于编程语言（如 Solidity），用 effect-and-obligation 类型代数表示函数的行为契约；对于 SQL/OWL，使用对应的类型系统和组合语义。
  - **同步耦合（未来方向）**：将增量 CCG 解析器与 LLM 解码过程耦合，实现软约束（重新加权）或硬掩码（禁止生成非法 token）。但论文主要论述后验提升，同步耦合仅作草图。

## 3. 实验设计：数据集、场景、benchmark、对比方法
- **实验设计**：论文本身**没有开展任何定量实验**。全文为理论框架和概念论证，未提供数据集、benchmark 或对比实验。
- **提及的实证参考**：
  - 引用了 Salzano 等人（2026）对 LLM 生成 Solidity 智能合约的正确性研究，该研究发现零样本功能正确率仅 20-26%，最多 45% 通过检索增强。但该研究是外部工作，非本文实验。
  - 提供了若干示例（如“John loves Mary”的增量对齐、“Aztec god Zeus”幻觉检测、Solidity函数缺失 require/event等的类型弱化）作为概念验证，但这些是定性案例，非严格实验。

## 4. 资源与算力
- **文中未提及**任何关于计算资源（GPU 型号、数量、训练时长、推理成本等）的信息。由于该框架不修改 LLM，仅需 CCG 解析器在生成后运行，算力需求主要来自解析器本身，但论文未作具体量化分析。

## 5. 实验数量与充分性
- **实验数量**：零组定量实验。所有论证基于理论推理和手工构建的例子。
- **充分性评估**：
  - 框架在概念层面论证充分：详细解释了 CCG 与自回归生成的对齐、双层检查的逻辑、跨域扩展的 Curry-Howard 基础。
  - 缺乏实证检验：未在真实 LLM 输出上评估框架的有效性（如正确检测率、假阳性率、延迟影响等）。也未与现有方法（如 FActScore、约束解码）进行对比。
  - 存在明显的局限性：多句子篇章、指代消解、鲁棒性等问题被承认但未解决。因此，实验充分性较低，结论主要依赖逻辑自洽性。

## 6. 主要结论与发现
- **核心结论**：CCG 的前缀驱动、类型补全动态与 LLM 自回归生成天然对齐，这使得对 LLM 输出进行 CCG 提升不仅在技术上可行，而且在结构上具有动机。
- **关键发现**：
  - LLM 输出可以（通过后验解析）重建成一致的、可审计的组合推导，不要求 LLM 内部拥有语法知识。
  - 提升后的 lambda 项支持双层检查：组合层捕获结构缺陷（如缺失参数、类型不匹配），内容层借助外部知识源检测幻觉。
  - 该框架通过 Curry-Howard 对应可扩展至形式语言（如 Solidity、SQL、OWL），类型系统变化而架构固定。
  - 增量性允许在最早可能时刻（即每个前缀）触发检查，从而支持早期干预（同步耦合中可阻止错误产生）。
  - 框架所需的生产者特征仅为前缀驱动的生成轮廓，而非认知能力，因此适用于人类和 LLM 等不同生产者。

## 7. 优点
- **理论创新**：将 CCG 的增量语义本质与 LLM 的生成机制建立深刻联系，为神经符号 AI 提供了一个新颖且结构合理的桥梁。
- **保守性**：框架不修改 LLM 内部，不重新训练，不访问内部状态，仅操作于输出，易于部署且风险低。
- **可审计性**：每个推导步骤源自词条和组合规则，透明无黑盒，有助于解释和调试。
- **增量性好**：CCG 的增量性允许在生成过程中尽早发现错误，而不是等到整个句子或段落完成。
- **跨域统一**：在自然语言和形式语言上使用相同架构，仅需替换类型系统和知识源，显示了泛化潜力。
- **对认知假设轻量**：消除了对生产者是否具有“心智”或“语法知识”的依赖，仅需前缀驱动生成，与 Turing 和 Montague 的精神一致。

## 8. 不足与局限
- **缺乏实证验证**：论文完全是理论推导，没有实际运行实验来支持其有效性。未在真实 LLM 输出上测量检测率、延迟、假阳性等指标。
- **篇章级处理缺失**：框架目前仅处理单句或前缀级别的提升，多句子篇章中的指代消解、语篇衔接、语用推理等未解决。虽提及 Grenander 等人的增量指代消解可作为后续整合方向，但当前未实现。
- **CCG 解析器鲁棒性不确定**：现有 CCG 解析器在新闻体上表现良好，但 LLM 输出覆盖广泛文体（非正式、创造、代码等），可能超出解析器的训练分布，尤其是结构性和语域层面的差异。虽然超标注器可推广到未见词汇，但句法结构泛化能力未验证。
- **知识源的信任问题**：框架依赖外部知识源的正确性和完备性，但未讨论如何选择或验证这些知识源。在安全性或信息关键场景中，不可信的知识源可能引入新错误。
- **同步耦合可行性未展开**：同步 LLM-CCG 耦合仅作概念描述，未给出具体算法、组合规则或实验。硬掩码可能导致解析器诱导的 Garden Path 错误。
- **实验设计缺失**：无对比实验、无消融研究、无基准测试，这使得框架相对于现有方法（如 FActScore、约束解码、RAG）的优势缺乏定量证据。
- **冷启动实现挑战**：对形式语言（如 Solidity）设计 CCG 类型词法需要大量工作，虽提到可通过规则引导和合成数据解决，但未实际构建。

（完）
