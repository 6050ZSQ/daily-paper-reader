---
title: "From Dependency to Compositionality: A Neurosymbolic Lifting of LLM Outputs via Combinatory Categorial Grammar"
title_zh: 从依存关系到组合性：通过组合范畴语法对LLM输出进行神经符号提升
authors: Remo Pareschi
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18961v2"
tags: ["query:llm-lang-rep"]
score: 9.0
evidence: 通过组合范畴语法对LLM输出进行神经符号提升，揭示语言结构表示
tldr: 大型语言模型（LLM）的自回归生成过程缺乏显式语法结构，依赖语法视角仅能描述局部依存关系。本文发现其类型补全动态与组合范畴语法（CCG）的增量处理模型高度一致，提出神经符号框架将LLM输出提升为类型化组合推导。通过Curry-Howard对应，该框架统一处理自然语言与形式语言（如Solidity、SQL），并支持两层审计：组合层检测结构错误，内容层借助外部知识早期识别幻觉。该工作为LLM输出提供可审计、可解释的语法桥接方案。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18961v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1504, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 709, \"height\": 717, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1330, \"height\": 538, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1612, \"height\": 837, \"label\": \"Table\"}]"
motivation: LLM生成缺乏可解释语法结构，现有依赖语法无法捕捉组合性，需增量类型驱动重建方法。
method: 基于CCG增量处理与自回归生成类型补全的对应，将输出提升为类型化推导，并扩展至形式语言。
result: 实现两层检查：组合层捕获结构失败，内容层结合外部知识早期标记幻觉。
conclusion: 通过同步LLM-CCG耦合，提供可审计语法桥接，无需生产者具备认知能力。
---

## 摘要
大型语言模型（LLM）通过从前缀逐步预测下一个标记来生成流畅文本。生成传统中的批评者认为，此类系统缺乏真正的语法；来自依存语法视角的有影响力回应认为，LLM行为可以通过逐词构建的局部中心语-依存结构得到良好描述。我们认为一个更尖锐的观察被忽视了：自回归生成的前缀驱动、类型补全动态，与组合范畴语法（CCG）最初设计所支持的增量处理模型紧密对齐。在此基础上，我们提出一个神经符号框架，将LLM输出提升为类型化的组合推导——并非声称LLM内部实现了CCG，而是它们的输出允许一种有原则的、增量的、可审计的CCG重建。这将带来两个后果。首先，通过Curry-Howard对应，这种提升从自然语言扩展到LLM也生成的形式语言——例如Solidity等编程语言、OWL等描述逻辑和SQL等查询语言——其中类型系统变化而架构保持固定。其次，这种提升支持两个检查层：一个组合层直接捕获结构故障，一个内容层对照外部知识源检查提升后的结构，从而能够尽早标记幻觉内容。因此，该要求对生成者而言并非认知能力，而是前缀驱动的生成轮廓。我们最后以同步LLM-CCG耦合的草图作为该框架开放的一个方向。

## Abstract
Large language models (LLMs) generate fluent text by incrementally predicting the next token from a prefix. Critics in the generative tradition argue that such systems lack genuine grammar; influential replies from the dependency-grammar perspective hold that LLM behavior is well described by local head-dependent structure built word by word. We argue that a sharper observation has been overlooked: the prefix-driven, type-completing dynamics of autoregressive generation align closely with the incremental processing model that Combinatory Categorial Grammar (CCG) was originally designed to support. On this basis we propose a neurosymbolic framework in which LLM outputs are lifted into typed compositional derivations -- not claiming that LLMs implement CCG internally, but that their outputs admit a principled, incremental, and auditable CCG reconstruction. Two consequences follow. First, through the Curry-Howard correspondence the lifting extends beyond natural language to the formal languages LLMs also produce -- programming languages such as Solidity, description-logic and query languages such as OWL and SQL -- with the type system varying and the architecture held fixed. Second, the lifting supports two layers of checking: a compositional layer that catches structural failures directly, and a content layer that checks the lifted structure against external knowledge sources, enabling the earliest possible flagging of hallucinated content. The account thereby requires of a producer not cognition but a prefix-driven generative profile. We close with a sketch of synchronous LLM-CCG coupling as one direction the framework opens.

---

## 论文详细总结（自动生成）

# 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）通过自回归方式逐词生成文本，批评者认为生成过程缺乏显式语法结构；依存语法视角虽能描述局部中心语-依存关系，但无法捕捉组合性语义。作者认为一个关键观察被忽视：LLM 的前缀驱动、类型补全动态与组合范畴语法（CCG）原本设计的增量处理模型高度吻合。
- **整体含义**：基于这一对齐，论文提出一个神经符号框架，将 LLM 输出提升为类型化组合推导（typed compositional derivations），从而为 LLM 输出提供可审计、可解释的语法桥接，并支持两层检查：组合层（结构正确性）和内容层（借助外部知识检测幻觉）。通过 Curry-Howard 对应，该框架统一适用于自然语言和形式语言（如 Solidity、SQL、OWL）。

# 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用 CCG 的增量、左到右、类型补全特性，将 LLM 生成的输出（自然语言或形式语言）通过 CCG 语义解析器（如 ccg2lambda）映射为 lambda 项，得到显式的类型化组合结构，而无需修改 LLM 内部。
- **关键技术细节**：
  - **两层框架**：（1）神经生成层（LLM 逐词生成）；（2）组合 CCG 层（对生成文本进行增量解析，分配类别并组成 lambda 项）。
  - **增量提升示例**：以“John loves Mary”演示每一步的 LLM 期望与 CCG 类型状态对齐（如前缀“John loves”对应类型 S/NP 与 lambda 项 λy.love(john, y)）。
  - **通过 Curry-Howard 对应扩展**：将类型提升操作推广到编程语言（Solidity）、描述逻辑（OWL）、查询语言（SQL）等——类型系统变化，架构固定。
  - **两层检查**：组合层直接检测结构故障（如缺失参数、类型不匹配）；内容层将提升后的结构化对象（如 lambda 项中的谓词）对照外部知识源查询，实现最早可能标记幻觉。
  - **同步耦合草图**：未来方向——将增量 CCG 解析器与 LLM 解码耦合，使符号状态实时约束生成（软约束或硬屏蔽）。
- **关键概念**：类型不仅作为组合指令，还作为外部知识检查的接口（类型决定如何查询外部源）。

# 3. 实验设计：数据集、基准测试、对比方法

- **论文本身未提出新实验**。它属于概念性/理论性论文，主要提供框架论证和示例性分析。
- **引用实证依据**：引用了 Salzano et al. (2026) 关于 LLM 生成 Solidity 智能合约的实证研究，显示零样本正确率仅 20–26%，检索增强最高 45%。论文以此说明实际场景中 LLM 输出的组合失败（如缺失 require、事件缺失）正是其框架可检测的类型。
- **基准与对比**：无自主提出的 benchmark 或对比方法；仅从概念上与依存语法、FActScore 等方法进行定性比较。

# 4. 资源与算力

- **论文未报告任何算力使用情况**（GPU 型号、数量、训练时长等）。由于属于理论框架，未涉及训练或大规模计算。

# 5. 实验数量与充分性

- **实验数量**：无字面实验。论文仅提供定性示例（如“The Aztec god Zeus”、Solidity 函数 setMinDepositAmount 的对比）。
- **充分性评估**：
  - 在理论论证和示例上足够清晰，但缺乏端到端实现验证（如真实管道在多段落文本上的运行）。
  - 实验部分不充分，因为未用实际系统测试框架的有效性。论文将实证实现留作未来工作。

# 6. 论文的主要结论与发现

1. **LLM 与 CCG 的形式对齐**：自回归生成的前缀驱动态与 CCG 的增量类型补全动态一致，这一对齐被先前工作忽视。
2. **可审计的神经符号提升**：通过 CCG 解析可将 LLM 输出转化为透明、可追溯的类型化 lambda 项，且不修改 LLM。
3. **跨域统一**：通过 Curry-Howard 对应，同一架构可应用于自然语言和多种形式语言（Solidity、SQL、OWL），只需更换类型系统。
4. **两层检查**：组合层捕获结构错误，内容层结合外部知识源实现早期幻觉标记（在谓语尚未出现时即可触发）。
5. **降低对认知假设的依赖**：框架对生成者的唯一要求是前缀驱动态，无需涉及心智理论，从而脱离认知语言学锚定。
6. **同步耦合具有潜力**：约束解码器可融合符号状态，但硬屏蔽需谨慎以避免解析器导引错觉。

# 7. 优点

- **理论创新**：首次系统论证 LLM 自回归生成与 CCG 增量处理的内在结构对齐，提供新的神经符号视角。
- **可审计性与透明度**：提升后的 lambda 项每个组件可追溯到词汇条目或组合步骤，无黑箱推理。
- **跨域泛化**：统一自然语言和形式语言处理，通过类型系统变化而非重新架构来实现，体现出高度抽象性。
- **增量检查**：支持在最早可能的时间点（如谓语出现前）触发内容检查，优于事后分解方法（如 FActScore 丢弃语境）。
- **架构适度性**：不修改 LLM，不访问内部，仅操作输出，易于集成。
- **区分组合/内容检查**：明确界限，避免过度声称（组合层不能检测事实错误，但提供检测所需结构）。

# 8. 不足与局限

- **缺乏实证验证**：论文未实现完整管道，缺乏在真实 LLM 输出上验证框架有效性的实验结果。示例为手工构造。
- **多句篇章处理未解决**：对回指、语用依赖、跨句组合（如合约中的重入）仅提及需借助现有增量篇章模型，未给出具体方案。
- **CCG 解析器鲁棒性问题**：LLM 生成文本分布远超 CCGbank 训练的新闻英语范围，超标签泛化虽有效，但结构/语域偏移可能降低解析质量，尤其在同步耦合中（解析失败会直接不良约束生成）。
- **内容检查依赖外部知识源可信度**：框架声明“给定可信源”，但未讨论知识源的选择、维护及对抗性操纵问题。
- **同步耦合仅草图**：软硬约束的具体融合方法、解析器导引错觉的具体防范未展开。
- **形式语言实现未完成**：Solidity 等语言的类型代数（效果-义务类型）、词库设计、编译器集成仅为概念描述，实际工程工作量大。
- **无消融或敏感性分析**：无法量化不同 CCG 解析器、外部知识源质量对最终检查效果的影响。

（完）
