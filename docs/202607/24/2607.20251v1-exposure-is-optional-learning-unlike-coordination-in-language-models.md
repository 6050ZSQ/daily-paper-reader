---
title: "Exposure is Optional: Learning Unlike Coordination in Language Models"
title_zh: 暴露非必需：语言模型中的异类并列学习
authors: "Jiamu Luo, Shane Steinert-Threlkeld"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20251v1"
tags: ["query:llm-ling"]
score: 9.0
evidence: 语言模型对句法并列结构的习得
tldr: 语言模型中的协调结构习得存在争议，尤其是不同类别成分的并列（unlike coordination）是否需要直接暴露。本文通过Filtered-Corpus Training（FiCT）从训练语料中移除所有unlike coordination实例，训练GPT-2模型，发现模型仍能良好泛化，困惑度和语法判断与未过滤训练无异。这表明直接暴露并非必要，模型通过将并列成分视为相同结构类别或删除机制处理unlike coordination。该工作揭示了语言模型内部表示机制，为协调结构理论提供了计算证据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20251v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 806, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2607.20251v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1612, \"height\": 698}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.20251v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 806, \"height\": 243}, {\"url\": \"assets/tables/arxiv/2607.20251v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1659, \"height\": 266}, {\"url\": \"assets/tables/arxiv/2607.20251v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1655, \"height\": 304}, {\"url\": \"assets/tables/arxiv/2607.20251v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1659, \"height\": 268}, {\"url\": \"assets/tables/arxiv/2607.20251v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 791, \"height\": 779}, {\"url\": \"assets/tables/arxiv/2607.20251v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 804, \"height\": 1059}]"
motivation: 探究语言模型习得unlike coordination是否需要直接暴露，还是可从一般组合能力中涌现。
method: 使用Filtered-Corpus Training（FiCT）构建无unlike coordination的语料，训练GPT-2模型并测试泛化能力。
result: 模型在无直接暴露下成功泛化，困惑度和语法判断与未过滤模型相当。
conclusion: 直接暴露非必需，模型通过类别等同或删除机制处理unlike coordination，这对理解语言模型内部结构有贡献。
---

## 摘要
并列作为一种基本的语言结构，一直是激烈争论的主题，其确切性质仍令理论语言学难以捉摸。一种普遍观点认为只有相同类别的成分才能并列，但这受到了自然语言中大量合乎语法的异类并列的挑战。以语言模型作为计算测试平台，我们探究异类并列的习得是否需要训练数据中的直接暴露，还是可以从一般的组合能力中有机产生。利用过滤语料训练（FiCT），我们在移除所有异类并列实例的语料上训练GPT-2模型。我们发现直接暴露并非必需：在过滤数据上训练的模型成功泛化至异类并列，其困惑度和语法判断与在未过滤文本上训练的模型相当。此外，我们对内部表示的分析表明，语言模型通过将并列元素视为属于相似的结构类别或通过类似删除的机制来处理异类并列，这两种方式似乎仅从同类并列的暴露中即可学习。这项工作有助于理解语言模型如何在内部表示语言结构，同时通过展示模型如何在无直接暴露的情况下泛化与处理异类并列，为更广泛的并列辩论增添了新的视角。

## Abstract
Coordination, a fundamental linguistic structure, remains a subject of intense debate, and its exact nature continues to elude theoretical linguistics. A common view holds that only same-category constituents can be conjoined, which has been challenged by the many grammatical unlike coordinations found in natural language. Treating language models as a computational testbed, we investigate whether the acquisition of unlike coordination requires direct exposure in the training data, or whether it can emerge organically from general compositional abilities. Using Filtered-Corpus Training (FiCT), we train GPT-2 models on corpora from which all instances of unlike coordination have been removed. We find that direct exposure is not necessary: models trained on filtered data successfully generalize to unlike coordination, achieving perplexity and grammaticality judgments comparable to models trained on unfiltered text. Furthermore, our analyses of internal representations indicate that language models process unlike coordination by treating the conjoined elements as belonging to similar structural categories or through a mechanism akin to deletion, both of which appear learnable from exposure to alike coordination alone. This work contributes to the growing understanding of how language models internally represent linguistic structures, while also adding to the broader debate on coordination by showing how models generalize and process unlike coordination without direct exposure.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：并列结构是语言学核心议题，传统“同类并列律”（Law of Coordination of Likes, LCL）认为只有相同句法范畴的成分才能并列，但自然语言中存在大量合乎语法的异类并列（Unlike Coordination），如 *you can depend on my assistant and that he will be on time*。
- **研究动机**：本文以语言模型（LM）为计算测试平台，探究LM习得异类并列是否需要训练数据中的直接暴露，还是可以从同类并列中通过组合能力泛化而来。
- **整体含义**：如果LM能在无直接暴露下成功泛化，则说明异类并列并非需要专门语法规则的例外现象，可为理论语言学关于并列本质的辩论提供计算证据。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用过滤语料训练（Filtered-Corpus Training, FiCT），通过构造无异类并列的语料训练GPT-2模型，观察模型能否泛化处理异类并列。
- **关键技术细节**：
  - 基于原始英语语料（Gulordava等，2018），使用伯克利神经解析器（benepar_en3）自动识别并列结构。
  - 两种过滤策略：
    - Filter I：移除所有包含异类并列的句子（得到 `filtered-all` 语料）。
    - Filter II：仅移除由 `and` 连接的异类并列句子（得到 `filtered-and` 语料）。
  - 经过过滤后，将原始语料下采样至与 `filtered-all` 句子数相同（1,948,104句），形成三个训练语料：`original`、`filtered-and`、`filtered-all`。
  - 模型架构：类GPT-2 Large（1024上下文长度，1280嵌入维度，36层，20头）。
  - 训练使用 Huggingface transformers，AdamW优化器，混合精度，有效batch size 16，学习率5e-5，权重衰减0.01，训练10个epoch。
- **未涉及公式或算法流程的详细数学表达式**，但文字说明了预处理、过滤、训练步骤。

## 3. 实验设计：数据集、benchmark、对比方法

- **训练数据集**：基于Gulordava等（2018）英语语料，构造三种变体（original, filtered-and, filtered-all）。
- **评估数据集**：人工构建五个评估语料：
  - `unlike`（100句异类并列）
  - `alike-from-unlike`（100句同类并列，作为`unlike`的松散最小对）
  - `supercat-deletion`（62句可解释为共享超类别的异类并列）
  - `deletion`（24句可解释为删除的异类并列，不含超类别）
  - `unlike-judgment`（22项语法判断测试，源自协调理论文献，每项含合法/非法句对）
- **基准（benchmark）**：以原始模型（original）为基线，对比过滤模型（filtered-and, filtered-all）。
- **评估指标**：
  - 困惑度（perplexity）用于评估除`unlike-judgment`外的语料。
  - 平均意外度（average surprisal）用于语法判断测试（比较合法/非法句的意外度）。
  - 平均注意力分数（average attention score）、余弦相似度、聚类分析用于内部表示分析。
- **对比方法**：三组模型（original, filtered-and, filtered-all）之间的定量比较，及同一模型内不同语料间的比较。

## 4. 资源与算力

- 文中明确提到：
  - 每块 GPU 为 **NVIDIA L40**。
  - 每个模型使用 **1块 NVIDA L40 GPU**。
  - 训练时长：最多 10 个 epoch，但未给出总训练时间。
  - 未提及总 GPU 数量（每个种子训练一个模型，共 9 个模型，但可顺序或并行执行）。
  - 其他硬件细节（如CPU、内存）未说明。

## 5. 实验数量与充分性

- **实验数量**：
  - 训练了9个模型：3个模型变体 × 3个随机种子（291, 1543, 9071）。
  - 过滤效果评估：使用了100句异类并列和100句同类并列的评估集。
  - 主要评估：困惑度在5个语料上计算（每个种子每个评估集1个值）；语法判断测试22项；注意力分析在 `deletion` 和 `supercat-deletion` 上各62/24句子；聚类分析在三个条件（alike-from-unlike, supercat-deletion, deletion）的句子集合上训练聚类系统。
- **充分性与公平性**：
  - 多随机种子重复实验，消除初始化影响。
  - 严格控制语料大小一致（下采样至1948k句），避免数据量差异。
  - 评估语料覆盖多种异类并列模式（10种类型），且源自真实语料和理论文献。
  - 但仅测试了GPT-2架构，未扩展到其他LM（如LLaMA、BERT等）；过滤方法依赖于自动解析器，可能存在误差（但作者已报告过滤效果评估，显示精度高）。
  - 缺乏人类实验对比，心理语言学数据缺失。

## 6. 论文的主要结论与发现

- **主要结论**：直接暴露于异类并列并非必要；即使训练语料中完全移除异类并列，GPT-2模型仍能成功泛化，困惑度和语法判断与未过滤模型相当。
- **机制发现**：模型通过两种机制处理异类并列：
  - **超类别（supercategory）机制**：将并列成分视为共享更高层类别，在内部表示中按句子语境聚类。
  - **删除（deletion）机制**：通过隐式重复结构解释，第二并列成分高度注意被删除的成分（如主语）。
  - 未过滤模型对二者区分更清晰，过滤模型仍能运用但区分度稍弱。
- **理论启示**：模型不预设严格的LCL约束；异类并列自然产生于一般的组合能力与类别泛化，无需单独语法规则。
- **局限性**：模型对更细粒度的约束（如动词选择限制、话题化要求）表现不稳定，且倾向于将异类并列当作同类处理。

## 7. 优点

- **方法创新**：FiCT允许精确操控训练数据，直接检验“是否需要直接暴露”这一理论问题。
- **多维度评估**：结合困惑度、语法判断、注意力分析、余弦相似度、聚类分析，全面揭示模型行为与内部表示。
- **理论对接**：将计算实验结果与语言学核心理论（LCL、超类别、删除分析）直接关联。
- **严格控制**：过滤精度高、语料大小匹配、多随机种子重复，保证结论可靠性。
- **开放科学**：代码与数据已上传GitHub（论文中承诺）。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅使用单一架构（GPT-2 Large），未验证其他类型LM（如编码器模型、不同规模Transformer）。
  - 仅测试英语，不涉及其他语言中可能更复杂的并列现象。
- **过滤依赖解析器**：自动解析器误差可能导致少量误删或保留，但作者评估显示影响较小。
- **人工评估集规模偏小**：`unlike-judgment`仅22项测试，可能不足以覆盖所有关键约束。
- **缺乏心理语言学验证**：未与人类判断或阅读时间数据对比，不清楚模型策略是否与人类一致。
- **训练资源细节不完整**：未报告总训练时间、能源消耗等，可复现性稍弱。
- **可能偏差**：模型在无需直接暴露下泛化，但未回答“在何种数据量下”或“何种语义相似度”下泛化成立；未来需通过控制语义相似度进一步验证。

（完）
