---
title: LLMs Encode Relevance as a Layer-Wise Cross-Lingual Signal
title_zh: 大型语言模型将相关性编码为逐层跨语言信号
authors: "Pietro Bernardelle, Samaneh Mohtadi, Stefano Civelli, Joel Mackenzie, Gianluca Demartini"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15555v1"
tags: ["query:llm-lang-rep"]
score: 9.0
evidence: 研究LLM如何通过逐层激活编码相关性；内部表征的可解释性
tldr: 大型语言模型在信息检索中常被用作相关性评判者，但其内部如何表征查询-文档相关性尚不明确。本文在TREC DL20和MIRACL数据集上，对指令微调LLM（4-9B参数）各层的残差流激活训练线性探针，以解码相关性标签。结果表明：探针性能随层数加深而增强，在中后层达到最优；且探针生成的伪标签在保持系统排名上有时优于模型直接生成的相关性判断，揭示了内部表示与外部表达之间的分离。跨语言实验显示部分可迁移性，但弱于同语言情境。这项工作提供了表示层面的诊断工具，有助于理解相关性信息在模型中的涌现时机及生成判断失败的原因。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15555v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1789, \"height\": 1149, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15555v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15555v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1835, \"height\": 363, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15555v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 871, \"height\": 408, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15555v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1575, \"height\": 425, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15555v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 907, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15555v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1703, \"height\": 776, \"label\": \"Table\"}]"
motivation: 现有LLM相关性评估研究侧重输出标签，缺乏对内部表示机制的探究，因此探索相关性是否可从LLM内部激活中线性解码。
method: 在指令微调LLM各层残差流激活上训练线性探针，预测查询-文档相关性，并与生成判断及人类标注进行对比。
result: 探针性能在中后层最强；伪标签在保持系统排名上有时优于生成判断；跨语言迁移有限，同语言解码更强。
conclusion: 相关性是深度依赖的内部信号，与外部表达存在分离，逐层探测可诊断生成判断的可靠性及跨语言差异。
---

## 摘要
大型语言模型（LLMs）越来越多地作为相关性评判和重排序器用于信息检索（IR）流水线中。然而，大多数分析仍以输出为中心，评估生成的标签或分数，而对模型内部如何表示相关性的洞察有限。在本工作中，我们研究查询-文档（q-d）相关性是否可以从指令微调LLMs的残差流激活中线性解码，该信号与生成的相关性判断相比如何，以及它是否跨语言迁移。使用TREC DL20和MIRACL评估数据集，我们以UMBRELA风格的相关性判断提示引导中等规模LLMs（4-9B参数），提取每个Transformer层最后一个令牌的激活，并训练线性探针来预测相关性标签。我们将探针预测与生成的判断进行比较，并使用TREC DL20测试探针衍生的伪标签是否相对于人工判断保持系统排名。我们的结果表明，q-d相关性被编码为深度依赖的信号：早期层探针性能较弱，中后期层最强，表明相关性在上下文整合后变得更容易线性访问。最重要的是，在几个模型中，验证选择的探针匹配或优于生成的判断，并更好地保持系统排名，揭示了内部相关性表示与外部表达之间的分离。多语言实验表明部分跨语言可移植性，尽管迁移仍弱于语言内解码。总体而言，本工作提供了基于LLM的相关性评估的表示层面视角。逐层探针可以帮助诊断相关性何时出现，生成的判断何时未能反映内部可用证据，以及相关性表示如何跨语言、数据集和模型家族变化。

## Abstract
Large language models (LLMs) are increasingly used in information retrieval (IR) pipelines as relevance judges and re-rankers. Yet most analyses remain output-centric, evaluating generated labels or scores while offering limited insight into how relevance is represented inside the model. In this work, we study whether query-document (q-d) relevance is linearly decodable from residual-stream activations in instruction-tuned LLMs, how this signal compares with generated relevance judgments, and whether it transfers across languages. Using the TREC DL20 and MIRACL evaluation collections, we guide medium-scale LLMs (4-9B parameters) with UMBRELA-style relevance judgment prompts, extract last-token activations from every transformer layer, and train linear probes to predict relevance labels. We compare probe predictions with generated judgments and use TREC DL20 to test whether probe-derived pseudo-labels preserve system rankings against human judgments. Our results suggest that q-d relevance is encoded as a depth-dependent signal: probe performance is weak in early layers and strongest in middle-to-late layers, indicating that relevance becomes more linearly accessible after contextual integration. Most importantly, in several models, validation-selected probes match or outperform generated judgments and better preserve system rankings, revealing a separation between internal relevance representation and external expression. Multilingual experiments suggest partial cross-language portability, although transfer remains weaker than within-language decoding. Overall, this work provides a representation-level perspective on LLM-based relevance assessment. Layer-wise probing can help diagnose where relevance emerges, when generated judgments fail to reflect internally available evidence, and how relevance representations vary across languages, datasets, and model families.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：大型语言模型（LLMs）越来越多地被用作信息检索（IR）中的相关性评判者（judge）和重排序器，但现有分析大多只关注输出标签（如生成的分数或标签），而对模型内部如何表示“查询-文档相关性”这一核心概念知之甚少。这导致难以判断模型是真正理解了相关性，还是仅仅利用了表层线索；也难以在生成判断出错时进行诊断。
- **整体含义**：本文旨在揭示 LLM 内部是否编码了与人类定义一致的相关性信号，该信号如何随层深变化，是否比模型直接生成的判断更可靠，以及是否跨语言可转移。这为可解释 IR（XIR）提供了新的表示级视角，有助于设计更透明、更可靠的自动化评估方法。

### 2. 论文提出的方法论

- **核心思想**：通过在指令微调 LLM 各层的残差流（residual stream）激活上训练线性探针（linear probe），检测 q-d 相关性是否可线性解码，并比较探针预测与模型直接生成的判断以及与人类标注的一致性。
- **关键技术细节**：
  - 对每个提示（包含查询、文档、UMBRELA 式相关性判断指令）进行前向传播，提取最后一个输入 token（紧接答案生成前）的残差流激活。
  - 在每个 transformer 层独立训练一个带 Ridge 正则化的线性探针（二分类用逻辑回归，四分类用序数逻辑回归）。
  - 使用基于查询的分组交叉验证（TREC DL20）或固定分组切分（MIRACL），避免查询泄露。
  - 通过验证集选择正则化强度 α 和最佳层，最终在测试集评估。
- **公式/算法流程**（文字描述）：
  1. 获取各层激活 \( h_i^{(\ell)} \)；
  2. 对每一层 ℓ，训练探针 \( \hat{y}_i = f_\ell(h_i^{(\ell)}) \)；
  3. 使用验证集 Cohen’s κ（二分类）或加权 κ（有序）选择超参数；
  4. 将探针预测与模型生成的判断（解析输出）对比，并评估系统排名保持能力（Kendall’s τ, Spearman’s ρ, RBO 等）。

### 3. 实验设计

- **数据集**：
  - **TREC DL20**：英语段落检索任务，含 54 个查询，人类提供的 0-3 级相关性判断。用于主要英语测评和系统排名分析。
  - **MIRACL**：多语言检索基准，采样 7 种语言（英文、中文、法文、阿拉伯文、日文、斯瓦希里文、泰卢固文），每语言 250 个查询，每个查询 1 个相关 + 1 个不相关文档，形成平衡二元标签。用于跨语言迁移实验。
- **基准（baseline）**：模型直接生成的 UMBRELA 式判断（解析输出标签），作为对比基线。
- **对比方法**：不同模型（Qwen2.5-7B、Qwen3-8B、Qwen3.5-9B、Llama3.1-8B、Aya-Expanse-8B、Gemma3-4B）的验证选择探针 vs. 直接生成。
- **实验设置**：
  - **RQ1**：在 TREC DL20 和 MIRACL 英文子集上逐层探测，绘制性能曲线。
  - **RQ2**：在 TREC DL20 上比较探针与生成判断的标签级一致性和系统级排名保持。
  - **RQ3**：在 MIRACL 全语言集上评估同语言与跨语言探针迁移。

### 4. 资源与算力

- 论文明确说明：所有实验在**单一 NVIDIA RTX 5090 GPU** 上运行，总耗时约 **1.5 天**（包括确定性的输出推理、逐层激活提取、探针训练和超参数选择）。

### 5. 实验数量与充分性

- **实验规模**：覆盖 6 个模型（分属不同家族）、2 个主要数据集、多种相关性定义（二元阈值 r≥1、r≥2、四分类），以及跨语言迁移实验（7 种语言间的配对，共 49 个语言对）。
- **充分性评价**：实验设计较为全面，既包含标准英语 IR 集合也包含多语言场景；使用分组交叉验证避免查询泄漏；对比了探针与直接生成、验证选择与 Oracle 上界；消融了不同阈值的敏感性；分析了逐层趋势和查询级别差异。总体上客观、公平，但在模型规模（仅 4-9B）和提示格式（仅 UMBRELA）上存在局限。

### 6. 论文的主要结论与发现

1. **相关性可线性解码**：q-d 相关性在残差流激活中呈深度依赖信号，早期层弱，中后层最强，说明需要上下文整合后信号才易线性访问。
2. **内部信号与生成判断存在分离**：在多个模型中（尤其是 Llama3.1-8B、Aya-Expanse-8B、Gemma3-4B），验证选择的探针比直接生成的判断更接近人类标签，并能更好地保持系统排名，表明内部表示可能比输出更准确。
3. **跨语言可迁移但有限**：跨语言探针在中后层也能解码一定程度的信号，但显著弱于同语言探针，表明存在共享语义表示但语言特定信息仍占主导。
4. **探针稳定性**：验证选择层与 Oracle 上界差距小，说明可用验证数据可靠选择高信息层。

### 7. 优点

- **创新视角**：从内部表示层面研究 LLM 相关性判断，而非仅关注输出标签，填补了可解释 IR 领域的空白。
- **严谨的实验设计**：采用基于查询的分组交叉验证，避免数据泄漏；同时考虑标签级和系统级两种评估，更具实用性。
- **跨语言分析**：首次系统评估 LLM 内部相关性表示的跨语言可迁移性，为多语言 IR 评估提供了新思路。
- **开源代码**：实验代码已公开，便于复现和扩展。

### 8. 不足与局限

- **模型规模限制**：仅涉及 4-9B 参数的中等模型，未测试更大规模或专门检索优化的模型，结论的泛化性有待验证。
- **提示格式固定**：仅使用 UMBRELA 风格提示，未研究其他提示变体对内部表示的影响。
- **激活提取位置单一**：仅提取最后输入 token 的激活，未探索对文档/查询区域的池化或注意力加权激活。
- **探针复杂性受限**：仅使用线性探针，未测试非线性解码器（如 MLP），可能低估了内部相关性信息量。
- **跨语言实验样本量有限**：每语言仅 250 个查询且每查询仅 2 个文档，可能不足以充分体现语言间差异。

（完）
