---
title: "Mixture of Probes: Learning from Privileged Modalities in Multimodal LLMs Through Probing"
title_zh: 探针混合：通过探针从多模态大语言模型中的特权模态学习
authors: "Dominick Reilly, Qiyu Wu, Hiromi Wakaki, Srijan Das, Yuki Mistufuji"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08839v1"
tags: ["query:llm-lang-rep"]
score: 8.0
evidence: 使用探针解耦多模态LLM中的模态特定及通用信号，提升可解释性
tldr: "多模态大模型通常假设所有模态始终可用，但现实场景常缺失模态。本文提出Mixture of Probes (MoP)框架，通过结构化探针从共享编码器中层提取并组织信息，解耦模态特异与通用信号，并设计MoP-X训练策略防止探针坍塌。在8项任务、4种模态下，MoP相比强基线最高提升65%，证明仅在训练中提供的辅助模态也能显著增强模型性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08839v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1385, \"height\": 353, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08839v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1384, \"height\": 704, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08839v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 458, \"height\": 249, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08839v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 457, \"height\": 248, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08839v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 457, \"height\": 250, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08839v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1436, \"height\": 609, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08839v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 642, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08839v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 718, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08839v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1440, \"height\": 790, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08839v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1426, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08839v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1393, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08839v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 734, \"height\": 167, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08839v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 790, \"height\": 205, \"label\": \"Table\"}]"
motivation: 现有MLLM无法有效利用训练中可用但推理时缺失的辅助模态，因为将模态视为可互换输入而非互补监督。
method: 提出MoP框架，通过结构化探针从共享编码器中层解耦模态特异与通用信号，并引入MoP-X跨模态训练和探针解耦损失。
result: "在8项任务、4种模态下，MoP相对于强基线MLLM平均提升显著，最高相对改进达65%。"
conclusion: MoP表明通过解耦训练可有效利用特权模态信息，为MLLM在模态缺失场景下的鲁棒部署提供了新范式。
---

## 摘要
多模态大语言模型（MLLMs）通常设计时假设训练时所有可用的模态在推理时也能访问。然而，许多现实场景违反了这一假设，要求模型在特权模态设置下运行，其中辅助模态仅在训练时可用。尽管这些模态包含有价值的信息，现有的MLLMs在很大程度上未能有效利用它们，因为它们将模态视为可互换的输入，而非互补监督的来源。我们提出了一种新颖的框架——探针混合（MoP），它将MLLM中的模态特定信号和模态通用信号解耦，使模型能够在跨模态学习可转移表示的同时保留模态依赖的结构。其核心是通过结构化的探针机制实现，该机制从共享模态编码器的中间表示中提取和组织信息，而不是像现有MLLMs那样仅依赖最终层的对齐。为支持这种解耦，我们进一步引入了MoP跨模态训练（MoP-X），这是一种以探针解耦损失为中心的MoP训练策略，以防止探针崩溃并促进跨模态学习。我们在一个针对特权模态设置量身定制的综合评估协议下，跨越两个领域、八项任务和四种模态评估了MoP，其中每种模态在推理时被独立视为唯一输入。MoP持续优于强大的MLLM基线，实现了高达65%的相对改进，表明即使辅助模态在推理时不可用，如果在训练时有效利用，也能带来显著收益。代码、模型检查点和评估协议将在https://github.com/Sony/MoP上提供。

## Abstract
Multimodal Large Language Models (MLLMs) are typically designed under the assumption that all modalities available during training will also be accessible at inference. However, many real-world settings violate this assumption, requiring models to operate under a privileged modality setting, where auxiliary modalities are available only during training. While these modalities contain valuable information, existing MLLMs largely fail to leverage them effectively, as they treat modalities as interchangeable inputs rather than sources of complementary supervision. We propose Mixture of Probes (MoP), a novel framework that disentangles modality-specific and modality-general signals within the MLLM, allowing the model to preserve modality-dependent structure while learning transferable representations across modalities. At its core, MoP achieves this through a structured probing mechanism that extracts and organizes information from intermediate representations of a shared modality encoder, rather than relying only on final-layer alignment as done in existing MLLMs. To support this disentanglement, we further introduce MoP Cross-modal Training (MoP-X), a training strategy for MoP centered around a probe disentanglement loss that prevents probe collapse and encourages cross-modal learning. We evaluate MoP across two domains spanning eight tasks and four modalities under a comprehensive evaluation protocol tailored to the privileged modality setting, where each modality is independently treated as the sole input at inference time. MoP consistently outperforms strong MLLM baselines, achieving up to 65% relative improvement, demonstrating that auxiliary modalities, even when unavailable at inference, can provide substantial gains when effectively leveraged during training. Code, model checkpoints, and evaluation protocols will be made available at https://github.com/Sony/MoP.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现代多模态大语言模型（MLLMs）通常假设训练和推理时所有模态（如视频、音频、深度）均可用。然而，许多实际场景（如日常生活活动识别、音乐理解）面临**特权模态设置**：辅助模态（如第一人称视角视频、深度图）仅在训练时存在，推理时只能依赖单一主模态（如第三人称视频或音频）。现有MLLMs将模态视为可互换输入，仅进行最终层对齐，无法有效利用特权模态提供的互补监督信息。
- **整体含义**：本文旨在设计一种能够从特权模态中学习并提升单模态推理性能的框架，突破传统多模态对齐的局限。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：借鉴神经科学中大脑分离模态特定和模态通用表征的证据，以及“模态聚焦假说”，提出**Mixture of Probes (MoP)** 框架。通过在共享通用编码器中层插入结构化的可学习探针（Probes），显式解耦模态特定信号（modality-specific）和模态通用信号（modality-general），实现跨模态知识转移。
- **关键技术细节**：
  - **通用编码器**：使用一个统一的Transformer编码器（如SigLIP）处理所有模态的token序列。
  - **Modality-specific probes**：为每个模态m定义一组Ks个可学习token，仅在该模态样本上更新，捕获模态独有结构。
  - **Modality-general probes**：定义一组Kg个共享可学习token，在所有模态上更新，捕获跨模态共享结构。
  - **层间交互**：探针通过交叉注意力机制与编码器每个中间层的隐藏状态交互，逐层提取信息。最终将探针输出与编码器最后一层输出拼接后投影到LLM嵌入空间。
  - **MoP-X训练策略**：包含两个组件：
    - **探针解耦损失**：计算每层中探针表示之间的余弦相似度，最小化对角线以外的相似度，防止探针坍塌，鼓励特异性和通用性分离。
    - **模态交织批处理**：每个batch包含来自不同模态的样本，确保跨模态参数共享更新。
  - **总损失**：自回归损失 + λ × 探针解耦损失（默认λ=100）。

### 3. 实验设计：数据集、场景、基准、对比方法

- **数据集与场景**：
  - **日常生活活动理解**：使用Ego-in-Exo Perception基准（来自EgoExo4D），包含3,881个问题，四个任务（动作理解、任务区域、手物交互、手部识别）。训练时使用第一人称视频、第三人称视频和深度（从第三人称视频提取），推理时分别只用单一模态。
  - **音乐理解**：使用Music-AVQA数据集，包含9,185个问题，五个任务（计数类、比较类等）。训练时使用音频和视频，推理时只用音频或视频。
- **对比方法**：
  - 无特权模态基线：Unimodal MLLM（仅用目标推理模态训练）。
  - 有特权模态基线：Naive MLLM（所有模态联合训练但无特殊机制）、X-InstructBLIP、OneLLM。
- **评估协议**：每个测试样本仅提供单一模态输入，分别报告各推理模态下的准确率。

### 4. 资源与算力

- 论文明确说明：在**4块NVIDIA H200 GPU**上训练，每个模型训练**1个epoch**。未提供训练总时长。冻结通用编码器，训练tokenizer、连接器、MoP组件，并通过LoRA（秩r=16）微调LLM。

### 5. 实验数量与充分性

- **主要实验**：
  - 两个领域共8个任务，覆盖4种模态（第一人称视频、第三人称视频、深度、音频）。
  - 对比5种基线方法（包括自身消融变体）。
  - 消融实验（探针类型、解耦损失权重、探针位置、损失应用位置）共计约4组。
  - 数据扩展性实验（25%~100%训练数据，3种推理模态）。
  - 探针表征分析（方差、子空间距离、t-SNE可视化）。
- **充分性评价**：实验设计较为全面，涵盖了主要基线、消融关键组件、数据规模影响和内部机制可视化。但仅在两个域（ADL和音乐）评估，缺乏更多样化场景（如医疗、机器人）；未报告统计显著性重复实验；基线选择虽包括主流MLLM，但未与最新蒸馏或特权学习专门方法对比（如LUPI变体）。整体足够支撑主要结论，但存在一定可扩展性空间。

### 6. 论文的主要结论与发现

- **性能提升显著**：MoP在所有推理模态上均优于所有基线，在Ego-in-Exo上平均提升6.5%～11.84%，最高相对改进65%。
- **解耦是关键**：仅用模态特定探针已有提升，再加入模态通用探针进一步改进，验证了解耦设计的必要性。
- **探针解耦损失有效**：该损失增大了探针间方差和子空间距离，防止坍塌，对性能稳定有益。
- **可扩展性**：MoP随训练数据量增加持续改善，而Naive MLLM则停滞。
- **辅助模态价值**：即使推理时缺乏某模态，通过训练时恰当利用也能显著提升单模态性能。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：首次将结构化探针用于MLLM的独特模态设置，明确解耦模态特异与通用表征，而非简单对齐。
- **训练策略精巧**：MoP-X的探针解耦损失和模态交织批处理简单有效，易于实现。
- **实验设计严谨**：针对特权模态设置设计专门评估协议，确保每个推理模态独立测试。
- **可视化分析深入**：通过子空间距离、方差、t-SNE等分析探针行为，验证解耦效果。
- **开源承诺**：代码、模型和评估协议将开源，可复现。

### 8. 不足与局限

- **应用限制**：假设使用通用编码器，无法直接扩展至异构专用编码器；仅处理单模态推理，未涉及多模态推理场景。
- **计算开销**：引入层间交叉注意力，推理时VRAM增加约33%、延迟增加40%，参数仅增加7.5%，但仍需权衡。
- **实验覆盖有限**：仅两个领域、四种模态；未在更大规模或多任务设置（如ImageBind）上验证。
- **隐私风险**：提及辅助模态可能包含敏感信息（如第一人称视频），但未深入讨论防御机制。
- **未与最新特权学习专用方法对比**：如LUPI、跨模态蒸馏等，基线局限于MLLM主流方法。

（完）
