---
title: "Breaking the Evaluation Paradox: Evaluating High-Entropy Search with Computationally Irreducible Constraints"
title_zh: 打破评估悖论：用计算不可约约束评估高熵搜索
authors: "Juntao Wu, Wei Wen, Xianting Huang, Shuai Pang, Ruizhi Qiao, Xing Sun, Ke Wang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22783v1"
tags: ["query:llm-eval"]
score: 8.0
evidence: 解决LLM穷举搜索评估的悖论
tldr: 评估大模型穷举搜索能力存在悖论：验证完整性需完整答案集，但高熵任务人类无法穷举。现有基准依赖部分标注、LLM裁判或单答案问题，无法测试真实遍历。本文提出VERITAS框架，通过设计计算不可约约束，创建易验证但无法优化的搜索任务，强制模型枚举全部空间。该框架可自动生成无限测试用例，具备完美标注与难度控制，成本仅哈希计算。为系统探索评估提供了鲁棒基准，并可生成训练数据以提升此类关键能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22783v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 810, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22783v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1620, \"height\": 695, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22783v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1611, \"height\": 718, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22783v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1326, \"height\": 635, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22783v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1665, \"height\": 724, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22783v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1630, \"height\": 1391, \"label\": \"Table\"}]"
motivation: 现有评估方法无法测试大模型在真实穷举搜索场景中的表现，因验证完整性需要难以获得的完整答案集。
method: 构建计算不可约约束的搜索任务，该约束易验证但无法优化，迫使模型真正遍历整个搜索空间。
result: VERITAS可自动生成无限测试用例，拥有完美ground truth和精确难度控制，边际成本仅是哈希计算。
conclusion: 该框架不仅提供鲁棒评估基准，还可生成训练数据，推动系统探索能力的发展。
---

## 摘要
评估大语言模型（LLM）的穷举搜索能力面临一个基本悖论：验证完备性需要完整的真实答案，然而高熵枚举任务使得人类无法创建这样的真实答案。这导致基准测试系统性地惩罚那些表现优于人类标注者的模型。尽管网络搜索和深度研究代理取得了快速进展——它们现在能发出数百条查询、遍历不同网站并合成长篇报告——但评估仍然在很大程度上依赖于部分标注的答案集、基于LLM的评判者或避免真正穷举搜索场景的单答案问题。我们通过将评估范式从模拟混乱的现实转变为构建计算上纯粹的挑战，打破了这一悖论。我们引入了VERITAS（可验证遍历评估搜索），这是一个基于计算不可约约束原则的框架。通过引入新颖的、不可优化的约束，我们创建了可验证的、稀疏答案的搜索任务，这些任务在计算上等同于穷举枚举。这些约束易于验证，但LLM或搜索引擎无法优化，迫使代理真正遍历整个搜索空间。VERITAS可以自动生成几乎无限数量的测试用例，具有完美的真实答案和精确的难度控制，边际实例成本主要由哈希计算主导。这不仅为评估不确定条件下的系统探索提供了稳健的基准，也为生成训练数据以改进这些关键但未被充分发展的能力提供了一种可扩展的方法。

## Abstract
Evaluating the exhaustive search capabilities of large language models (LLMs) is plagued by a fundamental paradox: verifying completeness requires complete ground truth, yet high-entropy enumeration tasks make such ground truth impossible for humans to create. This causes benchmarks to systematically penalize models for outperforming their human annotators. Despite rapid progress in web-search and deep research agents -- which now issue hundreds of queries, traverse diverse sites, and synthesize long reports -- evaluation still largely relies on partially annotated answer sets, LLM-based judges, or single-answer questions that avoid genuinely exhaustive search scenarios. We break this paradox by shifting the evaluation paradigm from simulating a messy reality to constructing computationally pure challenges. We introduce VERITAS (Verifiable Traversal Assessment for Search), a framework built on the principle of computationally irreducible constraints. By introducing novel, non-optimizable constraints, we create verifiable, sparse-answer search tasks that are computationally equivalent to exhaustive enumeration. These constraints are easy to verify but impossible for LLMs or search engines to optimize, forcing agents to genuinely traverse the entire search space. VERITAS can automatically generate a virtually infinite number of test cases with perfect ground truth and precise difficulty control, with marginal instance cost dominated by hash computations. This provides not only a robust benchmark for evaluating systematic exploration under uncertainty but also a scalable method for generating training data to improve these crucial, yet underdeveloped, capabilities.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：评估大语言模型（LLM）的穷举搜索能力存在一个根本性悖论——验证搜索结果的完备性需要完整的真实答案（ground truth），但对于高熵枚举任务（如“列出某艺术家的所有歌曲”），人类无法构建这样的完备答案集。现有基准（如GAIA、BrowseComp、WideSearch）依赖部分标注、LLM裁判或单答案问题，导致模型如果发现了标注之外的有效结果反而会被视为“幻觉”，从而系统性惩罚表现更好的模型。
- **整体含义**：真正的穷举搜索能力是“深度研究”等复杂信息获取任务的核心，但当前评估方法无法准确衡量这一能力。作者主张将评估范式从模拟混乱的现实转向构建**计算上纯粹的挑战**，通过不可优化的约束来等价模拟穷举枚举的难度，从而实现自动、可验证、可扩展的评估。

### 2. 论文提出的方法论

- **核心思想**：利用**密码学哈希函数**（如MD5）作为“计算不可约约束”。哈希函数具有两个关键性质：
  - **单向验证**：逆转哈希在计算上不可行，但验证只需计算候选对象的哈希值并比较，复杂度O(1)。
  - **语义无关联**：哈希输出与输入语义无关（雪崩效应），搜索引擎无法利用哈希值进行优化检索。
- **关键技术细节**：
  - 将“列出所有X”的任务转化为“找到满足哈希约束的k个稀疏目标”。例如：在某个艺术家的所有歌曲中，找到标题MD5值等于特定目标的歌曲。
  - **理论分析**：在大小为N的无结构搜索空间中，找到k个均匀分布的哈希匹配目标，预期搜索代价E[L_k] = (N+1)·k/(k+1) = Θ(N)，成功概率P ≈ (T/N)^k（T为搜索预算）。因此，寻找少量稀疏目标在计算上等价于完全枚举。
  - **难度控制**：通过调整k（目标个数）和N（搜索空间大小）实现精细化难度调控。
- **算法/流程**：
  - 每个任务定义一个非可优化的哈希约束（如某项目标识符的MD5等于给定值）。
  - 代理使用搜索、访问网页、执行Python代码等工具，必须遍历整个候选空间，然后通过哈希验证找出匹配项。
  - 任务分为四个递进难度层级：**Easy**（单源枚举，N≈50-200）、**Medium**（7个独立子任务复合搜索，N≈7n）、**Hard**（深度属性提取，需访问每个项获取隐藏属性）、**Extra Hard**（嵌套多任务，N≈7×10²~10³）。

### 3. 实验设计

- **数据集/场景**：自建VERITAS基准，任务基于真实网络源（Hugging Face模型库、DBLP论文库、GitHub仓库、conda-forge、Spotify等）。无现有公开数据集直接使用。
- **Benchmark**：VERITAS本身作为评估基准，包含Medium、Hard、Extra Hard三个难度层级（排除Easy）。任务样例见附录表3。
- **对比方法**：评估了9个大语言模型：
  - 开源：GLM-4、MiniMax-M2、DeepSeek-v3.1、Qwen3-235B-A22、Kimi-K2。
  - 闭源：GPT-4o、Gemini-2.5-Pro、GPT-5、Gemini-3-Pro。
- **工具**：统一agent框架，配备四个工具：Search（Google搜索）、Visit（用Qwen3-235B-A22提取页面信息）、Exec Python（执行Python代码，包括哈希验证）、Answer（提交答案）。
- **评估指标**：Pass@4（4次尝试中至少成功1次的成功率）和Avg@4（平均通过率）。工具调用数（Search和Visit次数）。

### 4. 资源与算力

- **资源说明**：论文**未明确说明**训练使用的GPU型号、数量或训练时长。VERITAS本身是评估基准而非训练方法，实验运行在自定义agent框架中，模型通过API调用（闭源）或本地部署（开源，具体硬件未提及）。工具调用中Visit使用了Qwen3-235B-A22作为信息提取模型，但未说明其部署资源。
- **计算成本**：作者指出VERITAS可自动生成测试用例，边际成本**主要由哈希计算主导**，非常低廉。

### 5. 实验数量与充分性

- **实验数量**：
  - 主实验（表2）：9个模型 × 3个难度 = 27个条件，每个条件4次尝试（Pass@4），共108次评估运行。
  - 附录C：用Gemini-3-Pro验证k=1,2,4,8,16的稀疏搜索等价性实验，共5个条件，每个条件多次实验（标准差<3%）。
  - 附录F：2个详细案例研究（成功与失败）。
  - 与WideSearch的工具调用对比（图3）。
- **充分性评价**：
  - **积极方面**：覆盖了多类模型（开源/闭源、不同规模），难度梯度明确，成功率和工具调用数均报告，消融了k的影响。
  - **不足**：
    - 缺乏消融实验：未比较不同哈希函数（SHA-256等）、未控制搜索预算T的精确变化、未分析不同工具组合的影响。
    - 未与现有基准（如GAIA、BrowseComp）在同一条件下直接对比成功率（仅在图3对比了工具调用数）。
    - 实验仅基于自建任务，外部泛化性尚未验证。
  - **公平性**：agent框架统一，工具调用约束一致，闭源模型通过API调用，公平性可接受。但Visit工具使用Qwen3-235B-A22，而同一模型也在被评估列表中（Qwen3-235B-A22），可能引入数据泄露或系统性偏差。

### 6. 论文的主要结论与发现

- VERITAS能有效区分模型能力：前沿模型（GPT-5、Gemini-3-Pro）显著优于其他开源模型。
- Extra Hard层级对当前所有模型仍具挑战：GPT-5 Pass@4=20.0%，Gemini-3-Pro仅6.7%，其他模型接近0。
- 哈希约束成功消除了语义捷径：模型在VERITAS上的工具调用数比WideSearch高1.4–4.2倍，但成功率依然很低，证明任务强制了真正的穷举枚举。
- 验证了计算等价性：随着目标数k从1增至16，成功率从43%收敛到22.1%，与完全枚举的基线一致，支持了“稀疏哈希搜索等价于穷举枚举”的理论。
- 当前模型在系统枚举方面存在根本性差距，不具备可靠的高熵搜索能力。

### 7. 优点

- **解决了评估悖论**：通过非可优化的哈希约束，自动生成无限测试用例，完美ground truth，无人类标注成本。
- **理论基础坚实**：对搜索代价和成功概率进行形式化分析（期望位置公式、超几何分布），并实验验证。
- **难度精准可控**：通过调整k和N实现精细难度分级，且约束本身不可优化，确保任务难度真实反映枚举能力。
- **可扩展性**：可从任意结构化数据源自动生成任务，覆盖文本、代码、API等。
- **训练潜力**：可生成带完美标注的训练数据，用于RL或其他后训练方法提升模型枚举能力。

### 8. 不足与局限

- **仅作评估，未用于训练**：论文明确指出尚未将VERITAS生成的数据用于模型训练，这一方向留作未来工作。
- **实验覆盖有限**：
  - 仅测试了网络搜索场景（Hugging Face、GitHub等），未覆盖其他高熵枚举领域（如代码库、数据库、多模态数据）。
  - Visit工具使用Qwen3-235B-A22，该模型本身也被评估，存在潜在的数据泄露或工具依赖偏差。
  - 未与现有的穷举搜索基准（如WideSearch）在同一度量下直接比较准确率。
- **资源说明缺失**：没有报告实验的硬件算力，影响可复现性。
- **跨语言与偏差**：论文提到中英日平衡，但未详细分析不同语言上的性能差异或文化偏见。
- **应用限制**：哈希约束任务虽在计算上等价，但实际交互体验与语义搜索不同，可能无法完全反映真实场景中的用户偏好（例如，用户通常不需要精确哈希匹配）。

（完）
