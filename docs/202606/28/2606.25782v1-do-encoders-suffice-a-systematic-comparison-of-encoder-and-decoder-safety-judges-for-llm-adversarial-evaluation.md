---
title: Do Encoders Suffice? A Systematic Comparison of Encoder and Decoder Safety Judges for LLM Adversarial Evaluation
title_zh: 编码器足够吗？用于LLM对抗性评估的编码器和解码器安全评判员的系统比较
authors: "Han Jeon, Shiv Medler, Joseph Voyles, Matt Wood"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25782v1"
tags: ["query:llm-eval"]
score: 8.0
evidence: 比较编码器和解码器安全裁判用于LLM对抗评估
tldr: 大型语言模型的安全评估常依赖LLM法官，但成本高、延迟大。本文系统比较了基于编码器的分类器（如ModernBERT、Ettin）与LLM法官在对抗性安全评估中的性能。通过微调编码器分类器并使用多数投票标签，发现编码器能在保持较低F1分数损失的同时显著降低成本与延迟。结果按攻击技术分解，为实际部署提供了权衡指导。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25782v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1249, \"height\": 667, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25782v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25782v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 696, \"height\": 339, \"label\": \"Table\"}]"
motivation: LLM法官虽有效但成本高、延迟大，需寻找更高效的替代方案用于安全评估。
method: 微调ModernBERT等编码器分类器，用LLM法官的多数投票标签训练，并在黄金标准数据集上比较。
result: 编码器分类器在F1和假阴性率上接近LLM法官，尤其在特定攻击类型下表现稳定。
conclusion: 编码器可作为LLM法官的低成本、低延迟替代方案，适用于大规模安全评估。
---

## 摘要
随着大型语言模型（LLM）在聊天机器人和日常应用中的广泛采用，公司越来越需要既有效又保持低成本和低延迟的护栏。LLM输出的安全评估通常依赖于基于LLM的评判员，这些评判员可能有效，但大规模部署时往往速度慢且成本高。在本文中，我们评估来自ModernBERT系列（包括ModernBERT和Ettin）的微调现代编码器分类器能否在用户-模型对话中可靠地识别有害的LLM输出，而相对于基于LLM的评判员，性能损失不会很大。我们使用基于规则的词缀匹配、微调的LLM分类器以及在不同开源对抗性数据集上采用多种评判员提示策略的LLM评判员，对这些编码器分类器进行基准测试。LLM评判员包括来自StrongReject、ShieldGemma、JailbreakBench、AILuminate、SorryBench和Claude作为评判员设置的评估方法，以及微调的安全分类器如LlamaGuard 3和LlamaGuard 4。编码器分类器使用多数投票标签策略在评判员标记的数据上进行微调，然后在金标准保留数据集上进行评估，以评估其相对于LLM评判员的性能。我们使用F1分数、假阴性率和精确率-召回率指标报告绝对性能。我们还按攻击技术（包括单轮提示、分解、升级和上下文操纵）细分结果，以确定编码器分类器在哪些方面与基于LLM的评判员一致或偏离。我们的发现为编码器分类器何时可以作为基于LLM的安全评估的成本和延迟有效的替代方案提供了指导。

## Abstract
With the widespread adoption of large language models (LLMs) in chatbots and everyday applications, companies increasingly need guardrails that are effective while remaining low-cost and low-latency. Safety evaluation of LLM outputs has generally relied on LLM-based judges, which can be effective but are often slow and expensive to deploy at scale.   In this paper, we evaluate whether fine-tuned modern encoder classifiers from the ModernBERT family, including ModernBERT and Ettin, can reliably identify harmful LLM outputs in user-model conversations without substantial performance loss relative to LLM-based judges. We benchmark these encoder classifiers against rule-based prefix matching, fine-tuned LLM classifiers, and LLM judges using a range of judge-prompting strategies across open-source adversarial datasets.   The LLM judges include evaluation methodologies from StrongReject, ShieldGemma, JailbreakBench, AILuminate, SorryBench, and a Claude-as-a-judge setup, as well as fine-tuned safety classifiers such as LlamaGuard 3 and LlamaGuard 4. The encoder classifiers are fine-tuned on judge-labeled data using a majority-voting label strategy and are then evaluated on a gold-standard holdout dataset to assess their performance relative to LLM judges.   We report absolute performance using F1 score, false negative rate, and precision-recall metrics. We also break down results by attack technique, including single-turn prompting, decomposition, escalation, and context manipulation, to identify where encoder classifiers align with or diverge from LLM-based judges. Our findings provide guidance on when encoder classifiers can serve as cost- and latency-efficient alternatives to LLM-based safety evaluation.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：大型语言模型（LLM）广泛应用于聊天机器人等场景，但其输出可能包含有害内容，因此需要安全评估（safety evaluation）。当前主流方法是使用基于解码器的LLM作为评判员（LLM-as-a-judge），如通过提示（prompt）让大型自回归模型判断回答是否有害，或使用微调的小型解码器模型（如LlamaGuard 3/4）。这些方法虽然有效，但存在**高成本、高延迟**的问题，尤其在需要大规模实时安全过滤的生产环境中难以承受。
- **核心问题**：能否使用基于编码器的分类器（encoder classifiers）替代基于LLM的评判员，在保持相近安全检测性能的同时，显著降低成本和延迟？具体地，论文评估了ModernBERT系列（包括Ettin）微调后的编码器模型在对抗性安全评估中的表现。
- **研究动机**：安全分类本质上是语言理解任务——判断响应是否有害需要全面理解整个输入，而非生成新token。编码器双向处理输入，天然适合分类；并且ModernBERT等新模型具有长上下文窗口，可能胜任该任务。因此，论文系统比较编码器与解码器评判员在对抗性安全评估中的性能、成本与延迟。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过微调编码器模型（ModernBERT家族的Ettin-150M和Ettin-400M），利用多个LLM评判员的**多数投票标签**作为训练信号，训练一个二分类器（有害/安全）。然后与多种解码器类评判员进行对比。
- **关键技术细节**：
  - **训练数据集构建**：收集14个源数据集（含11个公开数据集和3个合成数据集），包括HarmBench、Simple Safety Tests、ToxicChat、WildJailbreak、AdvBench、OR-Bench等。对每个提示，通过16个目标模型、4种攻击技术和2种温度设置生成约56.3万个（提示，响应）对。
  - **标签聚合**：用7个评判员（包括LlamaGuard 3/4、Claude Judge、SorryBench、JailbreakBench Judge、AILuminate Judge、StrongReject）对每个输出打标签。通过多数投票（>50%）生成最终训练标签。并采用**高置信度过滤**：只保留全票安全（0票有害）或强多数有害（≥5/7票）的样本，剔除1-4票的模糊样本，将数据集从56.3万缩减至30.7万行（Fleiss' κ从0.232提升至0.805）。
  - **评估方案**：使用OOD保持集（JailbreakBench + AILuminate，共4.29万样本），每个样本包含4个前沿目标模型、4种攻击技术和2种温度下的响应。保持集带有基准作者提供的地面真值标签，用于评估编码器分类器泛化能力。
  - **攻击技术**：单轮直接提示、分解（将有害请求拆分为多个良性子查询）、升级（从友好逐步升级）、上下文操纵（将有害请求嵌入良性框架）。每种技术有多轮调用但最终只取最后一个（提示，响应）对用于分类。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **训练数据**：14个来源，包括HarmBench、SafetyBench、Simple Safety Tests、ToxicChat、WildJailbreak、AdvBench、OR-Bench、BeaverTails-Eval、Do-Not-Answer、XSTest以及医疗/金融/内部合成对抗数据。总计9,968个源提示，563,216个（提示，响应）对。
  - **保持集（OOD评估）**：JailbreakBench Behaviors（200个行为）和AILuminate v1 Demo（1,200个测试用例），共1,400个源项，通过4个前沿模型 × 4种攻击 × 2个温度产生42,899个成功样本（1,901个失败去除）。该保持集从未用于训练。
- **基准对比方法**：
  - **编码器分类器**：Ettin-150M、Ettin-400M（均在多数投票标签上微调，并含全数据集和高置信度两种训练条件）。
  - **解码器式评判员**：LlamaGuard 3、LlamaGuard 4、Claude Judge（使用Claude Haiku 4.5）、SorryBench（GPT-4o-mini）、JailbreakBench Judge（LLaMA 3.3 70B）、AILuminate Judge（Claude Sonnet 4.5）、StrongReject（Claude Haiku 4.5）、ShieldGemma-2B/9B，以及一个基于规则的前缀匹配基线。
- **评价指标**：主要指标为F1分数（平衡精确率与召回率），同时报告假阴性率（FNR，安全关键指标）、精确率、召回率、有害率。还报告了AUROC（编码器模型达0.93以上）和高置信度子集上的AUPRC。所有指标含95%自举置信区间。

## 4. 资源与算力

- 论文明确提及了推理基准测试的硬件环境：在**Azure ML Standard_ND40rs_v2**实例上进行，该实例配备**8块NVIDIA Tesla V100 GPU**（32GB显存）。测试时使用FP16精度，批量大小为1、8、32、64。
- 训练细节未明确说明GPU数量、训练时间等，仅提到微调编码器分类器使用了多数投票标签。计算资源成本估算基于Azure ML定价（$22.03/小时）和低配CPU实例（$0.25/小时）。
- 因此，论文缺乏编码器微调的详细算力需求（如GPU型号、时长、显存占用等），但对推理性能的测量是充分的。

## 5. 实验数量与充分性

- **实验数量丰富**：
  - 训练阶段：14个数据集、16个目标模型、4种攻击技术、2种温度设置，生成超56万样本。
  - 评估阶段：OOD保持集包含4个前沿模型、4种攻击、2种温度，近4.3万样本。
  - 对比了10余种方法（包括编码器和解码器）。
  - 进行了两类训练条件（全数据集 vs. 高置信度过滤）的消融实验。
  - 按攻击技术分解分析（单轮、分解、升级、上下文操纵）的F1和FNR。
  - 推理吞吐量与延迟的基准测试（批量大小1、8、32、64）。
- **充分性和客观性**：
  - 使用question-level分组划分训练/验证/测试，防止数据泄漏（同一问题在不同模型间的响应不被分割到不同折）。
  - OOD保持集完全独立，且带有地面真值标签，可避免裁判偏差。
  - 所有指标含置信区间，结果具有统计意义。
  - 遵循标准评估协议（如JailbreakBench、AILuminate官方协议）保证了可复现性。
  - 但部分对比方法（如ShieldGemma）表现极差，可能未微调到该任务（论文未说明），需注意公平性。整体实验设计严谨、充分。

## 6. 论文的主要结论与发现

- **编码器分类器性能**：在OOD保持集上，Ettin-150M获得F1=0.547、FNR=0.422；Ettin-400M获得F1=0.449但FNR仅0.146（更安全，因误报率高）。两者均优于大多数解码器评判员如LlamaGuard 3/4、Claude Judge、StrongReject、ShieldGemma，但弱于基准对齐的AILuminate（F1=0.767）和SorryBench（F1=0.706）。
- **高置信度子集上的性能大幅提升**：在滤除低一致样本后，Ettin-150M F1=0.800，Ettin-400M F1=0.894，接近顶级评判员。说明**标签噪声是主要限制因素**，而非编码器架构能力。
- **对多轮攻击的脆弱性**：编码器分类器在分解攻击上FNR最高（Ettin-150M为0.506），因为其仅看到最终对话对，无法利用中间推理。增大模型尺寸可缓解（Ettin-400M分解FNR降至0.116），但这仍是根本局限。
- **成本与延迟优势巨大**：在批量大小为8时，Ettin-150M吞吐量244.8 completions/s vs LlamaGuard-4 1.31 completions/s（187倍提升），中位延迟3.6ms vs 718ms。成本上，Ettin可运行于CPU实例，大幅降低部署费用。
- **实际建议**：编码器分类器适合作为**第一级快速过滤器**，将模糊/困难案例传递给更强大的解码器评判员。

## 7. 优点

- **系统全面**：首个大规模比较现代编码器与解码器安全评判员的工作，覆盖多数据集、多目标模型、多攻击技术，实验设计严谨。
- **反数据泄漏措施**：采用question-group分割，避免模型之间的问题泄漏，确保了泛化评估的可靠性。
- **有效应对标签噪声**：通过高置信度过滤提升训练标签质量，并作为消融验证噪声是瓶颈。
- **成本-延迟-性能权衡分析**：提供了实际部署所需的吞吐量、延迟和成本数据，具有工程指导价值。
- **分解攻击类型的细粒度分析**：揭示了编码器在多轮攻击中的弱点，为后续改进指明了方向。

## 8. 不足与局限

- **训练标签依赖LLM评判员**：多数投票标签仍受评判员系统误差影响，虽然高置信度过滤部分缓解，但无法完全消除。
- **多轮攻击覆盖不足**：编码器仅处理最后对话对，无法利用完整对话历史。论文将其定位为第一线过滤器，但未探索结合序列建模或多轮提示的编码器变体。
- **实验局限性**：
  - 保持集中攻击类型与训练集相似（虽模型不同），可能低估域外泛化难度。
  - 部分对比方法（如ShieldGemma）未经微调或任务不匹配，比较可能有失公平（论文未详细说明其训练设置）。
  - 只有两个保持集（JailbreakBench和AILuminate），且地面真值标签为提示级而非响应级，可能与响应级判断存在偏差。
- **资源算力信息不完整**：未提供编码器微调的具体训练硬件、时长和成本，不利于完全复现。
- **F1分数总体偏低**：最优编码器F1为0.547（全集），若直接替代顶级评判员可能导致过多安全漏报或误报，仅在成本敏感场景下作为初步过滤有意义。

（完）
