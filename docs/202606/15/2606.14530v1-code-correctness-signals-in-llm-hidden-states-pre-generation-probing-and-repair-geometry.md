---
title: "Code Correctness Signals in LLM Hidden States: Pre-Generation Probing and Repair Geometry"
title_zh: 大语言模型隐藏状态中的代码正确性信号：生成前探测与修复几何
authors: Carlo Di Cicco
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14530v1"
tags: ["query:llm-lang-rep"]
score: 8.0
evidence: 从LLM隐藏状态解码代码正确性信号，探索表示的可解释性
tldr: 本研究探索大型语言模型隐藏状态中代码正确性的可读性。使用Qwen3-4B-Instruct模型在444个LiveCodeBench任务上，通过线性探针从生成前的最终隐藏状态解码首次尝试的正确性，AUC达0.93，残差化控制提示长度后仍为0.91。在236个修复案例中，隐藏状态偏移方向虽统计显著，但经修复上下文协变量残差化后消失，表明该信号由上下文驱动而非独立的修复理解特征。贡献在于同时报告正向与负向结果，强调方法学诚实。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14530v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1305, \"height\": 214, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14530v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1110, \"height\": 164, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14530v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 973, \"height\": 268, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14530v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 979, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14530v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 961, \"height\": 214, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14530v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1566, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14530v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 822, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14530v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 415, \"height\": 361, \"label\": \"Table\"}]"
motivation: 探究代码正确性是否在LLM生成前可从隐藏状态线性解码，以及修复失败尝试时隐藏状态变化是否蕴含修复成功信号。
method: 对Qwen3-4B-Instruct的444个任务，用线性探针解码生成前最终隐藏状态正确性，并通过残差化控制提示长度；对236个修复案例，分析隐藏状态偏移方向的统计显著性并进行上下文协变量残差化。
result: 预生成正确性解码AUC达0.93±0.01，残差化后0.91±0.01；修复偏移方向显著但被上下文协变量解释，不独立。
conclusion: 代码正确性在生成前可解码，但修复成功信号仅为上下文相关，方法学上警示需控制混杂变量，并诚实汇报负结果。
---

## 摘要
大语言模型在其隐藏状态中编码了丰富的信息。本研究探究代码正确性是否在Qwen3-4B-Instruct-2507生成之前以及修复失败尝试时，在其隐藏状态中可被识别，基于444个LiveCodeBench任务。研究通过一个混淆控制工具——残差化——报告了两个发现。首先，模型首次尝试代码的正确性可从提示末尾的隐藏状态中线性解码，在50次外部分割中无信息泄露的留出AUC为0.931±0.008。在从每个隐藏状态维度中移除提示长度的线性效应后，探测仍达到0.911±0.010，远高于提示长度基线0.754±0.014。其次，在236个模型尝试修复首次失败的清理案例中，从失败尝试到修复的隐藏状态偏移具有统计上可检测的对比方向，在幅度和分割半测试中相对于标签打乱的零假设均显著。该方向在针对成功与失败修复之间不同的修复上下文协变量进行条件残差化后不再存在，表明它是修复成功的一个相关因素，由修复上下文驱动，而非独立的修复理解特征。探测层通过嵌套交叉验证选择，相同的支撑生成前正确性结果的残差化方法推翻了修复方向的解释。贡献既是方法论的也是实证的：诊断方法诚实到足以同时报告正面和负面结果。

## Abstract
Large language models encode rich information in their hidden states. This work asks whether code correctness is legible in the hidden states of Qwen3-4B-Instruct-2507, before it generates and as it repairs a failed attempt, studied on 444 LiveCodeBench tasks. It reports two findings connected by a single confound-control tool: residualization. First, the correctness of the model's first-attempt code is linearly decodable from the prompt-final hidden state, with a leakage-free held-out AUC of 0.931 +/- 0.008 across 50 outer splits. After the linear effect of prompt length is removed from each hidden state dimension, the probe still reaches 0.911 +/- 0.010, well above a prompt-length baseline of 0.754 +/- 0.014. Second, on 236 cleaned cases where the model attempts to repair a failed first attempt, the hidden state shift from the failing attempt to its repair carries a statistically detectable contrastive direction, significant on both a magnitude and a split-half test against label-shuffled nulls. This direction does not survive a conditional residualization against repair-context covariates that differ between successful and failed repairs, marking it as a correlate of repair success driven by the repair context rather than an isolated repair-comprehension feature. The probe layer is selected by nested cross-validation, and the same residualization approach that upholds the pre-generation correctness result overturns the repair-direction interpretation. The contribution is as much methodological as empirical: a diagnostic honest enough to report a negative result alongside a positive one.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：随着大语言模型（LLM）在代码生成任务上的能力快速提升，研究者开始关注其内部计算过程——特别是隐藏状态是否编码了代码正确性。已有工作通过线性探针研究已生成代码的隐藏状态来判断正确性，但本文提出一个更“上游”的问题：在模型**生成任何输出之前**，仅基于提示（prompt）的隐藏状态，是否就能解码代码正确性？此外，当模型被允许修复自身失败的代码时，隐藏状态从失败到修复的变化是否包含一个稳定、可识别的修复成功方向？
- **整体含义**：该研究同时推进了**机制可解释性**（mechanistic interpretability）和**表示工程**（Representation Engineering）的实证理解。它提供了一个方法论贡献：用同一个混杂变量控制工具（残差化，residualization）来检验正向和负向两个结论，强调在可解释性研究中诚实汇报负结果的重要性。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  - **预生成正确性探测**：训练一个线性分类器（线性探针）于提示末尾隐藏状态向量，预测模型首次尝试代码的正确性（pass/fail）。通过嵌套交叉验证选择层和正则化参数，并用残差化控制提示长度这一混杂变量。
  - **修复方向分析**：对每个任务，计算从失败尝试（attempt 0）到修复尝试（attempt 1）的隐藏状态差δ = h(attempt1) - h(attempt0)，取成功组和失败组δ的均值差作为对比方向v = mean(success) - mean(failure)。通过幅度检验（||v||）和分割半余弦稳定性检验评估其统计显著性。然后对三个修复上下文协变量（Δprompt_tokens、code_length_attempt_0、failure_type）进行条件残差化，检验方向是否存续。
- **关键技术细节**：
  - 模型：Qwen3-4B-Instruct-2507，36层，2560维隐藏状态。
  - 探针流水线：StandardScaler → PCA（保留95%方差）→ LogisticRegression（L2正则，C通过5折CV选择，网格{1e-3,1e-2,1e-1,1,10}）。
  - 残差化：对每个隐藏状态维度，用OLS回归以候选协变量为预测变量，拟合后减去预测值。预生成中协变量为prompt_tokens；修复方向中对Δprompt_tokens、code_length_attempt_0和failure_type（哑变量）回归。残差化在每次随机分割的训练集上拟合，再应用于测试集。
  - 层选择：嵌套CV，外层50次80/20分割，内层5折CV联合选择层（29-36）和C，最终采用模态配置（layer 29, C=0.01）。
  - 修复方向统计检验：幅度检验——比较||v||与1000次标签打乱的零分布；分割半余弦——将236个样本分成两个分层半集，各自计算v后取余弦，重复1000次，与打乱标签零分布比较p值。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：444个**LiveCodeBench**任务，覆盖easy、medium、hard三个难度等级。每个任务有单元测试，代码正确性由测试通过/失败判定。
- **基准**：
  - **预生成探测**：
    - “Raw”：直接使用原始隐藏状态特征。
    - “Residualized”：残差化提示长度后的隐藏状态。
    - “Prompt-only baseline”：仅用prompt_tokens一个特征，用逻辑回归（以及随机森林、梯度提升、MLP验证非线性模型的边界）。
  - **修复方向**：
    - **原始δ**：不做协变量调整的对比方向。
    - **残差化δ**：对三个修复上下文协变量进行条件残差化后的对比方向。
    - 两个统计检验的零分布（标签打乱）。
- **对比方法**：没有与其他论文的方法直接对比（如对比先前工作[2][3]的隐藏状态读取时机不同），但通过嵌套CV和残差化减少了过拟合和混杂。

## 4. 资源与算力

- 论文明确说明：“所有实验在配备NVIDIA RTX 4050（6 GB VRAM）的单一笔记本上运行。”未提及训练时长、能耗或GPU数量。由于模型为4B参数且仅在本地推理探针训练，计算量相对较低，但论文未提供具体时间。

## 5. 实验数量与充分性

- **预生成正确性探测**：
  - 独立对各层（29-36）运行50次随机80/20分割，共8×50=400轮探针训练。
  - 嵌套CV 50次外层分割，每次内层5折CV选择层和C。
  - 对比三种配置（Raw、Residualized、Prompt-only），Prompt-only还额外测试了随机森林、梯度提升、MLP在相同嵌套CV下。
- **修复方向分析**：
  - 从444任务中经两次筛选（除去重复循环和truncated attempt-0）得到236个clean 0→1转换。
  - 进行幅度检验和分割半余弦检验各1000次打乱。
  - 检查三个协变量的组间差异（Welch t检验和卡方检验），然后对残差化后δ重新运行两个检验。
- **充分性**：实验设计较为充分：
  - 嵌套CV确保无信息泄露，避免层选择偏差。
  - 多次随机分割提供置信区间。
  - 对促进混淆变量（prompt length）及其非线性模型做了详尽验证。
  - 修复方向分析考虑了协变量平衡，控制混杂。
  - **不足之处**：仅基于单一模型（Qwen3-4B），跨模型泛化未验证；仅分析了第一次修复（0→1），后期修复数据不足（成功样本少），无法进行类似分析；层选择仅探测了29-36，峰值在边界，未探索更低层。

## 6. 论文的主要结论与发现

1. **预生成正确性可线性解码**：基于提示末尾隐藏状态的线性探针在无信息泄露的留出数据上达到AUC 0.931±0.008；残差化剔除提示长度后仍达0.911±0.010，远高于仅用提示长度的基线0.754±0.014。该信号在生成任何输出前即存在。
2. **修复方向存在但非独立**：在原始δ上，对比方向统计显著（幅度p<0.001，分割半余弦p<0.001）；但在对三个组间差异显著的修复上下文协变量进行条件残差化后，方向消失（幅度p=0.126，分割半余弦p=0.942）。表明该方向是修复成功的相关因素，由修复上下文的差异驱动，而非一个独立的修复理解特征。
3. **方法学贡献**：残差化这一工具在预生成探测中确认了信号稳健，在修复方向分析中推翻了原始信号，展示了如何在同一框架下诚实汇报正向和负向结果。

## 7. 优点：方法或实验设计上的亮点

- **严格的混淆控制**：对预生成探测，通过残差化明确去除提示长度的影响，并验证非线性模型无法超越逻辑回归基线；对修复方向，先检验协变量组间差异再决定是否残差化，避免过度调整。
- **嵌套CV防止信息泄露**：层选择、正则化参数选择都在内层CV完成，外层测试集从未参与选择，确保AUC估计无偏。
- **双重检验统计显著性**：修复方向使用幅度（norm）和几何稳定性（余弦）两种互补检验，增强可信度。
- **诚实报告负结果**：特意在摘要和结论中强调“同一诊断工具可能给出截然不同的结论”，体现了科学透明度。
- **开源完整代码、数据和脚本**：论文提供GitHub仓库，可复现所有分析。

## 8. 不足与局限

- **单一模型**：仅使用Qwen3-4B-Instruct，结论可能不适用于其他模型族（如Llama、GPT），需跨模型复现。
- **层范围受限**：探测出的最佳层为29，处于所捕获范围的下边界（28可能更佳？），且未分析更早层。修复方向结论虽在29-35稳定，但无法排除更低层存在不同模式。
- **后期修复数据不足**：0→1之后修复成功样本太少（1→2仅13个成功），无法进行方向分析，无法判断方向在后续步骤是否一致或消失。
- **协变量冗余**：Δprompt_tokens和code_length_attempt_0高度相关（r=0.995），虽然作者论证冗余对结果影响可忽略，但更干净的协变量选择会更理想。
- **方向一致性和距离收敛分析无结论**：因数据不足，无法判断修复路径的方向一致性或向成功区域的收敛趋势。
- **未公开全部实验细节**：如具体算力耗时、模型推理配置等未提及，可能影响复现细节。

（完）
