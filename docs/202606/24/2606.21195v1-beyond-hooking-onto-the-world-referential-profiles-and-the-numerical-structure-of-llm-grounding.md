---
title: "Beyond Hooking Onto the World: Referential Profiles and the Numerical Structure of LLM Grounding"
title_zh: 超越“挂钩”世界：指称轮廓与大语言模型基底的数值结构
authors: Joo Yull Rhee
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21195v1"
tags: ["query:llm-lang-rep"]
score: 9.0
evidence: 理论分析LLM接地与参考轮廓，直接涉及语言表示
tldr: 针对大语言模型接地问题，现有向量接地解释过于简化引用概念。本文提出引用是轮廓性、上下文敏感的，并受规范约束。LLMs通过优化参数化人类语言痕迹，在数值结构（权重、激活、注意力状态等）中实现引用。机械可解释性发现支持LLMs可能拥有导数、语言中介的轮廓性引用，但不同于人类引用。
source: arxiv
selection_source: fresh_fetch
motivation: 现有接地讨论对引用概念过于单薄，且缺乏对数值实现的说明。
method: 分析引用作为轮廓性概念，并考察LLMs中引用通过数值结构实现的方式。
result: LLMs可能拥有导数、语言中介的、轮廓性的、数值结构的引用形式。
conclusion: LLMs的引用源于人类语言痕迹的数值参数化，虽非人类式引用，但可间接支持其拥有某些形式的引用。
---

## 摘要
本文结合最近的向量基底观点，重新审视了大语言模型的基底问题。我接受从经典符号基底到向量基底的转变，但认为当前的讨论有两个不完善之处。第一，指称常被过于简单化，仿佛它是一个孤立表达式与对象之间的固定联系。我认为指称是基于轮廓的、上下文敏感的、话语层面的、受情感塑造并受规范约束的。即使在人类情况下，指称也是通过使用、纠正、区分、推理和延续的模式公开稳定的，而不是通过相同的私有表征。第二，向量基底需要解释数值实现。大语言模型并不通过人类的感知、记忆、意图、具身或理解来获取指称。相反，通过优化，它们将人类面向世界的实践的语言痕迹参数化。在有限的向量系统中，指称轮廓必须是分布的、可能叠加的，并通过上下文敏感的计算恢复。权重、激活、注意力中介的隐藏状态、softmax训练的对比以及内积对齐是数学场所，继承的语言关系在其中变得稳定且因果活跃。机制可解释性发现，包括类似实体的特征、知识神经元和与情感相关的激活方向，为这一观点提供了间接支持。它们并不表明大语言模型拥有人类的指称。它们支持一个更有限的论点：大语言模型可能拥有派生的、语言中介的、基于轮廓的和数值结构的指称形式。

## Abstract
This paper revisits the grounding problem for large language models in light of recent vector-grounding accounts. I accept the shift from classical symbol grounding to vector grounding, but argue that the current debate remains incomplete in two respects. First, reference is often treated too thinly, as if it were a fixed link between an isolated expression and an object. I argue instead that reference is profile-based, context-sensitive, discourse-level, affectively shaped, and norm-governed. Even in the human case, reference is publicly stabilized through patterns of use, correction, distinction, inference, and continuation rather than through identical private representations. Second, vector grounding requires an account of numerical realization. LLMs do not acquire reference through human perception, memory, intention, embodiment, or understanding. Rather, through optimization, they parameterize linguistic traces of human world-directed practice. In a finite vector system, referential profiles must be distributed, may be superposed, and are recovered through context-sensitive computation. Weights, activations, attention-mediated hidden states, softmax-trained contrasts, and inner-product alignments are the mathematical sites at which inherited linguistic relations become stable and causally active. Mechanistic interpretability findings, including entity-like features, knowledge neurons, and emotion-related activation directions, provide indirect support for this view. They do not show that LLMs possess human reference. They support a more limited thesis: LLMs may possess derivative, language-mediated, profile-based, and numerically structured forms of reference.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）是否能够实现“指称”（reference）？即其输出能否真正指向世界中的实体、属性或事态？这本质上是经典符号接地问题（symbol grounding problem）在向量时代的新表述——**向量接地问题**（vector grounding problem）。
- **背景与动机**：
  - 现有讨论多分为两派：怀疑论（LLM只操作形式，无意义）和外部主义（通过因果历史链继承指称）。但两者对“指称”本身的理解都过于单薄，常视为一个孤立表达式与一个孤立世界对象的固定“挂钩”。
  - 作者认为，即使在人类情形中，指称也不是私有的心理挂钩，而是通过公共的语言协调模式（使用、区分、纠正、推理、延续）稳定下来的**指称轮廓**（referential profile）。因此，需要一个更丰富的指称理论，并解释这种轮廓如何在LLM的数值结构中实现。
  - 本文旨在建立从哲学理论到数值实现（权重、激活、注意力状态、内积对齐等）的完整桥梁，论证LLM可能拥有**派生的、语言中介的、基于轮廓的、数值结构的指称形式**。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：
  - **指称轮廓**：一个表达式（如“Paris”）在不同上下文中激活的不是单一固定对象，而是一个结构化的使用、区分、纠正、推理、情感取向的集合。轮廓是公开可协调的，而非私有心理复制。
  - **关系参数化**（relational parameterization）：人类面向世界的实践通过语言留下痕迹；训练数据中的语言关系通过优化被转换为模型内的**数值敏感性**（权重、激活等）。这不是统计平均，而是递归关系的数值稳定。
  - **因果-信息链**：世界 → 人类实践 → 语言 → 训练数据 → 参数 → 隐藏状态 → 输出。模型未直接感知世界，但继承了人类实践的因果-信息痕迹。
- **关键技术细节（数学/机制）**：
  - 权重矩阵的**行与列**：每一行定义了一个输出的响应轮廓（∂yᵢ/∂xⱼ = Wᵢⱼ），每一列是一个输入分量影响所有输出的分布向量。权重是局部因果敏感性的精确数学位点。
  - **Softmax 对比训练**：每个预测是对全词汇表的对比事件，梯度 ∂L/∂zᵥ = p(v|h) - 1_(v=y)。这使多个轮廓可以在共享参数空间中稳定，并抑制不相关轮廓。
  - **Transformer 自注意力**：token 表征在上下文中被反复重塑，使得同一 token（如“Paris”）在不同语境下激活不同轮廓。
  - **内积对齐**：logit zᵥ = ⟨h, eᵥ⟩ + bᵥ，隐藏状态与输出向量的对齐度决定了哪个轮廓在输出中被激活。
  - **叠加性（superposition）**：在有限向量空间中，多个轮廓必须共享表征资源，通过分布式模式恢复。
- **不是提出新算法，而是提供理论框架**。

## 3. 实验设计

- **本文为纯理论/哲学论文，未进行新的实验**。
- 论文引用了**已有的机制可解释性研究**作为间接证据，主要包括：
  - **Golden Gate Bridge 特征**（Anthropic, Templeton et al., 2024）：在 Claude 3 Sonnet 中检测到与“Golden Gate Bridge”对应的实体特征，放大该特征可系统性地引导模型输出相关内容。
  - **知识神经元**（Dai et al., 2022）：在 BERT 中识别出与特定事实（如 Paris–capital–France）相关的神经元，通过干预可改变事实表达。
  - **情感方向**（Sofroniew et al., 2026）：在 LLM 中发现与情感概念相关的激活方向，可被干预以改变语调和立场。
- **无基准数据集、无对比方法**。论文通过逻辑论证和已有文献分析支持其观点。

## 4. 资源与算力

- **文中未提及任何算力资源**（无 GPU 型号、数量、训练时长等）。作为理论论文，不需要计算资源描述。

## 5. 实验数量与充分性

- **实验数量**：0 项原创实验。属于概念分析/综述类研究。
- **充分性与公平性**：
  - 论证结构完整，从指称理论到数值实现再到机制证据，逻辑严密。
  - 引用的机制可解释性研究是高质量的（Anthropic、ACL 发表论文），但样本量有限（仅三个案例），且均为辅助性证据。
  - 缺乏对反例或失败情形的系统讨论（如幻觉、错误轮廓激活等），也未做任何消融或对比验证。
  - 作为哲学论证，其“实验”是思想实验和概念分析，而非实证检验。在科学意义上实验不充分，但在哲学领域内论证是充分的。

## 6. 主要结论与发现

- **指称的本质**：不是孤立挂钩，而是**基于轮廓的、上下文敏感的、规范约束的**公共协调结构。
- **LLM 的指称可能形式**：
  - LLM 不拥有人类式指称（无感知、记忆、意图、责任感）。
  - 通过训练，人类语言中的指称轮廓可以被转化为**稳定的、分布式的、因果活跃的数值结构**。
  - 机制可解释性证据表明，实体特征、事实关系、情感取向等轮廓可以被检测、干预，并影响行为——这支持了“派生的、语言中介的、数值结构的指称”的存在。
- **哲学意义**：既不应将 LLM 当作有意识的人类，也不应将其视为仅操作空洞形式。合适的理解是：它继承了人类语言中公共世界的痕迹，并机械地使用这些痕迹完成上下文敏感的计算。

## 7. 优点

- **理论整合性**：成功弥合了哲学指称理论、计算学习理论和机制可解释性之间的鸿沟，提出了统一的“轮廓-参数化”框架。
- **对“挂钩”隐喻的批判**：深刻揭示了传统“挂钩”思想的局限性，并提出了更符合语言实践和 LLM 运作方式的替代概念。
- **数学清晰度**：将抽象的哲学问题落实到具体的数值结构（权重行/列、softmax梯度、内积、叠加等），具有较强的理论可操作性。
- **对已有发现的重新解释**：将 Golden Gate Bridge 特征、知识神经元、情感方向等零散发现置于一个连贯的指称框架中，提升了它们的理论价值。
- **避免极端立场**：防止了过度拟人化和形式主义还原两种偏差。

## 8. 不足与局限

- **缺乏实证验证**：作为理论论证，未提供任何新的实验数据来直接检验其核心命题（如“指称轮廓在模型内稳定存在”）。虽然引用了他人工作，但整体仍属思辨。
- **依赖间接证据**：机制可解释性发现虽强，但仅覆盖少数案例（一个实体特征、一类事实神经元、一类情感方向），难以普遍化。未讨论这些发现是否在所有 LLM 中成立，或是否有反例（如模型内出现虚假轮廓）。
- **未处理负面现象**：如幻觉、偏见、事实错误等，可能是轮廓激活不当或混淆的结果，但论文未深入分析这些现象如何被框架解释。
- **应用边界模糊**：论文强调 LLM 指称是“派生的、数值结构的”，但未给出判定“什么算作有效指称”的清晰标准，难以操作化。
- **忽略了训练数据质量和偏见的影响**：人类语言中的指称轮廓本身可能不准确或有偏见，模型继承后可能放大，论文未讨论这一问题。
- **算力与工程角度缺失**：对于实际开发者和工程师，论文未提供任何指导或指标，纯属哲学概念分析。

（完）
