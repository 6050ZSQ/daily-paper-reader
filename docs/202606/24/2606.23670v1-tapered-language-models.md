---
title: Tapered Language Models
title_zh: 锥形语言模型
authors: "Reza Bayat, Ali Behrouz, Aaron Courville"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23670v1"
tags: ["query:llm-lang-rep"]
score: 9.0
evidence: 非均匀层参数分配改善LLM中的语言表示
tldr: 现代语言模型默认在深度上均匀分配参数，但研究表明各层次贡献不同。本文提出Tapered Language Models (TLMs)，在固定预算下对MLP宽度按深度单调递减（如余弦调度），让早期层容量更大、后期层更小。实验表明，该策略在不增加参数和计算量的前提下，显著提升困惑度和下游任务性能，并跨多种架构（Transformer、Gated Attention等）有效。TLMs揭示了深度感知分配这一简单且架构无关的设计杠杆。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23670v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1702, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23670v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1694, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23670v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1648, \"height\": 510, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23670v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1702, \"height\": 524, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23670v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1402, \"height\": 458, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23670v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1709, \"height\": 1333, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23670v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 831, \"label\": \"Table\"}]"
motivation: 均匀层参数分配未反映层贡献的不对称性，启发探索在固定预算下按深度差异分配容量以提升性能。
method: 提出TLMs，对MLP宽度使用单调余弦调度从深到浅递减，其他结构不变，总参数固定。
result: 在三个规模和四种架构上，TLMs相比均匀基线持续降低困惑度并提升下游基准，无额外成本。
conclusion: 深度感知的参数分配是简单、架构无关且免费的语言模型设计杠杆。
---

## 摘要
现代语言模型，包括Transformer、循环神经网络和基于记忆的变体，共享一个共同的基础架构：一组相同的层，其中参数在深度上均匀分配。这是从原始Transformer继承而来的默认设置，此后基本未变，但越来越多的证据表明，各层对最终输出的贡献是非均匀的，较后的层更多地是细化残差流而非转换它。我们探究参数容量是否应反映这种不对称性。我们的受控实验表明，在固定预算下，将更多容量分配给较前层、较少容量分配给较后层，相比于均匀宽度的基线能够改善困惑度，而反向分配则会造成损害。基于这一结果，我们引入了锥形语言模型（TLM），这是一种架构原则，其中承载参数的组件在固定总预算下沿深度单调地逐渐变细。多层感知机是这种实例化的自然场所：它们在现代语言模型家族中占主导的参数数量，并将宽度暴露为单一、清晰的变异轴。在三个模型规模和四种架构（Transformer、门控注意力、Hope-attention和Titans）中，通过平滑的余弦调度来逐渐减小多层感知机宽度，在无额外参数或计算成本的情况下，始终优于均匀基线的困惑度和下游基准性能。这些发现建立了深度感知容量分配作为语言模型设计中简单、与架构无关的轴，是隐藏在视线之外的免费杠杆。

## Abstract
Modern language models, including transformer, recurrent, and memory-based variants, share a common chassis: a stack of identical layers in which parameters are allocated uniformly across depth. This is a default inherited from the original transformer and largely unchanged since, yet a growing body of evidence suggests that layers contribute non-uniformly to the final output, with later layers refining the residual stream rather than transforming it. We ask whether parameter capacity should reflect this asymmetry. Our controlled experiment shows that, under a fixed budget, allocating more capacity to earlier layers and less to later layers improves perplexity over a uniform-width baseline, while the reverse allocation hurts. Building on this result, we introduce Tapered Language Models (TLMs), an architectural principle in which a parameter-bearing component is monotonically tapered across depth under a fixed total budget. MLPs are the natural site for this instantiation: they dominate parameter count across all modern LM families and expose width as a single, clean axis of variation. Across three model scales and four architectures (Transformer, Gated Attention, Hope-attention, and Titans), tapering MLP width via a smooth cosine schedule consistently improves perplexity and downstream benchmark performance over uniform baselines, at no additional parameter or compute cost. These findings establish depth-aware capacity allocation as a simple, architecture-agnostic axis of language model design, a free lever hidden in plain sight.

---

## 论文详细总结（自动生成）

# 论文总结：Tapered Language Models（锥形语言模型）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代语言模型（Transformer、门控注意力、循环状态空间模型、记忆增强模型等）尽管在 token 混合模块上各不相同，但都共享一个默认架构：由 L 层相同的、参数均匀分布的块堆叠而成。然而，越来越多的证据（早期退出、层跳过、结构冗余分析、可解释性研究等）表明各层对最终输出的贡献是非均匀的——较后的层更多是细化残差流而非变换它。论文的核心问题就是：**如果各层重要性非均匀，为什么各层的参数容量还要保持均匀？**
- **研究动机**：通过简单的受控实验（将 440M Transformer 的层分为三组，分配不同 MLP 宽度但保持总参数固定）发现：将容量集中在前层（wider-early）可将困惑度从均匀基线的 16.28 降至 15.96，而反向分配（wider-late）则使困惑度升至 17.29，说明**容量分配的方向至关重要**。这启发作者系统性地探究如何在固定预算下非均匀分配容量，从而提出锥形语言模型（TLM）这一架构原则。
- **整体含义**：TLM 表明“深度感知的容量分配”是一个简单、与架构无关且免费的（不增加参数和计算量）语言模型设计杠杆，可以广泛适用于多种现代语言模型家族，甚至可能推广到视觉 transformer、扩散 transformer 等基础模型。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
在固定总参数预算下，将承载参数的组件的宽度沿深度单调递减（即“锥形”），使前层容量更大、后层容量更小。论文聚焦于 MLP 宽度（中间维度 \(d_{\text{ff}}\)），因为 MLP 占语言模型绝大多数参数，且宽度是单一、清晰的变异轴。

### 关键技术细节
- **形式化定义**：设某组件的每层维度为 \(d_C(l)\)，均匀设计固定 \(d_C(l) = d_C^{\text{baseline}}\)；锥形设计要求 \(d_C(l+1) \le d_C(l)\) 且 \(\frac{1}{L}\sum_{l=0}^{L-1} d_C(l) = d_C^{\text{baseline}}\)，即维度单调递减且平均值等于基线。
- **MLP 宽度锥形化**：用平滑衰减调度替换常数 \(d_{\text{ff}}\)，本文试验三种调度（图 3）：
  - **线性**：\(d_{\text{ff}}(l) = d_{\text{start}} - (d_{\text{start}} - d_{\text{end}})\cdot \frac{l}{L-1}\)
  - **余弦**：\(d_{\text{ff}}(l) = d_{\text{end}} + \frac{d_{\text{start}} - d_{\text{end}}}{2}\left(1 + \cos\frac{\pi l}{L-1}\right)\)
  - **Sigmoid**：\(d_{\text{ff}}(l) = d_{\text{end}} + \frac{d_{\text{start}} - d_{\text{end}}}{1 + e^{k\left(\frac{l}{L-1} - 0.5\right)}}, k=10\)
- **参数保持**：选择 \(d_{\text{start}}\) 和 \(d_{\text{end}}\) 使平均宽度等于基线 \(d_{\text{ff}}^{\text{baseline}}\)，从而总参数量和 FLOPs 保持不变。每层宽度四舍五入到 16 的倍数以保证硬件效率，同时保持单调递减。
- **仅修改 MLP 中间维度**，其他超参数（残差流维度、注意力头数等）与均匀基线相同。

### 算法流程（文字描述）
1. 确定基线模型（如 \(d_{\text{ff}}=4d\) 或 \(8/3 d\) 的均匀 MLP）。
2. 选择调度（余弦）和目标宽比（如 \(d_{\text{start}}/d_{\text{end}}=1.5/0.5\)）。
3. 根据调度计算每层 \(d_{\text{ff}}(l)\)，并调整以满足平均宽度约束。
4. 用这些每层宽度替换均匀 MLP 宽度，保持其他模块不变。
5. 用相同训练超参数从头预训练锥形模型和均匀基线模型，比较性能。

## 3. 实验设计：数据集 / 场景、基准、对比方法

### 数据集和评估基准
- **训练数据**：使用 Llama 3 tokenizer（词表 32K），序列长度 4K tokens。模型规模：440M（30B tokens）、760M（50B tokens）、1.3B（100B tokens）。
- **验证/测试**：
  - 在调度和宽度选择阶段使用**训练集内验证集**的困惑度。
  - 主要结果使用**分布外**的 WikiText 和 LAMBADA 困惑度，以及 8 个常识推理基准的准确率（LAMBADA、PIQA、HellaSwag、WinoGrande、ARC-easy、ARC-challenge、SIQA、BoolQ）。
  - 附录中还有长上下文检索任务（Needle-in-a-Haystack 多种变体：S-NIAH-1/2/3 和 MQ-NIAH，上下文长度 4K/8K/16K）。

### 对比方法
- **均匀基线**：所有层 MLP 宽度相同，总参数和 FLOPs 与锥形模型相等。
- **调度和宽度比扫查**：5 种宽比 × 3 种调度（余弦、线性、sigmoid），共 15 个配置。
- **四种架构**：Transformer++、Gated Attention、Hope-attention、Titans，各模型规模分别训练均匀和锥形版本。
- **实验设置公平性**：总参数量、训练 FLOPs、推理 FLOPs 均固定；训练超参数（优化器、学习率、weight decay、batch size）完全相同。

## 4. 资源与算力

论文中未明确说明 GPU 型号、数量及训练时长。只提到“训练在加速器硬件上进行”，没有给出具体硬件信息和耗时。因此无法评估具体算力消耗。

## 5. 实验数量与充分性

- **调度和宽度扫查**：在 440M Transformer 上进行了 5×3=15 组实验，覆盖五种宽比和三种调度。
- **主要结果**：在 760M 和 1.3B 两个规模下，对四种架构分别训练均匀和锥形，共 2×4×2=16 个模型，报告了困惑度和 8 个下游任务准确率。
- **长上下文检索**：在 1.3B 模型上对四种架构进行 NIAH 评估，4 种变体 × 3 种长度，共 4×4×3=48 个指标报告。
- **消融/分析**：层对齐实验（Figure 4）在 GPT-2 家族（124M~1.5B）上验证了 MLP 输出与残差流余弦相似度随深度增加而上升，提供了机制解释。
- **充分性评价**：
  - **充分**：实验覆盖了多种架构、多种规模、多种调度和宽度比、多种评估维度（困惑度、常识推理、长上下文检索），且严格控制了参数和计算量的公平性。
  - **客观**：与均匀基线对比，没有引入其他额外技巧；采用固定配置跨架构迁移，避免了调参偏倚。
  - **不足**：调度和宽度扫查仅在 440M Transformer 上做，未在更大模型或其他架构上验证最优配置；缺乏更广泛的缩放律分析（如是否最优宽比随深度/隐层维数变化）。此外，仅对 MLP 宽度锥形化，未探索注意力头数等其他维度。

## 6. 论文的主要结论与发现

1. **锥形化 MLP 宽度显著改善性能**：在 440M Transformer 上，余弦调度 + 宽比 1.5/0.5 使验证困惑度从 16.28 降至 14.44（改善 1.84 点），且无需额外参数或计算。
2. **跨架构和规模一致有效**：在 Transformer、Gated Attention、Hope-attention、Titans 四种架构上，760M 和 1.3B 规模下，锥形模型在所有报告指标中均优于或持平均匀基线（尤其下游常识推理平均准确率持续提升）。
3. **容量前重后轻优于其他分配模式**：图 2 的三组实验直观显示，早期层更多容量、后期层更少容量效果最好，反向分配和核中分配都明显更差。
4. **机制解释**：层对齐分析（Figure 4）表明，随深度增加，MLP 输出与输入残差流的余弦相似度显著上升，意味着深层 MLP 主要强化已有表示而非提供新信息。锥形化将容量从信息冗余的深层转移到信息多样化的浅层，从而提升效率。
5. **长上下文能力不受损**：附录中的 Needle-in-a-Haystack 结果显示，锥形模型在长上下文检索上匹配或优于均匀基线，说明容量重新分配不损害长距离依赖处理能力。

## 7. 优点

- **方法简洁、架构无关**：仅通过修改 MLP 宽度沿深度衰减，即可在零额外成本下获得性能提升，设计原则适用于多种后续架构，不需要改变训练流程或模型其他部分。
- **实验设计科学、公平**：严格控制总参数量和 FLOPs 相等；所有超参数保持相同；调度和宽比扫查独立于主要结果，避免了过拟合特定配置。
- **机制分析有说服力**：通过余弦相似度量化层更新与残差流的对齐程度，直接支持了容量应该前重后轻的直觉，而非仅仅报告性能提升。
- **覆盖范围广**：测试了四种截然不同的 token 混合架构、三个模型规模、多种评估维度（困惑度、常识推理、长上下文检索），结果一致，增强了结论的稳健性。
- **识别了被忽视的设计杠杆**：指出“均匀深度分配”是默认但非最优的，锥形化打开了新的设计空间。

## 8. 不足与局限

- **调度和宽比扫查仅限单一条件**：最佳调度（余弦）和宽比（1.5/0.5）仅在 440M Transformer 上确定，然后直接迁移到其他规模和架构。虽然这证明了鲁棒性，但未能评估是否每个架构/规模需要不同的最优配置，因此报告的性能提升可能只是下界，而非上界。
- **仅锥形化 MLP 宽度**：论文指出其他维度（注意力头数、key-value 维度、循环状态大小等）也是候选，但未做实验。锥形化原理的普适性有待进一步验证。
- **未讨论与 MoE、层共享等正交方法的交互**：锥形化可以与此类方法结合，但未提供组合实验。
- **算力信息缺失**：未报告 GPU 型号、数量、训练时间，不利于他人重现或评估计算开销。
- **长上下文评估较有限**：仅测试了 16K 长度，若模型处理更长上下文（如 128K 以上）时锥形化的影响未知。
- **理论分析较浅**：除对齐度测量外，没有更深入的理论解释（如为什么早期层更“有效”），也未分析锥形化对训练动态（如梯度传播）的影响。

（完）
