---
title: "BabelJudge: Measuring LLM-as-a-Judge Reliability Across Languages and Agent Trajectories"
title_zh: BabelJudge：跨语言和智能体轨迹的LLM作为评判者的可靠性度量
authors: Shreyas KC
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22329v1"
tags: ["query:llm-eval"]
score: 9.0
evidence: LLM作为评判者的可靠性基准与审计框架
tldr: LLM-as-a-judge评估存在位置、冗长、顺序不一致和跨语言退化等系统偏见，原始准确率无法揭示。BabelJudge框架通过可控退化生成有金标签的配对样本，无需人工标注即可审计这些偏见。对Qwen2.5模型在英、印地、阿拉伯、斯瓦希里语评估显示，复合可靠性分数从印地语0.714降至斯瓦希里语0.550，且斯瓦希里语顺序一致性仅0.480，接近随机。框架还扩展至智能体轨迹评估，涵盖工具准确性、幻觉检测等指标。BabelJudge作为Python包发布，支持11种后端。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22329v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1495, \"height\": 605}, {\"url\": \"assets/figures/arxiv/2606.22329v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1556, \"height\": 792}, {\"url\": \"assets/figures/arxiv/2606.22329v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 764, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2606.22329v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 766, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2606.22329v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 764, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2606.22329v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 767, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2606.22329v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1692, \"height\": 517}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22329v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1646, \"height\": 324}, {\"url\": \"assets/tables/arxiv/2606.22329v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1467, \"height\": 349}, {\"url\": \"assets/tables/arxiv/2606.22329v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1239, \"height\": 630}, {\"url\": \"assets/tables/arxiv/2606.22329v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1617, \"height\": 112}]"
motivation: 原始准确率掩盖了LLM评判者的系统性偏见，尤其低资源语言退化严重，缺乏无标注的可靠性审计工具。
method: 通过退化打标构建已知金标签的配对项，定义位置、冗长、顺序不一致和跨语言退化四个失败模式，并设计复合可靠性分数。
result: Qwen2.5-7B-Instruct-4bit可靠性分数从印地语0.714降至斯瓦希里语0.550，顺序一致性低至0.480；原始准确率差距被低估。
conclusion: BabelJudge实现了可扩展的LLM评判可靠性审计，暴露了准确率隐藏的失败模式，并支持多语言和智能体评估。
---

## 摘要
LLM作为评判者已成为NLP流程中可扩展评估的主导方法，但评判者本身存在系统性的偏差，而原始准确率掩盖了这些偏差：它们偏向于放在A槽位的回答（位置偏差），无论质量如何都偏好更长的回答（冗长偏差），并且在低资源语言中其可靠性急剧下降。我们提出了BabelJudge，一个开源基准测试和可靠性审计框架，可以在任何评判模型上测量所有四种失效模式——位置偏差、冗长偏差、顺序不一致性和跨语言退化，而无需人工偏好标签。其关键思路是通过退化进行金标签标注：从一个高质量的参考回答出发，施加一个受控的扰动，得到一个配对项，其金标签通过构造已知，从而消除了标注成本。我们在英语、印地语、阿拉伯语和斯瓦希里语上评估了Qwen2.5-7B-Instruct-4bit，发现我们的复合偏差惩罚可靠性评分从印地语的0.714下降到斯瓦希里语的0.550，而原始准确率（0.835 vs. 0.660）低估了这一差距。斯瓦希里语的顺序一致性骤降至0.480，意味着在槽位顺序交换下评判结果近乎随机——这是仅靠准确率无法发现的失效模式。我们进一步将该框架扩展到智能体评估，通过九种轨迹级别的扰动（参数破坏、工具交换、幻觉调用、缺失步骤）和三个新指标：工具准确率、幻觉检测率和轨迹长度偏差。BabelJudge作为Python包发布，支持11种评判后端。代码：https://github.com/Shreyaskc/BabelJudge

## Abstract
LLM-as-a-judge has become the dominant approach to scalable evaluation in NLP pipelines, yet judges themselves carry systematic biases that raw accuracy hides: they favor responses placed in slot A (position bias), they prefer longer responses regardless of quality (verbosity bias), and their reliability degrades sharply in lower-resource languages. We introduce BabelJudge, an open-source benchmark and reliability audit framework that measures all four failure modes -- position bias, verbosity bias, order inconsistency, and cross-lingual degradation -- on any judge model, without requiring human preference labels. The key insight is gold-labelling by degradation: starting from a high-quality reference response and applying a controlled perturbation yields a pairwise item whose gold label is known by construction, eliminating annotation cost. We evaluate Qwen2.5-7B-Instruct-4bit across English, Hindi, Arabic, and Swahili and find that our composite bias-penalised reliability score drops from 0.714 in Hindi to 0.550 in Swahili, a gap that raw accuracy (0.835 vs. 0.660) understates. Swahili order consistency collapses to 0.480, meaning judge verdicts are near-random under slot-order swaps -- a failure mode invisible to accuracy alone. We further extend the framework to agentic evaluation via nine trajectory-level perturbations (argument corruption, tool swaps, hallucinated calls, missing steps) and three new metrics: tool accuracy, hallucination detection rate, and trajectory-length bias. BabelJudge is released as a Python package supporting 11 judge backends. Code: https://github.com/Shreyaskc/BabelJudge

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：LLM-as-a-judge（以大语言模型作为评判者）评估范式被广泛用于自动化质量评估（如MT-Bench、RLHF偏好数据收集），但评判者本身存在系统性偏差，比如位置偏差、冗长偏差、自我增强偏差以及跨语言退化。已有零星研究分别考察这些问题，但缺乏统一的可重复审计工具。
- **核心问题**：如何在不依赖人工标注的前提下，系统度量任意LLM评判模型的四个关键失效模式（位置偏差、冗长偏差、顺序不一致性、跨语言退化），并给出一个综合的可靠性分数，以便在部署前诊断评判模型是否可靠。
- **整体含义**：论文揭示了“原始准确率”会显著高估评判模型的实际可靠性，尤其在低资源语言（如斯瓦希里语）中，顺序不一致性接近随机水平，导致评估结果噪声主导。BabelJudge为社区提供了一套标准化、免标注的审计框架，有助于提升LLM评判的透明度与可信度。

---

### 2. 论文提出的方法论

- **核心思想**：**金标签通过退化构造（Gold-Labelling by Degradation）**  
  - 从高质量参考响应`r`出发，施加受控的扰动函数`perturb_t(·)`，得到退化响应`p`。由于扰动保证严格降低质量，因此`r`优于`p`这一金标签由构造过程直接确定，无需人工标注。
  - 对每个扰动类型`t`，构建一个评判项目`(q, r, p, gold=r)`，并以两种槽位顺序呈现给评判模型（即`(r,p)`和`(p,r)`），从而同时探测位置偏差和顺序一致性。

- **关键技术细节**：
  1. **五种扰动类型**（语言无关，基于空格、句界、数字等）：
     - `truncate`：截断后部句子 → 信息丢失容忍度
     - `shuffle`：随机重排句子 → 连贯性敏感性
     - `number corrupt`：随机扰动数字 → 事实准确性检测
     - `drop entities`：移除大写/实体类词 → 实体完整性
     - `repeat pad`：追加冗余句子（**冗长性探针**，使退化响应更长，如果评判偏好长答案则暴露冗长偏差）
  2. **复合可靠性分数**`R`（公式见论文§3.4）：
     - 基础分：`0.6×acc + 0.4×κ_ord`（准确率与顺序一致性的加权平均）
     - 乘性惩罚：`(1 - 0.8·|Δ_pos|)`（位置偏差惩罚）、`(1 - 0.7·max(0, 2v-1))`（冗长偏差惩罚，`v`为冗长探针上的偏好率）
     - 乘性结构设计使偏差被严重惩罚，突出偏差对可靠性的实际危害。
  3. **智能体轨迹扩展**：
     - 定义九种轨迹级扰动（参数破坏、工具名交换、幻觉调用、缺失必需参数、额外虚假参数、删除中间步骤、工具结果篡改、提前终止、步骤填充——冗长探针）。
     - 新增指标：工具准确率、参数准确率、幻觉检测率、轨迹长度偏差。
     - 支持两种序列化模式：`focus="tool calls"`（紧凑调用序列）和`focus="full trace"`（含思维过程的全轨迹）。

---

### 3. 实验设计

- **数据集/场景**：
  - 参考响应来自**Aya数据集**（Apache-2.0，支持65+语言），每个语言采样10条参考响应。
  - 评测语言：英语(en)、印地语(hi)、阿拉伯语(ar)、斯瓦希里语(sw)。
  - 每种语言应用全部5种扰动，每个扰动2种严重程度（0.3和0.6），每个项目2种顺序，共：10 refs × 5 perturb × 2 severities × 2 orders = 200 judge calls/语言，总计800次。
  
- **基准（Benchmark）**：论文本身提出了一个基准：**BabelJudge**，用于审计评判模型。没有对比其他方法，因为它是首款统一审计工具，但相关工作引用了MT-Bench、Chatbot Arena、METaL等。
- **对比方法**：论文未对比其他审计框架（因为没有直接可比工作），而是重点展示了在同一模型上不同语言的可靠性对比，以及原始准确率与复合分数之间的差距。

---

### 4. 资源与算力

- **模型**：`mlx-community/Qwen2.5-7B-Instruct-4bit`，4-bit量化版，在Apple Silicon（M系列芯片）上本地运行，使用MLX框架。
- **算力**：未明确给出GPU型号、数量或训练时长。所有评判调用都在一台Apple M系列机器上本地完成，无需外部API。整个实验规模较小（800次评判），因此算力需求很低。

---

### 5. 实验数量与充分性

- **实验数量**：仅针对一个评判模型（Qwen2.5-7B-4bit）在四种语言上完成5类扰动×2严重度×2顺序的测试，总共800次评判。对于智能体扩展，论文列出九种扰动，但**未报告具体实验数据**（仅给出方法定义）。
- **充分性评估**：
  - **充分之处**：各扰动类型覆盖了多种失效模式，双向顺序设计可分离位置偏差与一致性，且跨语言对比能揭示退化差异。无需人工标注，可快速重复。
  - **不足**：
    - 每个语言仅10条参考响应（200次评判），样本量偏小，作者自己也承认不足以做稳定模型选择。
    - 仅测试一个模型，缺乏多模型对比（作者标为未来工作）。
    - 智能体部分仅给出框架，无实际实验结果，验证不充分。
    - 未做消融实验（如不同惩罚权重的影响）。

总体而言，实验设计合理但初步，足以证明方法有效性但不足以得出普适结论。

---

### 6. 论文的主要结论与发现

1. **原始准确率显著高估可靠性**：宏平均准确率0.770，但宏平均可靠性仅为0.614（按语言平均），平均偏差15.6个百分点。
2. **斯瓦希里语可靠性崩溃**：可靠性分数低至0.550，远低于阈值0.65；顺序一致性仅0.480，说明槽位交换下评判结果近乎随机。准确率（0.660）虽低但未完全揭示这一失效。
3. **位置偏差在可控范围内**：所有语言|Δ_pos|均小于0.10，模型可能经过一定去偏处理。
4. **冗长偏差具有语言依赖性**：英语仅0.075，印地语升至0.250，表明评判模型对不同脚本的校准差异。
5. **建议生产部署阈值**：R≥0.65；斯瓦希里语不合格，其他语言勉强通过。
6. **智能体框架可行**：通过九种扰动可以构造已知金标签的轨迹对，并扩展工具准确率、幻觉检测等新指标（但缺少实验证据）。

---

### 7. 优点

- **免标注设计**：通过退化构造金标签，完全避免昂贵的人工偏好标注，可快速、大规模审计任何评判模型。
- **多维失效探测**：同时覆盖位置偏差、冗长偏差、顺序不一致性、跨语言退化，并提供复合分数，便于直观比较。
- **可扩展性强**：支持多种后端（本地/云API/OpenAI兼容），并容易扩展到新语言、新扰动类型。
- **智能体评估扩展**：将审计思想延伸到工具调用轨迹，填补了该领域空白。
- **开源工具化**：发布为Python包，提供标准化接口，有利于社区复现和共建排行榜。

---

### 8. 不足与局限

- **样本量过小**：每个语言仅10条参考响应，统计稳定性差；作者也指出未来需扩展到100+条。
- **仅一个评判模型**：不能代表其他模型的表现，未提供多模型排行榜（仅规划为未来工作）。
- **语言覆盖有限**：仅4种语言，但框架理论上支持200+语言（FLORES-200）。
- **扰动覆盖不够细致**：扰动是保守的（保证退化），但忽略了风格转移、软事实错误、近似流畅幻觉等更微妙退化，可能低估某些失效模式。
- **智能体部分缺乏实验数据**：仅定义了扰动和指标，未给出任何实验结果，无法验证其有效性。
- **缺乏消融与敏感性分析**：未论证惩罚权重（0.8、0.7等）设置的合理性，也未比较不同权重对排序的影响。
- **未对比人类评判或已有度量**：论文明确表示本研究不关注与人类一致性，而是内部一致性与无偏性；但这也限制了在真实应用场景中的意义验证。

（完）
