---
title: An expressivity analysis of hierarchical modelling in deep transformers via bounded-depth grammars
title_zh: 基于有界深度语法对深度Transformer中层次建模的表达性分析
authors: "Vinoth Nandakumar, Qiang Qu, Pramod Thebe, Sakshi Khachariya, Tongliang Liu"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17522v1"
tags: ["query:llm-lang-rep"]
score: 9.0
evidence: transformer层次表示的理论分析
tldr: 深度transformer被认为具有层次化表示能力，但缺乏严格理论分析。本文通过有界深度非递归上下文无关文法，证明深度transformer能编码抽象语法状态到低维线性子空间。构造的位置注意力transformer深度随文法深度线性增长，神经元数量与推导树形状和产生式规则相关。理论支持线性表示假说，揭示层次建模的表达力机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17522v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1592, \"height\": 244, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17522v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1701, \"height\": 591, \"label\": \"Table\"}]"
motivation: 缺乏严格理论解释深度transformer如何形成层次化表示，本文通过形式文法建模分析其表达能力。
method: 使用有界深度非递归上下文无关文法，显式构造位置注意力transformer，深度与文法深度线性增长，神经元规模与推导树形状和产生式规则相关。
result: 证明此类架构能将抽象语法状态编码到低维线性子空间，支持线性表示假说。
conclusion: 为深度transformer的层次化表示提供了理论支撑，推动对语言模型内部表征的理解。
---

## 摘要
深度神经网络被广泛认为其表达力源于形成层次表示的能力，在层间捕获越来越抽象和组合的特征。在语言建模中，Transformer已成为主导架构，早期层捕获局部句法模式，后期层编码更复杂的从句级依赖。尽管这种直觉塑造了模型设计，但仍缺乏严格的理论工作来展示深度Transformer如何表示这种层次结构。在这项工作中，我们通过有界深度、非递归上下文无关语法的形式化视角分析深度Transformer模型的表达性。针对这类语法，我们显式构造了具有位置注意力的Transformer，其深度随语法深度线性增长，而神经元数量随派生树形状数量以及随产生式规则数量的二次方增长。我们的理论结果通过证明这些架构具有将抽象语法状态编码为残差流中低维、线性可分子空间的结构能力，支持了线性表示假说。

## Abstract
Deep neural networks are widely believed to derive their expressive power from their ability to form \textbf{hierarchical representations}, capturing progressively more abstract and compositional features across layers. In language modeling, \textbf{transformers} have emerged as the dominant architecture, with early layers capturing local syntactic patterns and later layers encoding more complex clause-level dependencies. While this intuition has shaped model design, there remains a lack of rigorous theoretical work demonstrating \textbf{how} deep transformers represent such hierarchical structures. In this work, we analyze the expressiveness of deep transformer models through the formal lens of bounded-depth, non-recursive context-free grammars. For this class of grammars, we explicitly construct transformers with positional attention whose depth grows linearly with grammar depth, while the neuron count scales with the number of derivation-tree shapes and quadratically with the number of production rules. Our theoretical results support the linear representation hypothesis by demonstrating that these architectures possess the structural capacity to encode abstract grammatical states into low-dimensional, linearly separable subspaces within the residual stream.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：深度Transformer在语言建模中表现出强大的层次化表示能力，早期层捕获局部句法，后期层捕获更抽象的依赖，但缺乏严格的理论分析证明其如何表示层次结构。
- **整体含义**：本文试图用形式语言理论（有界深度、非递归的上下文无关文法CFG）为桥梁，证明深度Transformer具备编码层次结构的能力，并支持“线性表示假说”（即抽象语法状态可编码为低维线性子空间中的向量）。这项工作增强了人们对Transformer表达能力上限及其内在机制的理论理解。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：对一类均匀、非递归、固定深度\(d\)的CFG，显式构造一个位置注意力Transformer，其深度与语法深度\(d\)线性增长，神经元数量与推导树形状数\(c\)线性相关、与产生式规则数\(M\)二次方相关。
- **关键技术细节**：
  - 定义“长度均匀、固定深度”的CFG，所有派生树的根到叶路径长度恒为\(d\)，无递归，以保证层次结构明确。
  - **位置注意力**：使用简化版相对位置编码（基于Tay et al. 2020的随机合成器构造），每个注意力头只关注与特定派生树形状对应的子序列（稀疏注意力），不使用softmax，而是硬编码的注意力权重。
  - **层次化自底向上解析**：第一层Transformer块处理叶子节点（终端符号），第\(i\)层块聚合第\(i\)层子节点信息并输出父节点非终端符号，即实现动态规划的逐步归约。
  - **前馈网络（FFN）作为查找表**：利用Zhang et al. (2017)的记忆化定理，用单隐藏层ReLU网络精确记忆有限个输入-输出映射，从而将子节点序列映射为父节点符号。
  - **最后一个词预测**：在Transformer块之后堆叠\(5d+7\)个前馈层，通过归纳计算每个非终端符号的合法后续集，最终线性映射到词汇表 logits，再经 softmax 得到预测概率。
- **没有公式或算法流程的详细数值推导，文中以构造性证明为主**。

## 3. 实验设计
- **本文为纯理论论文，没有实验**。未使用任何数据集、基准测试或对比方法。所有结论基于数学证明，而非经验验证。

## 4. 资源与算力
- **文中未提及任何计算资源（GPU型号、数量、训练时长等）**。由于是理论工作，不需要实际训练或推理。

## 5. 实验数量与充分性
- **无实验**。因此无法讨论实验数量、消融实验或公平性。
- **充分性**：从理论角度，论文给出了完整的构造性证明和数学推导，证明了“存在性”，但缺乏实证研究验证其结论在真实训练模型中的出现。作者在讨论部分也承认了这一点，并指出未来可通过机械可解释性工具（如ACDC、EAP）来验证训练后模型是否学出类似结构。

## 6. 论文的主要结论与发现
1. **存在性定理（Theorem 4.1）**：对于任何给定的满足约束的CFG，总存在一个具有位置注意力机制的Transformer，能够以任意小误差\(\epsilon\)完成下一个词预测，且模型深度等于语法深度\(d\)，每层神经元数量为\(O(c M^2)\)，其中\(c\)是推导树形状数，\(M\)是最大规则数。
2. **线性表示假说的理论支持**：论文证明了Transformer架构有能力将不同派生树形状下的非终端符号编码到残差流中不同的低维正交子空间，这些子空间是线性可分的（尽管实际模型中是通过叠加实现的）。
3. **层次深度对应**：每层Transformer对应语法的一层，实现了自底向上的动态规划解析。
4. **与现有工作的关系**：与Zhao et al. (2023)相比，本文的层数随语法深度线性增长而非句子长度，更加高效；与Liu et al. (2023)的有限状态自动机构造相比，避免了将CFG展开为指数级状态数。

## 7. 优点
- **理论贡献突出**：首次从形式语言角度系统分析了深度Transformer的层次表达能力，给出了严格的显式构造，填补了理论空白。
- **连接实证发现与理论**：支持了Allen-Zhu & Li (2025)和Saglam et al. (2025)的线性表示假说，为理解Transformer内部机制提供了数学基础。
- **可扩展性**：构造中使用了稀疏注意力、相对位置编码等实际常用技术，为未来分析实际Transformer提供了理论视角。
- **界清晰**：给出了层数、神经元数、注意力头数等与语法复杂度的明确关系，具有指导意义。

## 8. 不足与局限
- **假设过于理想化**：仅针对非递归、固定深度的CFG，而自然语言包含递归和可变深度，因此结论不能直接推广到现实语言。
- **简化位置注意力**：去掉了softmax，直接使用硬编码的相对位置权重，与标准Transformer中的自注意力有差距，证明在标准softmax注意力下需要额外假设（如位置嵌入设计）。
- **构造依赖记忆化**：前馈网络作为查找表需要精确记忆所有可能的输入-输出对，但实际训练中模型通过梯度下降学习，可能无法学出这种精确映射。
- **未能解释泛化**：论文仅证明存在性，未分析训练过程中模型是否真的能发现该结构，或该结构是否唯一。
- **缺乏实验验证**：全部为理论推导，没有在真实或合成数据上验证构造的有效性，也未与基线模型比较。
- **复杂度上界可能不紧**：论文中的神经元数量依赖于推导树形状数\(c\)，对于复杂语法的\(c\)可能很大，实际意义有限。

（完）
