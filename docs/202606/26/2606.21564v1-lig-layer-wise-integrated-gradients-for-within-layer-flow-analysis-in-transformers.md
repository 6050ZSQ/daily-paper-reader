---
title: "LIG: Layer-wise Integrated Gradients for Within-Layer Flow Analysis in Transformers"
title_zh: LIG：用于Transformer层内流分析的逐层积分梯度
authors: "Eight Suzuki, Hideitsu Hino, Noboru Murata"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21564v1"
tags: ["query:llm-lang-rep"]
score: 9.0
evidence: 用于分析Transformer层内token表示的可解释性方法
tldr: 针对Transformer内部计算不透明的问题，提出LIG方法，将每层视为动态图，在ATT和MLP等非线性模块边界应用集到集集成梯度（Set-to-set IG），通过L2标量化扩展传统IG，并借鉴LRP的保守性进行逐层归因。在BERT-base和PTB上实验，发现以目标token嵌入作为ATT基线、零向量作为MLP基线时层内一致性最佳。LIG作为模块边界粒度的诊断工具，无需重新训练或设计逐操作解释器。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21564v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1000, \"height\": 453, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21564v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 824, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21564v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 624, \"height\": 498, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21564v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 754, \"height\": 515, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21564v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1040, \"height\": 523, \"label\": \"Table\"}]"
motivation: 现有归因方法难以分析Transformer层内信息流，且缺乏模块边界粒度的解释工具。
method: 提出LIG，在每层ATT和MLP边界应用集到集集成梯度，通过L2标量化扩展IG，并组合模块贡献实现层内归因。
result: 在BERT-base和PTB上，目标token嵌入作为ATT基线、零向量作为MLP基线时，层内归因一致性最优。
conclusion: LIG作为模块边界粒度的XAI工具，可无模型特定重训练地分析Transformer层内流动。
---

## 摘要
Transformer模型表现强劲，但其内部计算仍不透明。我们将每个Transformer层视为一个动态图，其节点是词元表示和每头注意力输出，以多头注意力（ATT）和MLP作为模块边界。在此图上，我们使用LIG（逐层积分梯度），它在非线性模块边界应用集到集积分梯度（IG）。集到集IG将IG应用于从一组输入词元表示到一组输出表示的映射，评估词元到词元的贡献，这在先前的IG应用中并不标准。这通过L2标量化的方式将IG从通常的标量目标设定扩展到集到集映射，并按照逐层相关性传播（LRP）的精神组合层内贡献，IG完备性在每个边界处扮演LRP风格守恒的角色。我们使用LIG分析：(i) 在L2准则下，模块级组合与整层归因之间的一致性；(ii) 通过分离ATT和MLP贡献来分析层内信息流。在BERT-base和PTB上，最能保持层内一致性的配置是使用目标词元的嵌入作为ATT基线，并使用a=0时的ATT输出或零作为MLP基线。因此，我们将LIG作为一种在模块边界粒度上的诊断性XAI工具，无需特定模型重新训练或操作级解释器设计。代码见https://github.com/eightsuzuki/layer-wise-integrated-gradients。

## Abstract
Transformers achieve strong performance, but their internal computations remain opaque. We view each Transformer layer as a dynamic graph whose nodes are token representations and per-head attention outputs, with Multi-Head Attention (ATT) and MLP as module boundaries. On this graph we use LIG (Layer-wise Integrated Gradients), which applies set-to-set Integrated Gradients (IG) at nonlinear module boundaries. Set-to-set IG applies IG to a map from a set of input token representations to a set of output representations, evaluating token-to-token contributions, which is not standard in prior IG applications. This extends IG from the usual scalar-objective setting to set-to-set maps via an L2 scalarization, and composes within-layer contributions in the spirit of Layer-wise Relevance Propagation (LRP), with IG completeness playing the role of LRP-style conservation at each boundary. We use LIG to analyze (i) the agreement between module-wise composition and layer-whole attribution under an L2 criterion, and (ii) within-layer information flow by tracing separated ATT and MLP contributions. On BERT-base and PTB, configurations that best preserved within-layer consistency used the target token's embedding as the ATT baseline and either the ATT output at a=0 or Zero as the MLP baseline. We therefore present LIG as a diagnostic XAI tool at module-boundary granularity, without model-specific retraining or per-operation interpreter design. Code is available at https://github.com/eightsuzuki/layer-wise-integrated-gradients.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：Transformer模型（如BERT、GPT）性能强大，但其内部计算过程高度不透明，存在“黑盒”问题。现有的可解释性方法（如注意力图、SHAP、LRP）大多聚焦于输入到输出的整体归因，或需要设计特定操作的解释规则，难以直接分析**层内信息流**——即同一层内多头注意力（ATT）与MLP之间如何分离并组合贡献，以及各模块贡献与整层归因之间的一致性。
- **整体含义**：论文试图在模块边界（ATT和MLP）粒度上，提供一种无需重新训练或定制解释器的诊断工具，从而揭示Transformer层内token表示之间的贡献流动，并验证模块级分解是否能忠实反映整层行为。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  - 将每个Transformer层视为一个动态图，节点为token表示和每头注意力输出，以ATT和MLP作为模块边界。
  - 在模块边界应用**集到集积分梯度（Set-to-set IG）**：并非传统IG针对单个标量输出，而是将输入token表示集合映射到输出token表示集合，通过**L2标量化**（即输出向量与参考点的L2范数）将多输出归约为标量，从而评估token-to-token贡献。
  - 借鉴**逐层相关性传播（LRP）**的守恒精神：IG在每个模块边界上的完备性（输入贡献之和等于输出变化）充当LRP中的守恒关系，使得模块内/模块间的贡献可以逐层传播和组合。
- **关键技术细节**：
  - **ATT贡献计算**（式6）：基于L2标量化 \(A_j(a) = \|u_j(a) - u_j(0)\|_2\)，对输入token \(z_i\) 的每个维度积分梯度，得到 \(IG^{\text{ATT}}_{i,j,h}\)，再归一化为 \( \tilde{f}^{\text{ATT}}_{i,j,h} \)。
  - **MLP贡献计算**（式8）：类似地，对注意力输出 \(u^{(l,h)}_j\) 积分得到 \(IG^{\text{MLP}}_{h,j}\)，归一化为 \(\tilde{f}^{\text{MLP}}_{h,j}\)。
  - **整层贡献计算**（式9）：直接对整层映射 \(z^{(l)} \to z^{(l+1)}\) 应用IG，得到 \(\tilde{f}^{\text{LAYER}}_{i,j}\)。
  - **路径组合**（式11）：\(c^{\text{ATT} \circ \text{MLP}}_{i,j} = \sum_{h} \tilde{f}^{\text{ATT}}_{i,j,h} \cdot \tilde{f}^{\text{MLP}}_{h,j}\)，理论上应与 \(\tilde{f}^{\text{LAYER}}_{i,j}\) 一致，从而衡量分解一致性。
  - **基线设计**：探索了多种基线（Zero向量、Input Token Base ITB、ATTITB\(_{a=0}\)等），以及自我项补全策略（zeroRatio、mapRatio）。
- **算法流程**：输入token序列 → 对每层、每头、每个输出token，沿直线路径（从基线到实际输入）积分梯度 → 获得ATT和MLP的归一化贡献矩阵 → 组合为路径分解贡献 → 与整层IG贡献比较L2距离。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：PTB（Penn Treebank）开发集句子（Treebank-3，1700句子），用于层内L2一致性分析。
- **基准**：以整层IG（z2z）的三种基线变体（Zero、ITB、LAYER-ITB-zeroRatio）作为参考的“真实”贡献。没有与其他归因方法（如Attention Rollout、AttnLRP）进行定量比较，而是内部比较不同基线配置下路径组合与整层归因的接近程度。
- **对比方法**：论文自身提出的LIG方法内部的基线消融实验，对比了多种ATT基线（Zero、ITB、ITB-zeroRatio等）和MLP基线（Zero、ATTITB\(_{a=0}\)）的组合，共在三个参考块内各取前三名报告L2距离。

## 4. 资源与算力

- **未明确说明**：论文没有提及使用多少GPU、型号、训练时长或推理计算量。仅提到使用预训练的BERT-base-uncased（12层、12头、768维），在PTB开发集上运行IG积分（步长m=32）。由于需要逐层逐头计算梯度和积分，计算开销较大，但作者未量化资源需求。

## 5. 实验数量与充分性

- **实验数量**：
  - 主要定量实验为**表1**：针对三种整层参考基线，报告了每种下L2距离最小的前三组ATT×MLP基线组合。共涉及约3×3=9种配置（实际表内列出了9行），但论文提到“ATT/MLP baseline combinations are numerous”，仅展示了最优三组。
  - 可视化实验：图4展示了单个句子的12层token-to-token贡献矩阵（Layer 0到11），定性说明层间角色变化。
  - 未进行多数据集、多模型（如GPT、大语言模型）或与其他归因方法（如Attention Rollout、AttnLRP）的定量对比。
- **充分性评价**：
  - **局限**：实验仅在BERT-base和PTB单数据集上进行，规模较小；消融实验不完整（未列出所有基线组合的具体数值）；缺乏与现有方法的性能比较；没有统计显著性测试。
  - **客观性**：表1中L2距离的比较在相同参考块内是公平的，但跨块不能直接比较。论文对发现（ATT基线主导、MLP基线影响小）的解读是合理的，但支撑证据有限。

## 6. 论文的主要结论与发现

- **基线设计影响显著**：ATT基线的选择是影响分解与整层一致性的主导因素；MLP基线在Zero与ATTITB\(_{a=0}\)之间差异极小（L2变化≤0.001）。
- **最佳配置**：当整层参考为ITB时，使用ATT(ITB) × MLP(Zero)或ATT(ITB) × MLP(ATTITB\(_{a=0}\))的L2距离最小（0.214）。说明将ATT端ITB路径的端点（a=0）输出作为MLP基线有助于一致性。
- **层内角色层次化**：定性的可视化（图4）显示浅层贡献集中于局部邻域，中层出现远程语义联系，深层趋于全句均匀融合，支持注意力解释中的“局部到全局”层次结构。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：将IG从标量目标扩展到集到集映射，并巧妙结合L2标量化和LRP守恒思想，为层内模块边界级归因提供了统一框架。
- **无需重训练或定制规则**：仅需梯度访问，适用于任何可微Transformer模型，易于推广。
- **可诊断层内分解一致性**：通过比较路径组合与整层归因的L2距离，首次量化了ATT与MLP分离归因的合理性。
- **基线设计系统化**：明确研究了不同基线（零向量、输入自身、ATTITB等）对一致性的影响，并提出了合理的自我项补全策略。

## 8. 不足与局限

- **实验覆盖不足**：仅在BERT-base和PTB单数据集上验证；未测试其他架构（如GPT、T5）或更大模型；未与其他归因方法（如Attention Rollout、AttnLRP）进行定量对比。
- **计算开销大**：对每层、每头、每个输出token都需要数值积分（m=32个步长），可能不适用于超长序列或实时解释。
- **理论局限性**：直线路径可能穿过分布外区域（OOD），影响梯度稳定性；自我项补全策略（zeroRatio/mapRatio）的理论唯一性未证明。
- **可视化结论缺乏量化**：图4的层次角色认知仅为定性观察，未做统计检验或量化指标。
- **无法证明分解的真实性**：L2一致性仅表明路径组合与整层IG接近，但整层IG本身是否反应真实贡献尚存争议（归因方法固有的近似性）。

---

（完）
