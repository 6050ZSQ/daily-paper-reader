---
title: "ProbeScale: Probing Analysis to Optimize Neural Scaling Laws for Efficient Small Language Model Inference"
title_zh: "ProbeScale: 通过探针分析优化神经缩放律以实现高效的小语言模型推理"
authors: Sourav Das
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01806v1"
tags: ["query:llm-lang-rep"]
score: 9.0
evidence: 使用探测分析语言模型中编码的语言知识
tldr: "小语言模型（SLM）在严格资源约束下部署困难。ProbScale框架结合神经缩放定律与探针分析，通过任务特定探针量化每层相关性，在参数预算下选择最大化加权性能的层子集。在RoBERTa-Large和T5-Base上实现5-10倍参数缩减，性能保持95-98%，显著优于启发式基线。该方法为高效SLM推理提供了系统化工具。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01806v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 801, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01806v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 800, \"height\": 471, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01806v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1652, \"height\": 582, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01806v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 729, \"height\": 1023, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01806v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 879, \"height\": 528, \"label\": \"Table\"}]"
motivation: SLM在资源受限设备上部署时性能与效率难以平衡，现有缩放定律未直接指导子网络构建。
method: 利用探针量化每层对下游任务的相关性，构建优化问题在参数预算下选择最大化加权探针性能的层子集。
result: "在RoBERTa-Large和T5-Base上，子网络参数减少5-10倍，性能保持95-98%，优于常见启发式方法。"
conclusion: ProbScale有效融合缩放定律与探针分析，为SLM高效推理提供了实用且性能优异的子网络选择方案。
---

## 摘要
小语言模型（SLM）在能力与计算可行性之间取得了平衡。神经缩放律为其最优训练提供了指导，表明它们拥有随规模增长而丰富的内部表示。然而，即使在严格的资源约束下，部署这些SLM也可能具有挑战性。语言模型探针提供了分析模型内部所编码语言知识的方法。我们提出ProbScale框架，该框架统一了缩放律和探针分析的见解，用于识别预训练SLM中参数高效的子网络。ProbScale利用良好缩放的SLM的高质量表示，并使用任务特定探针数学量化每层对目标下游能力的相关性。这使得能够选择在性能与参数大小之间最优权衡的子网络。我们将子网络选择问题形式化为在参数预算下寻找最大化聚合的、任务加权探针性能的层子集。在RoBERTa-Large和T5-Base等代表性SLM上的实验表明，ProbScale能够识别出实现显著参数缩减（5到10倍）的子网络，同时在目标任务上保持高性能（原始SLM的95%到98%），优于启发式基线方法。

## Abstract
Small Language Models (SLMs) offer a balance between capability and computational feasibility. Neural scaling laws inform their optimal training, suggesting that they possess rich internal representations that scale with their size. However, deploying even these SLMs can be challenging under strict resource constraints. Language model probing provides methods for analyzing the linguistic knowledge encoded in a model's internals. We propose ProbScale, a framework that unifies insights from scaling laws and probing to identify parameter-efficient subnetworks within pre-trained SLMs. ProbScale utilizes the high-quality representations of well-scaled SLMs and uses task-specific probes to mathematically quantify the relevance of each layer for target downstream capabilities. This allows selecting subnetworks that optimally trade off performance against parameter size. We formulate the subnetwork selection as finding a layer subset maximizing aggregated, task-weighted probe performance under a parameter budget. Experiments on representative SLMs such as RoBERTa-Large and T5-Base demonstrate that ProbScale identifies subnetworks achieving significant parameter reduction, from 5 to 10 times, while maintaining high performance (95% to 98% of the original SLMs) on targeted tasks, outperforming heuristic baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：小语言模型（SLM）虽然比大语言模型（LLM）更适合资源受限场景，但在移动设备或延迟敏感服务器上部署仍然困难。现有模型压缩方法（如剪枝、量化）通常忽略模型内部语言学知识分布，或依赖启发式规则。如何在不显著损失性能的前提下，从预训练SLM中提取出参数高效的子网络，是一个关键挑战。
- **研究动机**：神经缩放律表明训练良好的SLM具有丰富的内部表示；语言模型探针技术可以分析每层编码的语言知识。将两者结合，有望指导子网络的选择。
- **整体含义**：提出ProbScale框架，利用探针量化每层对目标任务的重要性，在参数预算下选择最优层子集，从而实现5-10倍参数缩减，性能保持95%-98%。

### 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将探针性能作为层相关性的量化指标，将子网络选择形式化为在参数预算约束下最大化加权探针性能的优化问题。
- **关键技术细节**：
  - **任务定义与探针选择**：针对目标下游任务T，选择一组相关的探针属性集合PT（如情感分类任务直接用情感标签作为探针，NLI任务同时使用QNLI标签和POS标签），并为每个探针赋予权重w。
  - **逐层探针分析**：对每层l，提取表示hl，训练简单探针分类器（线性或浅层MLP）预测属性p，得到性能Vp,l。
  - **层相关性量化**：计算每层对任务T的综合得分Rl,T = Σ w(p,T)·Vp,l。
  - **预算约束的子网选择**：采用连续块策略，寻找大小为k（k受参数预算B限制）的连续层块，使得块内Rl,T之和最大。选择满足|ΘS|≤B的最大k对应的最优块S*。
  - **子网络提取与微调**：保留输入嵌入和任务头，用选中的层构建子网络MS，并在目标任务上微调几个epoch。
- **算法流程**（文字描述）：
  1. 输入：基础SLM, 目标任务T, 探针集合PT, 权重w, 参数预算B。
  2. 对每层l：提取表示，训练所有探针，得到Vp,l，计算Rl,T。
  3. 从k=1到kmax，遍历所有连续块，计算块得分，检查参数预算，记录最优块S*。
  4. 用S*构建子网络，微调后输出。

### 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：使用GLUE benchmark中的两个任务：
  - SST-2（情感二分类，准确率）
  - QNLI（问答自然语言推理，准确率）
- **base模型**：
  - RoBERTa-Large（24层，355M参数）
  - T5-Base（编码器12层，约110M参数，整体220M）
- **对比方法**：
  - 原始SLM（微调后的完整模型）
  - DistilRoBERTa（知识蒸馏基线，仅用于RoBERTa）
  - Top-k层（保留最后k层）
  - Uniform-k层（均匀间隔选择k层）
  - ProbScale（连续块选择，k=6和k=4两种预算）
- **评估指标**：任务准确率、参数数量|ΘS|、参数比例|ΘS|/|Θ|。
- **额外实验**：对T5-Base编码器做同样对比，验证跨架构泛化性。

### 4. 资源与算力

- **论文未明确说明使用的GPU型号、数量或具体训练时长**。仅在伦理考虑中提到“探针分析的计算成本是一次性的，可能低于完整模型训练或广泛的蒸馏”，未给出量化数据。
- **推断**：实验在普通学术GPU上应可完成，因为只训练简单的线性探针和短时微调（3 epoch），模型规模为百万级。

### 5. 实验数量与充分性

- **实验数量**：共两大类实验：
  - RoBERTa-Large上：SST-2和QNLI各测试两种预算（k=6和k=4），加上对比基线、原始模型和DistilRoBERTa，约3×2×3=18组（粗略）。
  - T5-Base上：同样两个任务，两种预算（k=4和k=6），对比Top和Uniform，约2×2×2=8组。
  - 合计约26组结果。
- **充分性评价**：
  - 覆盖了两种不同架构（encoder-only与encoder-decoder）、两种不同类型的任务（单句分类与句对推理）。
  - 对比了常见启发式基线（Top-k, Uniform-k）和蒸馏模型（DistilRoBERTa）。
  - 但缺少以下内容：①更多任务（如GLUE其他任务、生成任务）；②不同探针权重设置的消融；③非连续块选择的探索；④与结构化剪枝等其他压缩方法（如量化、知识蒸馏的变体）的直接对比。总体而言实验较充分但偏早期验证。

### 6. 主要结论与发现

- **ProbScale显著优于启发式基线**：在同等参数预算下，ProbScale选择的子网络准确率高于Top-k和Uniform-k。例如RoBERTa-Large k=6时，SST-2准确率94.9%（原始96.5%），比Top-6高1.1%，比Uniform-6高2.4%。
- **参数缩减与性能保持**：可减少5-10倍参数，性能保持原始模型的95%-98%（k=4时约97%）。
- **任务特定层重要性差异**：通过探针得分发现，不同任务峰值层不同（如SST-2偏向上中层，QNLI由于涉及句法，中层和上层都重要），ProbScale能正确选出对应区域，而Top-k和Uniform-k盲目。
- **跨模型泛化**：在T5-Base上观察到类似趋势，验证方法对不同架构有效。

### 7. 优点

- **创新性**：首次将神经缩放律与探针分析系统结合，将探针从分析工具转化为子网络选择的量化指导。
- **数学严谨性**：子网选择形式化为带约束的优化问题，目标明确。
- **实用性强**：仅需一次探针分析，后续微调成本低，适合资源受限部署。
- **可解释性**：每层相关性得分可直接可视化，提供模型内部功能洞察（如图1）。
- **性能优异**：在保持高性能的同时实现大幅参数压缩，优于常用启发式。

### 8. 不足与局限

- **实验覆盖有限**：仅测试了两个分类任务（SST-2、QNLI），未涉及生成任务（如文本摘要、机器翻译）或多任务场景。需更多验证来证明通用性。
- **探针选择与权重设定较简单**：本文使用均匀权重或直接任务探针，未系统研究不同探针组合和权重的影响，可能存在次优。
- **仅考虑连续块选择**：未探索非连续层选择或混合注意力头剪枝，可能错过更优子网络结构。
- **未与正交压缩方法结合**：如量化、剪枝等，本文仅对比了启发式层选择和蒸馏，缺少与SOTA结构化剪枝的直接比较。
- **偏置与公平性风险**：分析仅基于探针性能，未考虑子网络可能继承或放大预训练模型中的社会偏见（作者已提及但未处理）。
- **无算力记录**：相比许多LLM论文，本文未报告GPU小时数，影响可重复性评估。

（完）
