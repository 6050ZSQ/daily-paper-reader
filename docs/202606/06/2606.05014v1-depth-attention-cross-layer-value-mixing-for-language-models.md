---
title: "Depth-Attention: Cross-Layer Value Mixing for Language Models"
title_zh: 深度注意力：语言模型的跨层值混合
authors: "Boyi Zeng, Yiqin Hao, Zitong Wang, Shixiang Song, He Li, Feichen Song, Yifan Liu, Ziwei He, Xinbing Wang, Zhouhan Lin"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05014v1"
tags: ["query:llm-lang-rep"]
score: 8.0
evidence: 跨层值混合改进Transformer中的神经表示流动
tldr: "Transformer深层无法直接选择重用浅层表示，现有跨层方法增加推理状态。Depth-Attention在注意力模块内部实现跨层值混合，通过让查询关注同一位置前几层的键来混合值，替换原始值。在1.5B和3B参数的Qwen3解码器上，该方法以最低困惑度和最高下游准确率（提升达2.3点）超越基线，且不增加参数和KV缓存大小，仅增加不到0.01%的FLOPs。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05014v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1693, \"height\": 950, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05014v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 830, \"height\": 661, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05014v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 693, \"height\": 607, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05014v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 494, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05014v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1256, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1726, \"height\": 345, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 876, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 740, \"height\": 306, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 980, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 978, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1649, \"height\": 641, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1726, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1647, \"height\": 724, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1665, \"height\": 1061, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05014v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1655, \"height\": 659, \"label\": \"Table\"}]"
motivation: 让深层注意力能够灵活选择重用浅层表示，同时不增加推理开销和KV缓存大小。
method: 在注意力计算前，当前层查询对同一位置前几层的键做注意力，将混合后的值替换自注意力的原始值。
result: 在Qwen3 1.5B和3B模型上实现最低困惑度与最高平均下游准确率，较基线提升达2.3个点。
conclusion: 通过极小的计算代价实现跨层信息选择，优于基于隐藏状态的跨层方法，且保持原生KV缓存大小，适用于不同规模及循环Transformer。
---

## 摘要
自注意力可以在序列中自由选择信息，但在深度方向上，Transformer只是将每一层的输出添加到残差流中，因此后续层无法有选择地重用早期层的表示。最近的跨层方法改善了这种流动，但操作的是注意力之外的隐藏状态，在推理时增加了键值缓存之外的状态——随着现代大语言模型通过分组查询和多头潜在注意力压缩缓存，这种成本变得越来越显著。我们引入了深度注意力（Depth-Attention），它在注意力模块内部执行这种选择：在一层对序列进行注意力计算之前，其查询会关注同一词元位置上早期层的键，并将它们的值混合到自注意力随后读取的值中。由于深度注意力重用了标准的注意力查询、键和值缓存槽，用深度混合后的值替换原始值，因此它不增加参数，也不在标准键值缓存之外引入持久的推理状态——其缓存大小与普通解码器相同，并且少于基于隐藏状态的跨层方法。在1.5B和3B参数的Qwen3风格解码器上，深度注意力取得了最低的困惑度和最高的平均下游准确率，相比普通Transformer准确率提升高达2.3个百分点，并在困惑度和平均准确率上超越了强大的跨层基线，同时仅增加不到0.01%的额外算术FLOPs，且无额外的持久推理状态。这些收益在360M到3B参数范围内均成立，并扩展到了循环Transformer上。

## Abstract
Self-attention selects information freely across the sequence, but across depth, Transformers merely add each layer's output to the residual stream, so later layers cannot selectively reuse earlier-layer representations. Recent cross-layer methods improve this flow but operate on hidden states outside attention, adding state beyond the key-value cache at inference--a cost that becomes increasingly salient as modern LLMs compress the cache with grouped-query and multi-head latent attention. We introduce Depth-Attention, which performs this selection inside the attention module itself: before a layer attends over the sequence, its query attends over the keys of earlier layers at the same token position and mixes their values into the value that self-attention then reads. Because Depth-Attention reuses the standard attention queries, keys, and value-cache slots, storing depth-mixed values in place of the original values, it adds no parameters and introduces no persistent inference state beyond the standard key-value cache--the same cache size as a vanilla decoder and less than hidden-state-based cross-layer methods. On Qwen3-style decoders at 1.5B and 3B parameters, Depth-Attention attains the lowest perplexity and the highest average downstream accuracy, improving over the vanilla Transformer by up to 2.3 accuracy points and surpassing strong cross-layer baselines in perplexity and average accuracy, while adding under 0.01% extra arithmetic FLOPs and no additional persistent inference state. The gains hold from 360M to 3B parameters and extend to looped Transformers.

---

## 论文详细总结（自动生成）

# 论文深度总结：Depth-Attention: Cross-Layer Value Mixing for Language Models

## 1. 核心问题与整体含义（研究动机与背景）

- **问题**：标准 Transformer 解码器中，自注意力可以在序列维度上自由选择信息，但在深度（层间）维度上，信息流动缺乏选择性：每一层仅将输出加到残差流中，后续层无法直接、有选择地重用特定浅层层的表示。
- **动机**：现有跨层方法（如 DenseFormer、Attention Residuals、mHC）虽然改进了层间信息流动，但它们操作的是隐藏状态（hidden states），需要在推理时维护或重新计算这些状态，从而在标准 KV 缓存之外引入额外开销。随着现代 LLM 使用 GQA、MLA 等压缩缓存的方案，这种开销的占比越来越显著。
- **核心目标**：在不增加额外持久推理状态的前提下，让深层注意力能够灵活选择并重用浅层表示。

## 2. 方法论：核心思想、关键技术细节、公式流程

- **核心思想**：在自注意力模块内部执行跨层值混合。具体来说，在当前层对序列进行标准因果自注意力之前，利用当前层的查询（query）去关注同一词元位置上先前层的键（key），并根据注意力权重混合这些层的值（value），得到深度混合后的值（depth-mixed value），再用该值替换原始值，用于后续的因果自注意力。
- **关键技术细节**：
  - **深度注意力（Depth-Attention）**：对于第 ℓ 层第 t 个词元，其查询 *q*ₜ^ℓ 对先前层（包括当前层）在同一位置的键进行注意力计算，softmax 得到权重 *α*ₜ^ℓ,ⱼ，然后按权重混合当前层值 *v*ₜ^ℓ 与先前层的深度混合值 *ẽv*ₜ^ⱼ，得到 *ẽv*ₜ^ℓ。
  - **公式概要**：
    1. 计算深度注意力权重：*α*ₜ^ℓ,ⱼ = softmax( *q*ₜ^ℓ ⋅ *k*ₜ^ⱼ / √d )，其中 j ≤ ℓ。
    2. 混合值：*ẽv*ₜ^ℓ = *α*ₜ^ℓ,ℓ *v*ₜ^ℓ + Σⱼ<ℓ *α*ₜ^ℓ,ⱼ *ẽv*ₜ^ⱼ。
    3. 用混合后的值 *ẽV*^ℓ 执行标准因果自注意力：*O*^ℓ = CausalAttn(*Q*^ℓ, *K*^ℓ, *ẽV*^ℓ)。
  - **稀疏性**：为避免深度源集随层数增长，采用步长 stride 策略，默认 stride = L/2（层数的一半），只关注当前层及每隔 s 层的浅层，保证计算量 O((ℓ/s+1)*dₖᵥ) 远小于 O(T²) 的序列自注意力。
  - **兼容 GQA**：将每组查询的平均值作为深度注意力的查询，在 KV 头分辨率上运行，进一步减少计算和内存。
  - **零额外参数和持久状态**：混合后的值直接存入标准 V 缓存槽，替换原始值，不增加任何参数或额外缓存大小。

## 3. 实验设计

- **数据集**：预训练使用 Pile 数据集（32B tokens）。下游评估包含 8 个任务：LAMBADA、PIQA、WinoGrande、SciQ、HellaSwag、ARC-Easy、ARC-Challenge、RACE。
- **Benchmark 与对比方法**：
  - 基线包括 vanilla Transformer、manifold hyper-connections (mHC)、Attention Residuals、DenseFormer。
  - 模型架构基于 Qwen3 风格，规模为 1.5B 和 3B 参数，以及缩放实验中的 360M、500M、710M。
- **评估设置**：零样本和 5-shot 评估，报告验证困惑度（PPL）和下游任务准确率。

## 4. 资源与算力

- **文中未明确说明使用的 GPU 型号、数量或训练时长**，仅提及训练使用 FP16 精度，且所有模型在相同协议下训练（32B tokens，相同优化器、学习率调度等）。因此无法确定具体算力投入。
- 作者提到“由于计算资源有限”，缩放实验仅做到 3B 参数和 32B 训练 tokens，但认为方法本身没有本质限制。

## 5. 实验数量与充分性

- **实验数量**：论文进行了多组实验：
  1. **主实验**：在 1.5B 和 3B 两个尺度上对比四种方法（Vanilla、mHC、Attention Residuals、DenseFormer 和自己方法），报告零样本和五样本结果，共 2 个表格。
  2. **效率分析**：理论 FLOPs/I/O 对比（表 2）、训练 wall-clock 对比（表 3）、推理耗时和内存对比（表 4）。
  3. **缩放行为**：从 360M 到 3B 四个模型尺寸，验证 Loss 下降趋势（图 2）。
  4. **循环 Transformer**：500M 循环 Transformer 上的额外实验。
  5. **权重可视化**：分析深度注意力权重分布（图 3）。
  6. **消融实验**：在 500M 模型上对步长（stride）、混合规则（均匀 vs 自适应 softmax）、混合对象（只混合值 vs 键值混合）进行 3 组消融。
- **充分性评价**：实验设计较为全面，覆盖了不同模型规模、多种跨层基线、效率定性和定量分析、消融分析等。对比方法设置合理（均按照原论文推荐参数配置）。但下游任务数量（8 个）和模型规模上限（3B）仍有局限。总体而言，实验充分且客观公平，支持主要结论。

## 6. 主要结论与发现

- Depth-Attention 在 1.5B 和 3B 尺度上均取得最低困惑度和最高平均下游准确率（zeroshot 分别提升 2.34 和 1.93 个点，five-shot 分别提升 2.07 和 2.01 个点），优于 vanilla Transformer 和所有跨层基线。
- 方法仅增加不到 0.01% 的额外算术 FLOPs，训练 wall-clock 开销约 +9%~11%，推理耗时增加约 +1.18%，且不增加持久推理内存（KV 缓存大小不变）。
- 收益在 360M 到 3B 参数范围一致成立，并扩展到循环 Transformer 场景（Loss 从 2.208 降至 2.194）。
- 消融表明：使用步长 s = L/2 最佳；自适应 softmax 混合优于均匀混合；只混合值（value-only）优于混合键或键值。

## 7. 优点

- **创新性**：首次将跨层信息选择完全嵌入注意力模块内部，利用已有的 Q、K、V 和缓存结构，零参数开销。
- **高效性**：理论计算开销极小（稀疏深度注意力），实际推理内存无增加，训练墙钟开销可控。
- **兼容性**：直接适用于 GQA 和循环 Transformer，无需修改框架。
- **实验设计清晰**：消融实验充分论证各设计选择的有效性，效率分析包含理论推导和实际基准。
- **可扩展性**：缩放实验显示收益随模型大小保持稳定，具有良好的迁移潜力。

## 8. 不足与局限

- **实现层面**：当前实现没有融合到注意力内核中，导致训练墙钟开销（9%~11%）高于理论 FLOPs 表征（0.004%）。作者期待未来专门的内核融合能进一步缩小差距。
- **规模限制**：实验局限于 3B 参数和 32B 训练 tokens，尚未验证在更大模型（如 7B、70B）和更多数据下的收益。作者承认此限制源于计算资源有限。
- **设计局限**：仅混合值状态，且深度源集使用固定步长；未探索可学习的源选择或更丰富的跨层交互。消融显示仅混合值是最优的，但这限制了方法的通用性。
- **下游任务多样性**：只使用了 8 个 NLP 任务，未涵盖多模态、编程、数学推理等场景，通用性有待进一步检验。
- **与后期训练方法的兼容性**：未讨论微调（SFT/RLHF）或持续预训练情景下的表现。

（完）
