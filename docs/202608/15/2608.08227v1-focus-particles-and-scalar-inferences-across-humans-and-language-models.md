---
title: Focus particles and scalar inferences across humans and language models
title_zh: 焦点粒子与人类和语言模型中的标量推理
authors: "Catherine M. Brousse, Nelu D. Radpour"
date: 2026-08-08
pdf: "https://arxiv.org/pdf/2608.08227v1"
tags: ["query:llm-lang-rep"]
score: 9.0
evidence: 人类与大语言模型在焦点算子标量表征上的对比研究
tldr: 焦点粒子“甚至”和“仅仅”在形式语义中依赖替代集合产生标量推理。为考察这种表征是否稳定且可推广，本文使用约100个句子，要求人类被试和大型语言模型分别进行标量判断。初步结果显示，两者的判断输出较为相似，但进一步的对比表明，相似行为可能源于完全不同的内在机制。这一发现提示，行为层面的一致不能直接推断为共享的语义表征。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.08227v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 888, \"height\": 558, \"label\": \"Figure\"}]"
motivation: 检验人类与LLM对焦点粒子是否产生一致的标量推理，从而验证形式语义的普遍性。
method: 构建约100个含焦点粒子的句子，要求人类被试和多个LLM进行标量判断并对比。
result: 人类与LLM在标量判断上表现出相似输出，但分析提示二者依赖的潜在机制并不相同。
conclusion: 焦点粒子引发的标量推理在行为层面具有一致性，但机制差异提醒我们不能将模型行为直接等同于人类语义能力。
---

## 摘要
诸如 "even" 和 "only" 之类的焦点粒子在形式语义学理论中占据核心地位，这些理论假设了基于备选集合的结构化表征。"Even" 突出意外或极端的备选项，而 "only" 则强制排他性。如果这样的标量表征是稳健且可泛化的，它们应当在不同的语境和系统中产生一致的判断。在本工作中，我们测试了人类和大型语言模型（LLMs）是否能从包含这些粒子的句子中构建稳定的标量表征。使用一个包含约100个条目的数据集，参与者与模型被要求做出标量判断。初步结果表明，人类与LLMs之间的相似输出可能源自不同的底层机制。

## Abstract
Focus particles such as "even" and "only" are central to formal semantic theories that posit structured representations over sets of alternatives. "Even" highlights unexpected or extreme alternatives, while "only" enforces exclusivity. If such scalar representations are robust and generalizable, they should give rise to consistent judgments across contexts and systems. In this work, we test whether humans and large language models (LLMs) construct stable scalar representations from sentences containing these particles. Using a dataset of approximately 100 items, participants and models were asked to make scalar judgments. Preliminary results suggest that similar outputs across humans and LLMs may arise from different underlying mechanisms.

---

## 论文详细总结（自动生成）

根据论文提供的文本，以下是对该研究的中文详细总结：

## 1. 论文的核心问题与整体含义

- **核心问题**：人类和大型语言模型（LLM）在理解包含焦点粒子 `even`（甚至）和 `only`（仅仅）的句子时，是否会形成稳定的标量推理？这种推理是受空间表征驱动，还是由语义/评价性结构驱动？
- **研究动机**：
  - 焦点粒子是形式语义学的核心研究对象，它们不改变句子的命题内容，但通过引入“备选集合”上的期望来调整句子的推论含义（如 `even` 强调意外或低概率，`only` 强调排他性）。
  - 学术界对标量判断的底层机制存在争议：一种观点认为标量判断依赖于空间编码（如左右/上下维度上的数量表征），另一种观点则认为由极性对应原则和语义评价结构决定。
  - LLM 展现出结构化语言表征能力，但其底层机制（双向注意力、无具身体验）与人类不同，因此对比人类与 LLM 的标量判断可以检验相似行为是否源于共享表征原则。
- **整体含义**：如果行为和判断模式在空间布局变化下仍保持稳定，则支持语义/评价性机制；同时，即使 LLM 在聚合水平上复现人类模式，其内部机制与人类可能存在本质差异，不能简单将模型行为等同于人类认知能力。

## 2. 论文提出的方法论

- **核心思想**：通过操控响应量表的空间方向和标签映射，比较人类被试与 LLM 在 `even`/`only` 句子上的能力评分，判断标量判断是否受空间因素影响。
- **实验刺激**：60 个描述人物执行能力相关任务的句子（如 "Mike can bake a cake"），其中 30 个含 `even`，30 个含 `only`。
- **量表设计**：5 点 Likert 量表，按两个维度操纵：
  - 空间格式：水平 vs. 垂直。
  - 标签映射：标准（低能力在左/下，高能力在右/上） vs. 反转（标签对调）。
  - 组合形成四种条件：水平-标准、水平-反转、垂直-标准、垂直-反转。
- **人类数据采集**：108 名被试，随机分配到四种 Qualtrics 问卷之一。
- **LLM 数据采集**：
  - 模型：Llama 3.3 70B（开源，700亿参数），通过 Groq API 访问。
  - 提示词结构模仿人类问卷，并要求模型输出 1–5 的整数。
  - 每个提示在 temperature = 1.0 下运行 20 次，共 60 个句子 × 4 种条件 × 20 次 = 4800 个观测值。
- **统计方法**：使用线性混合效应模型，固定效应包括 Particle（Even/Only）、Source（Human/LLM）、Format（Horizontal/Vertical）、Mapping（Standard/Reversed），随机截距为被试和项目。随后使用估计边际均值进行对比，并进行温度扫描（temperature 0.0–2.0，每条件 20 次采样）检验温度对变异的影响。

## 3. 实验设计

- **数据集/场景**：自建 60 个能力句，分含 `even` 与 `only` 两组；四类响应量表配置；人类在线问卷 + LLM API 查询。
- **基准/对比**：
  - 人类 vs. LLM 的评分对比。
  - 不同空间格式（水平/垂直）和标签映射（标准/反转）之间的对比。
  - 温度扫描实验（0.0–2.0）作为额外稳定性检验。
- **未对比其他模型**：论文仅使用一个 LLM（Llama 3.3 70B），没有与多个模型或不同规模模型比较。

## 4. 资源与算力

- 论文中未明确说明使用的 GPU 型号、数量或训练时长。仅提到通过 Groq API 调用 Llama 3.3 70B 进行推理。
- 由于是推理任务而非训练，算力需求相对较低，但具体硬件资源未披露。

## 5. 实验数量与充分性

- **主要实验**：一组人类-LLM 对比实验，覆盖 60 个句子 × 4 种量表条件，人类 108 名，LLM 每条件重复 20 次。
- **辅助实验**：针对子集的温度扫描（0.0–2.0，每条件、每温度 20 次采样），检验温度对变异性的影响。
- **充分性评估**：
  - 优点：对空间格式和标签映射进行了完全交叉设计，能有效分离空间因素与语义因素；温度扫描提供了额外证据。
  - 不足：仅使用单个 LLM、单一任务类型（能力判断）、单一语言（英语），样本量中等；缺少对项目级内容（如任务难度、语义上下文）的系统控制；实验类型单一，未涉及其他焦点粒子或其他推理任务；缺乏跨模型泛化验证。

## 6. 论文的主要结论与发现

- 人类和 LLM 均对 `even` 与 `only` 的标量含义敏感：`only` 句获得更高能力评分，`even` 句获得更低评分。
- 关键发现：改变响应量表的方向或标签映射并未消除或反转该模式，说明标量判断主要由语义/评价内容驱动，而非空间组织。该结果更支持“极性对应/评价性机制”而非“空间编码”假设。
- LLM 与人类存在质性和量性差异：
  - LLM 评分离散且更极端：`only` 条件下 100% 给出量表最大值（5），`even` 条件的变异性也远小于人类。
  - 即使在 temperature = 1.0 下重复采样，LLM 依然产生高度一致的响应；温度扫描（0.0–2.0）也显示响应稳定，说明单纯提高温度无法产生类人的个体差异。
- 结论：行为层面的相似性（人类与 LLM 都区分 `even`/`only`）可能掩盖了不同的底层机制。LLM 的确定性输出反映的是围绕单一固定判断的噪声，而人类的变异性来自真实的个体差异。因此，当前 LLM 是否适合作为人类认知模型值得质疑。

## 7. 优点

- **实验设计巧妙**：通过同时操纵空间格式和标签映射，清晰地区分了“空间表征”与“语义/评价表征”两个理论假设。
- **比较框架新颖**：将形式语义学的焦点粒子研究与 LLM 行为分析结合，提供了一个跨系统验证语义普遍性的窗口。
- **引入温度扫描**：系统检查了采样温度对响应变异的影响，对“temperature 越高越像人”的常见假设提出了实证质疑。
- **统计方法规范**：使用线性混合效应模型，考虑了随机效应，并报告了效应量和显著性。

## 8. 不足与局限

- **实验覆盖面有限**：仅包含 60 个句子，且只涉及能力判断场景；未包括其他类型的焦点粒子、其他语义环境或跨语言材料。
- **模型单一**：仅使用 Llama 3.3 70B，未在多个 LLM（不同架构、规模、训练数据）上验证结论的普适性。
- **样本量中等**：人类的 108 名被试虽足够检测主效应，但对个体差异的建模可能不足。
- **响应方式不对等**：人类使用 5 点 Likert 量表，LLM 被强制输出整数，可能影响比较的公平性；另外，LLM 对量表标签的解读可能与人不同。
- **缺乏机制层面的证据**：论文仅通过行为输出推测机制，没有深入分析 LLM 的内部表征（如 attention 模式或隐藏状态），也未对人类的心理过程进行过程级测量（如反应时、眼动等）。
- **应用限制**：结论提示 LLM 不能很好地模拟人类个体差异，因此在实际应用中（如作为用户行为模拟器、认知模型）需谨慎。

（完）
